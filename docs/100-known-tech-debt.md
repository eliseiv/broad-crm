# 100 · Реестр технического долга

Формат: `TD-NNN`. Осознанные упрощения Этапа 1. Любой `TODO`/`FIXME` в коде/docs должен ссылаться на запись отсюда или на `Q-NNN-N`.

| ID | Долг | Причина (Этап 1) | Предлагаемое решение | Приоритет |
|----|------|------------------|----------------------|-----------|
| TD-001 | Нет soft-delete и аудит-лога серверов | Простота, один админ | Таблица аудита + soft-delete при многопользовательском режиме | low |
| TD-002 | node_exporter не удаляется с целевого сервера при `DELETE` | Снятие таргета достаточно для мониторинга | Плейбук `uninstall_node_exporter` | low |
| TD-003 | Нет повторного запуска провижининга при `error` (retry) | Минимизация scope | Endpoint `POST /api/servers/{id}/reprovision` | medium |
| TD-004 | Провижининг не масштабируется на несколько воркеров | Монолит, один процесс ([ADR-006](adr/ADR-006-async-provisioning-bez-brokera.md)) | Очередь задач (Celery/RQ) + блокировки | medium |
| TD-005 | Rate-limit входа — in-memory (не переживает рестарт, не распределён) | Один воркер | Вынос в Redis | low |
| TD-006 | Нет ротации `FERNET_KEY` | Простота | `MultiFernet` (новый+старый ключ), процедура ротации | medium |
| TD-007 | SSH host key checking выключен (`ANSIBLE_HOST_KEY_CHECKING=false`) | Новые серверы без known_hosts | Управление known_hosts / fingerprint-pinning ([Q-SEC-2](99-open-questions.md)) | medium |
| TD-008 | ~~Нет кэша метрик~~ | — | **resolved**: реализован короткий TTL-кэш (`METRICS_CACHE_TTL_SEC`=5) + single-flight + семафор конкурентности + ретраи ([modules/monitoring](modules/monitoring/README.md#устойчивость-read-path-нормативно)) | — |
| TD-009 | Нет UI смены пароля **супер-админа** (`.env`) | Супер-админ — bootstrap вне БД ([ADR-008](adr/ADR-008-admin-iz-env.md)+[ADR-021](adr/ADR-021-rbac-users-roles.md)): пароль только через `.env`/деплой by design | **Сужен** ([ADR-021](adr/ADR-021-rbac-users-roles.md)): UI-управление паролями реализовано для **БД-пользователей** (`users.password_hash`, bcrypt; создание/сброс на странице «Пользователи»). Остаётся только супер-админ (`.env`) — при желании ввести `ADMIN_PASSWORD_HASH`/UI-смену для него | low |
| TD-010 | Grafana — только datasource, нет преднастроенного дашборда node_exporter | Минимизация scope Этапа 1; drill-down через Explore | Автопровижининг дашборда node_exporter в Grafana | low |
| TD-011 | LE-продление через certbot standalone даёт кратковременный downtime (`proxy` останавливается на время валидации :80) | Простой выпуск без reverse-proxy-интеграции | Zero-downtime: webroot-режим certbot или ACME-companion (без остановки `proxy`) | low |
| TD-012 | CSP содержит `style-src 'unsafe-inline'` | Tailwind/Radix используют inline-стили; без этого UI ломается ([05-security.md](05-security.md#content-security-policy-spa-location-)) | Перейти на nonce/hash для стилей (CSP3 `style-src-attr`/`'unsafe-hashes'` или сборка без inline-стилей) | medium |
| TD-013 | CPU `detail` показывает только число ядер, без частоты (GHz) | Частота (`node_cpu_scaling_frequency_*hertz`) недоступна на многих VM → разнобой; стандартизировано на ядра ([Q-MON-1](99-open-questions.md)) | Опционально добавить частоту как доп. поле, когда доступна (не вместо ядер) | low |
| TD-014 | runtime-smoke (поднятие стека до healthy + проверка health/SPA) не автоматизирован в CI | Этап 1 — ручной/полуавтоматический прогон по чек-листу ([07-deployment.md §CI/CD](07-deployment.md#cicd)) | Автоматизировать в CI: `docker compose up` + ожидание healthy + проверки `GET /api/health`/SPA | medium |
| TD-015 | Frontend-проверки не покрыты CI (линт/typecheck/сборка/`vitest`/e2e) | Решение пользователя: в CI только backend-качество + deploy; SPA валидируется сборкой образа на сервере ([07-deployment.md §CI/CD](07-deployment.md#cicd)) | Вернуть в CI frontend lint/typecheck/build + тест-гейты (vitest ≥70 %, e2e) на этапе qa | medium |
| TD-016 | Нет автоматизированного e2e-провижининга (реальный SSH-хост в CI) с проверкой `up=1` | Требует эфемерного Linux+sshd+systemd в CI; моки скрыли отсутствие `sshpass` и обрыв цепочки скрейпа ([06-testing-strategy.md](06-testing-strategy.md)) | Автоматизировать прогон плейбука против тест-хоста с password-SSH + проверку реального `up=1`; на старте — обязательная контейнерная проверка состава backend-образа (ansible/sshpass/ssh) | medium |
| TD-017 | Авто-открытие `:9100` на цели покрывает только ufw/firewalld | **Реализуется** (шаг 6 плейбука открывает `9100` для `SCRAPE_SOURCE_IP` через ufw/firewalld, graceful skip — [09-provisioning.md](09-provisioning.md#шаг-6--открытие-firewall-на-цели-нормативно-реализует-devops)). Остаточный зазор: нестандартные firewall (nftables напрямую, iptables-only, облачные security groups) и тестовое покрытие firewalld | Поддержать nftables/iptables; покрыть e2e-тестом ufw и firewalld | low |
| TD-018 | Нет гранулярной деградации per-server при недоступности Prometheus и нет нагрузочных тестов read-path | Этап 1: устойчивость покрыта кэшем/single-flight/ретраями, но per-server fallback и load-тесты не реализованы | Гранулярная деградация (последнее известное значение per-server со stale-меткой); нагрузочные тесты polling × вкладки против Prometheus | low |
| TD-019 | ~~State Telegram-нотификатора — in-memory (рестарт backend сбрасывает)~~ | — | **resolved** ([ADR-014](adr/ADR-014-persist-notifier-state-alert-on-first-elevated.md)): состояние персистится в таблице `notifier_server_state` (`online` + `zone_cpu/ram/ssd` per server, [03-data-model.md](03-data-model.md#таблица-notifier_server_state)), читается из БД каждую итерацию → переживает рестарт/деплой; правило **alert-on-first-elevated** шлёт ровно один catch-up-алерт для currently-«горячих» серверов. Redis не понадобился | — |
| TD-020 | Точный баланс/остаток средств по AI-ключу недоступен | Провайдеры (OpenAI/Anthropic) не отдают биллинг по обычному API-ключу; детектируем только валидность и признак исчерпания квоты (`insufficient_quota`/429) через `GET /v1/models` ([ADR-010](adr/ADR-010-ai-key-monitor-vnutri-backend.md), [modules/ai-keys](modules/ai-keys/README.md#проверка-ключа-у-провайдера-нормативно)) | При появлении провайдерских billing-API — показывать остаток/лимит; уточнять причину квоты | low |
| TD-021 | Фиксированный глобальный интервал проверки AI-ключей; нет ручного «проверить сейчас» | Минимизация scope Этапа 1 ([ADR-010](adr/ADR-010-ai-key-monitor-vnutri-backend.md)); интервал глобальный (`AI_KEY_CHECK_INTERVAL_SEC`). **Ротация/редактирование ключа (`PATCH /api/ai-keys/{id}`) реализованы** ([ADR-011](adr/ADR-011-poryadok-blokov-server-side-dnd-kit.md)) — из долга исключены | Настраиваемый интервал per-key; endpoint ручной проверки «проверить сейчас» | low |
| TD-022 | Перестановка карточек (drag-and-drop) недоступна с клавиатуры | Этап 1: основной жест — pointer с delay-активацией (~200 мс); клавиатурный `KeyboardSensor` @dnd-kit опущен ради минимального scope ([ADR-011](adr/ADR-011-poryadok-blokov-server-side-dnd-kit.md)). Редактирование и удаление карточек с клавиатуры доступны | Включить @dnd-kit `KeyboardSensor` + русские screenreader-анонсы (взятие/перемещение/отпускание); покрыть a11y-тестом (NFR-8) | medium |
| TD-024 | Модуль «Почты»: **нет server-side полнотекстового поиска** по письмам. (Newest-first и фильтры по ящику/команде — **resolved**.) | С [ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) письма **хранятся в БД CRM** (`mail_messages`), т.е. прежняя причина («read-through-прокси без хранения» + внешний API поиска не отдаёт) **снята** — остался чисто реализационный пробел: полнотекстового индекса и эндпоинта поиска нет. Ретенция агрегатора больше не ограничивает архив (письма живут в CRM). | Полнотекстовый индекс (`tsvector` GIN по `subject`/`body_text`) + параметр поиска в `GET /api/mail/messages`. Данные уже есть — нужен индекс и эндпоинт. | medium |
| TD-025 | Модуль «Почты»: **фоновый poll новых писем** (авто-prepend без действия пользователя) не реализован — свежие письма подтягиваются перезагрузкой ленты. | Осознанно отложено как v1-опция: `useInfiniteQuery` не даёт безопасного prepend без риска регрессии порядка/авто-выбора письма. Не дефект — порядок `internal_date DESC, id DESC` и контракт соблюдены. **Смягчено [ADR-044](adr/ADR-044-mail-full-merge-into-crm.md):** о новом письме пользователь узнаёт **Telegram-уведомлением** (push + `MailDispatcherService`), т.е. «не заметить письмо» нельзя. | Неагрессивный фоновый запрос ленты **без `before`** по `env.pollIntervalMs`; prepend писем, которых нет в наборе, с дедупом по `id`, без нарушения порядка и авто-выбора. | low |
| TD-026 | Модуль «Почты»: **`cid:`-изображения** (инлайн-вложения, `<img src="cid:…">`) в теле письма не отрисовываются. | Вложения **не переносятся в CRM by design** ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md), [TD-034](#)) → данных для резолва `cid:` нет. Remote https-картинки отрисовываются ([ADR-015](adr/ADR-015-csp-img-src-remote-mail-images.md)); `cid:` — отдельный класс (инлайн-вложение), CSP тут ни при чём. | Появится только вместе с вложениями (отдельный ADR): резолвить `cid:` в `data:`-URI перед инъекцией в srcDoc. | low |
| TD-027 | Durable-лог алертов (`notifier_alert_log`, [ADR-018](adr/ADR-018-notifier-windowed-offline-recovery-alert-log.md)): нет ретенции/автоочистки старых строк; логируются только серверные алерты (не AI-ключей); нет API-эндпоинта просмотра | Этап 1: лог append-only, объём ограничен числом серверов × частота алертов (незначим); AI-key-алерты — отдельный сервис ([ADR-010](adr/ADR-010-ai-key-monitor-vnutri-backend.md)); UI-просмотр вне scope ([03-data-model.md](03-data-model.md#таблица-notifier_alert_log)) | Периодическая очистка `DELETE ... WHERE created_at < :cutoff` (по `ix_notifier_alert_log_created_at`); опционально — логирование AI-key-алертов в тот же лог; read-only API-эндпоинт «последние алерты» (+ индекс по `server_id`) | low |
| TD-028 | Страница «Прокси» ([ADR-019](adr/ADR-019-proxies-availability-monitor.md)): (а) фиксированный глобальный интервал проверки (`PROXY_CHECK_INTERVAL_SEC`), нет ручного «проверить сейчас»; (б) нет windowed-детекта доступности — единичный транзиентный сетевой сбой backend может дать ложную пару 🔴/🟢 (смягчено ретраями внутри проверки); (в) на «Дашборде» нет карточки «Прокси» (сводный счётчик Работает/Не работает); (г) микроскопическое окно гонки: алерт 🔴/🟢 может отправиться для прокси, удалённого ровно во время in-flight проверки (снимок сделан до `DELETE`) — поведение наследуется от `AiKeyMonitorService` by-design | Минимизация scope Этапа 1 ([ADR-019](adr/ADR-019-proxies-availability-monitor.md)): монитор по образцу AI-ключей ([ADR-010](adr/ADR-010-ai-key-monitor-vnutri-backend.md)), интервал глобальный; недоступность прокси = отслеживаемое событие → провал проверки сразу `error` (без `unknown`), транзиентность гасится ретраями; дашборд-карточка вне scope этой итерации; окно гонки удаления (г) — остаточное, наследуется от ai-keys, вероятность ничтожна | (а) Настраиваемый интервал per-proxy + endpoint «проверить сейчас»; (б) windowed-детект по образцу [ADR-018](adr/ADR-018-notifier-windowed-offline-recovery-alert-log.md) при шумных ложных срабатываниях; (в) добавить карточку «Прокси» на «Дашборд» ([ADR-017](adr/ADR-017-dashboard-client-aggregation-mail-server-filters.md)); (г) при желании — гейтить отправку alert по rowcount атомарного `UPDATE ... update_check` (пропускать алерт, если строка уже удалена; наследуется от ai-keys) | low |

| TD-029 | Страница «Бэки» ([ADR-020](adr/ADR-020-backends-healthcheck-monitor.md)): (а) фиксированный глобальный интервал проверки (`BACKEND_CHECK_INTERVAL_SEC`), нет ручного «проверить сейчас»; (б) фиксированный путь `/health` и схема `https://` — нет per-backend health-path/схемы; (в) нет windowed-детекта доступности — единичный транзиентный сетевой сбой backend может дать ложную пару 🔴/🟢 (смягчено ретраями внутри проверки); (г) на «Дашборде» нет карточки «Бэки» (сводный счётчик Работает/Не работает); (д) микроскопическое окно гонки: алерт 🔴/🟢 может отправиться для бэка, удалённого ровно во время in-flight проверки (снимок сделан до `DELETE`) — наследуется от `ProxyMonitorService`/`AiKeyMonitorService` by-design; (е) нет allowlist доменов healthcheck (SSRF hardening): монитор выполняет `GET https://{domain}/health` на произвольный домен, заданный оператором | Минимизация scope Этапа 1 ([ADR-020](adr/ADR-020-backends-healthcheck-monitor.md)): монитор по образцу прокси ([ADR-019](adr/ADR-019-proxies-availability-monitor.md)); интервал/путь глобальны; недоступность бэка = отслеживаемое событие → провал проверки сразу `error` (без `unknown`), транзиентность гасится ретраями; дашборд-карточка вне scope; окно гонки удаления (д) — остаточное, вероятность ничтожна | (а) Настраиваемый интервал per-backend + endpoint «проверить сейчас»; (б) per-backend health-path/схема; (в) windowed-детект по образцу [ADR-018](adr/ADR-018-notifier-windowed-offline-recovery-alert-log.md) при шумных ложных срабатываниях; (г) карточка «Бэки» на «Дашборд» ([ADR-017](adr/ADR-017-dashboard-client-aggregation-mail-server-filters.md)); (д) гейтить отправку alert по rowcount атомарного `UPDATE`; (е) allowlist доменов healthcheck (SSRF hardening) — при появлении недоверенных операторов; сейчас домен задаёт только админ, https-only, `follow_redirects=False` | low |
| TD-030 | Модуль «СМС» ([ADR-030](adr/ADR-030-sms-module-full-merge.md)): (а) **исторические SMS-данные старой SMS-БД не мигрируются** — таблицы `sms_*` стартуют пустыми (BIGINT→UUID id-маппинга команд/юзеров нет); (б) rate-limit публичных webhook/`telegram/link` полагается на существующий in-memory лимитер CRM (наследует [TD-005](#), не переживает рестарт/не распределён); (в) `label` номера — системное зеркало Twilio `friendly_name`, авто-обновляется только при `sync` (нет реакции на изменение `friendly_name` в реальном времени) | Первый перенос: минимизация scope ([ADR-030](adr/ADR-030-sms-module-full-merge.md) §2) — движок и модель важнее исторических строк; ETL BIGINT→UUID требует ручного маппинга команд/пользователей старой БД на CRM `teams`/`users` и выходит за рамки переноса. Rate-limit — in-memory достаточно при NFR-1 (один воркер). `label` — детерминированное зеркало, редкое изменение | (а) Отдельная ETL-задача импорта исторических `phone_numbers`/`inbound_sms`/`deliveries` с явным маппингом team/user id (старый BIGINT → CRM UUID); (б) вынести rate-limit webhook/link в durable-хранилище вместе с [TD-005](#); (в) периодическая ресинхронизация `label` или webhook Twilio на изменение номера | low |
| TD-031 | Модуль «СМС» ([ADR-030](adr/ADR-030-sms-module-full-merge.md)): **гонка двойной доставки (at-least-once)**. `sms_ingest_service.py` + retry-монитор (`sms_delivery_monitor_service.py`): `pending()` выбирает `status IN ('pending','failed')`, включая только что зарезервированные (`try_reserve`), ещё не отправленные pending-доставки. Если fan-out webhook'а не успевает отправить за интервал монитора (`SMS_DELIVERY_RETRY_INTERVAL_SEC=60`), монитор параллельно повторит ту же доставку → возможен **дубль** SMS оператору в Telegram. Нет `SELECT ... FOR UPDATE SKIP LOCKED` / промежуточного статуса `sending` / lease. | Осознанно (semantics at-least-once): корректность сохранена (UNIQUE `(inbound_sms_id, telegram_user_id)` не даёт дубль-строку доставки; дублируется только сам Telegram-месседж). Вероятность при текущем масштабе низкая — окно отправки sub-second против 60с интервала монитора; растёт под нагрузкой/при большой команде/медленном Telegram | Claim с промежуточным статусом `sending` + `lease`/`updated_at`-таймаут (монитор берёт только `failed` и «протухшие» `sending`), либо `SELECT ... FOR UPDATE SKIP LOCKED` при выборке кандидатов; альтернативно — не включать «свежие» pending (моложе lease-порога) в выборку монитора | low |
| TD-032 | Модуль «СМС» ([ADR-030](adr/ADR-030-sms-module-full-merge.md)): **синхронный fan-out в теле webhook**. `sms_webhooks.py`: `handle_incoming_sms` отправляет всем получателям команды **последовательно ДО возврата `200` Twilio**. При большой команде/медленном Telegram Bot API ответ webhook может превысить таймаут Twilio (~15с) → Twilio-ретрай того же `MessageSid`. | Осознанно: корректность сохранена (дедуп по `MessageSid` через partial-UNIQUE + идемпотентный `try_reserve` + retry-монитор добьёт недоставленное) — ретрай Twilio не создаёт дублей записей и добирает получателей. Простота Этапа 1 (нет фоновой очереди задач). Проблема проявляется только на крупных командах/деградации Telegram | Персистить SMS и вернуть `200` Twilio **сразу**, fan-out вынести в фоновую задачу (`asyncio.create_task`/lifespan-воркер по образцу retry-монитора); при необходимости — параллелить отправки (`asyncio.gather` с ограничением конкурентности). Пересекается с TD-031 (общий claim-механизм фоновой доставки) | low |
| TD-033 | Модуль «Почты» ([ADR-038](adr/ADR-038-mail-headless-integration.md)): **1:1 команда↔группа** — `teams.mail_group_id INTEGER NULL UNIQUE` привязывает CRM-команду ровно к одной группе агрегатора. Команде нельзя привязать ящики из **нескольких** групп агрегатора (`UNIQUE` не даёт одной команде указать >1 группу; одна группа — максимум одной команде). | Осознанно ([ADR-038](adr/ADR-038-mail-headless-integration.md) §2): 1:1 покрывает текущую модель «одна команда = один почтовый поток», минимизирует scope (одно поле, без таблицы связей и фонового sync). Источник истины владения ящиком — агрегатор (`mail_accounts.group_id`); CRM только маппит команду↔группу. | Per-mailbox владение через таблицу `mail_account_teams` в CRM (M:N команда↔ящик) ЛИБО расширение до `teams ↔ mail_group_id` M:N; резолв `MailScope.group_ids` тогда собирается по нескольким группам команды. Вводить при появлении требования мультигрупповых команд. | low |
| TD-035 | Секреты бэка (`api_key`/`admin_api_key`, [ADR-040](adr/ADR-040-backend-relations-secrets-reverse-lookup.md)): **очистка секрета через UI не реализована**. В форме edit `AddBackendModal` пустое поле секрета = «не менять» (по образцу прокси/AI-ключа); явной кнопки «Очистить» нет, поэтому убрать уже заданный `api_key`/`admin_api_key` из UI нельзя (FK `server_id`/`ai_key_id` очищаются опцией «Не выбрано»; `git`/`note` — очисткой поля, т.к. не секреты). API-контракт **очистку поддерживает** (`PATCH` с `api_key: null`/`""` → `NULL`) — не реализован лишь UI-аффорданс. | Осознанно ([ADR-040](adr/ADR-040-backend-relations-secrets-reverse-lookup.md)): пустое=не-менять устраняет случайное затирание секрета; массовая очистка ключей на Этапе 1 не требуется | Добавить в форму явный контрол «Очистить API KEY/ADMIN API KEY» (чекбокс/иконка), отправляющий `api_key: null` в `PATCH` | low |
| TD-034 | Модуль «Почты» ([ADR-038](adr/ADR-038-mail-headless-integration.md)): **вложения писем недоступны из CRM**. Внешний DTO агрегатора вложений не отдаёт, MinIO агрегатора в CRM не проксируется → в ленте `/mail` нельзя открыть/скачать attachment; `cid:`-инлайн-картинки не резолвятся (наследует [TD-026](#)). | Осознанно ([ADR-038](adr/ADR-038-mail-headless-integration.md), read-through без хранения): вложения пользователем на этапе headless-интеграции не требуются; проксирование бинарных объектов MinIO — отдельная поверхность (SSRF/объёмы/стриминг). | При появлении требования — прокси-эндпоинт вложений CRM → external attachments-API агрегатора (когда тот добавит выдачу вложений во внешний DTO) со стримингом и лимитом размера; тогда же резолв `cid:` → `data:`/прокси-URL (снятие TD-026). | low |

| TD-037 | Модуль «Почты» ([ADR-043](adr/ADR-043-lazy-mail-group-provisioning.md)): **группа без команды** (осиротевшая группа агрегатора). Два источника: (а) удаление CRM-команды с непустым `mail_group_id` **не** удаляет/не открепляет группу и её ящики в агрегаторе → группа становится непривязанной (ящики продолжают синк, но без командной видимости в CRM); (б) редкая **пустая** сирота от ленивого провижининга — проигравшая CAS-ветка гонки или read-timeout между созданием группы и записью привязки ([ADR-043](adr/ADR-043-lazy-mail-group-provisioning.md) §3/§4); best-effort `DELETE /api/external/teams/{id}` реклеймит её сразу, но при сбое DELETE остаётся. **Отлично от [TD-036](#)** (команда без группы) и [TD-033](#) (модельное 1:1): TD-037 — **группа без команды**. | Осознанно ([ADR-043](adr/ADR-043-lazy-mail-group-provisioning.md)): (а) каскадное удаление ящиков чужого сервиса при удалении CRM-команды — отдельная поверхность (перенос/удаление ящиков — админ-операция); (б) пустая сирота безвредна (0 ящиков ⇒ 0 видимости/доставки), CAS даёт «одна команда — одна группа» строго, цена — редкая пустая группа. | (а) при удалении команды — предложить админу перенести/удалить ящики + `DELETE /api/external/teams/{mail_group_id}` (только пустую); (б) периодический cleanup-скан пустых непривязанных групп (парный агрегатор `TD-048`). Взять реализацию при появлении потребности. | low |

| TD-038 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §10): итоговый каталог тегов — **16 глобальных** (10 канонических + 6 org-специфичных, повышенных до глобальных: `Поддержка`/`Билд в коннекте`/`Small Business`/`Билд не дошёл`/`Ждет Ревью 2`/`Билд не приняли`). **Личные копии** канонических тегов (дубликаты) НЕ переносились; `mail_message_tags` НЕ переносились — разметка воспроизведена **пере-применением** 16 тегов по всему корпусу (движок матчинга портирован побуквенно). **Уточнение [ADR-047](adr/ADR-047-mail-fix-pack.md) §1:** признак `is_builtin` **упразднён** (колонка дропнута миграцией `0023`) — деление на «встроенные/кастомные» больше не существует, **любой** тег удаляется, сева при старте нет. | Осознанно (владелец: теги глобальные, создаёт только админ). Ревизия исходного «персональные не переносим»: 2284 привязки висели на персональных тегах — терять было нельзя. | Каталог тегов полностью в руках админа: добавление/удаление — через UI. | low |
| TD-039 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md)): пользователи CRM **без членства в команде** на момент миграции (`Никита`/`Анна`/`Юля`) — их `mail_telegram_links` перенесён с `user_id`, но уведомления пойдут только после включения в команду (права на почту выводятся из членства `user_teams`). | Осознанно (владелец «позже раскидает по командам»); корректное поведение, а не потеря. | Владелец добавляет пользователей в команды; линк уже есть → доставка включится автоматически. | low |
| TD-040 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §8): **переадресация лидеру (forwarding) ОТЛОЖЕНА — решение владельца.** Правило прода `Команда Ивана → springtechco99@gmail.com` **не реализуется на старте и не переносится как активный канал**. **⚠️ Явно зафиксировано: с момента cut-over и до реализации пересылка входящих на почту лидера НЕ работает** (в агрегаторе `group_forwarding` снимается, ADR-0043). Спринт S5 удалён из плана. Таблицы `mail_forwarding`/`mail_message_forwards` миграциями `0021`/`0022` **НЕ созданы** (заводятся вместе с реализацией). **Правило прода для повторного ввода при реализации:** `team_id` = CRM «Команда Ивана», `forward_to` = `springtechco99@gmail.com`, `is_active=true`. | Осознанно (решение владельца): вторичный канал (1 правило), приоритет — основной перенос. | Реализовать `mail_forwarding`/`mail_message_forwards` (миграция) + форк на `/ingest` (образец агрегаторского ADR-0034, минус вложения) + ввести правило выше при возврате приоритета. | medium |
| TD-041 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §3): push `/api/mail/ingest` с **неизвестным `mail_account_id`** роняет письмо (счётчик `unknown_mailbox`, батч не отклоняется). | Осознанно: все ящики заводятся через CRM (каталог существует до первого письма) → unknown — редкая аномалия (гонка/рассинхрон); отклонять батч = вечный ретрай агрегатора. | Логировать аномалию; при появлении — авто-подтяжка каталога ящика из агрегатора либо алерт. | low |
| TD-042 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §6): ящик без `team_id` (unassigned) / команда без участников → уведомлений не получит никто (super-admin-фолбэк не вводится без решения владельца). **Известное следствие переноса:** `Business 2` **маппится штатно** (`mail_group_id=8`, 3 ящика получают `team_id`), НО в CRM у неё **0 участников** → уведомления по её ящикам временно не идут никому (раньше — Юля `@yuliya_2704`, теперь вне команд); `Алтынай` (`ashaykenova`) в «Команде Мухамеда», но без Telegram-привязки — до `/start`. | Осознанно (владелец добавит участников/привяжется позже). | Добавить участников в `Business 2`; `Алтынай` пишет боту `/start`. | low |
| TD-043 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §6): **push-бот команды — fire-and-forget** (без `mail_telegram_notifications`-трекинга/идемпотентности/recovery), паритет агрегаторского TD-041. Само письмо не теряется (в БД CRM + основной бот с дедупом); теряется только дублирующее уведомление в admin-канале при рестарте/429. | Осознанно (admin-канал «второй экран»). | Лёгкий трекинг доставки push-бота + recovery при значимых пропусках. | low |
| TD-044 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §6): `MailDispatcherService` шлёт получателям **последовательно** в цикле (без параллелизма/лимита конкуренции), паритет SMS [TD-032](#). При большой команде/медленном Telegram итерация растягивается. | Осознанно: масштаб мал, дедуп-таблица гарантирует корректность. | `asyncio.gather` с ограничением конкуренции при росте нагрузки. | low |
| TD-045 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §3): push-батч агрегатор→CRM `POST /api/mail/ingest` ограничен только по **количеству** писем (`MAIL_INGEST_MAX_BATCH=100` — CRM `backend/app/config.py`; `CRM_PUSH_BATCH_SIZE=100` — агрегатор `shared/config.py`), но **НЕ по суммарному размеру в байтах**. `client_max_body_size 50m` ([07-deployment.md §reverse-proxy](07-deployment.md#reverse-proxy-nginx--требования), `frontend/nginx/default.conf`) покрывает реальный корпус (худший батч ~5.8 МБ), но теоретический патологический батч из 100 писем с телами близко к `2×MAX_BODY_BYTES` (агрегатор `shared/config.py`, `MAX_BODY_BYTES=1_048_576`; `body_text`/`body_html` усечены **независимо**) превысит любой разумный nginx-лимит. | Осознанно: эмпирически недостижимо на текущем корпусе (max одиночное письмо ~103 КБ); count-лимит + запас nginx достаточны на Этапе 1. | Резать батч по **суммарному размеру в байтах** на стороне агрегатора (`worker/app/crm_push_dispatch.py`), а не только по count. Зона — **агрегатор**. | low |
| TD-046 | Модуль «Почты» ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md)): косметический дрейф `pushed_at` в БД **агрегатора** для **24 писем** (id 2032–2068, `fetched_at` ~2026-06-10). Во время 413-шторма (nginx резал `/api/mail/ingest`, [TD-045](#)) они выпали из Redis push-очереди и оказались **старше** 30-дневного recovery-окна (`CRM_PUSH_RECOVERY_WINDOW_HOURS=720` — агрегатор `shared/config.py`) → `crm_push_recovery_scan` их не переenqueue'ит, `pushed_at` остаётся `NULL`. **Данные НЕ потеряны:** все 24 присутствуют в CRM (перенесены миграцией, `notified_at` проставлен, повторных уведомлений не будет) — проверено. Расхождение чисто косметическое (агрегатор не знает, что письма уже в CRM). | Осознанно, **no-action**: строки уйдут по ретенции (`RETENTION_DAYS=30` — агрегатор `shared/config.py`); чинить не требуется. | Задокументировать; опц. ручной backfill `pushed_at=now()` для этих id в агрегаторе. Зона — **агрегатор**. | low |
| TD-047 | Модуль «Почты» ([ADR-045](adr/ADR-045-mail-outlook-oauth-headless-reonboarding.md) §3): при OAuth-подключении Outlook сбой server-to-server-уведомления `POST /api/mail/oauth/ingest` (сеть/`410 oauth_state_expired` — консент завершён после TTL `crm_state.exp`) оставляет ящик **созданным в агрегаторе, но без CRM-каталога/привязки к команде**. Письма такого ящика роняются на `/api/mail/ingest` (`unknown_mailbox`, [TD-041](#)), пока каталог не подтянут. | Осознанно: агрегатор ретраит `/oauth/ingest` (connect-only, upsert идемпотентен) → штатно долетает; истечение `crm_state.exp` — редкий кейс (консент дольше TTL). | reconcile-подтяжка каталога ящиков из агрегатора (`GET /api/external/mailboxes`) с ручным назначением команды админом; либо увеличить TTL `crm_state`. Пересекается с [TD-041](#). | low |
| TD-048 | Модуль «Почты» ([ADR-045](adr/ADR-045-mail-outlook-oauth-headless-reonboarding.md) §3): признак **`oauth_needs_consent`** Outlook-ящика (известен агрегатору) **не зеркалится в CRM** → нет серверного индикатора «нужно переподключить». Кнопка «Переподключить Outlook» в detail-строке OAuth-ящика доступна вручную, без подсветки просроченного consent. | Осознанно: на старте OAuth e2e не подтверждён (наследует агрегаторский TD-031); ручного переподключения достаточно. | Зеркалить `oauth_needs_consent` в `mail_accounts` CRM (доп. поле в status-канале `POST /api/mail/mailbox-status` или в `/oauth/ingest`) → бейдж «Требуется переподключение» на вкладке «Почты». | low |
| TD-049 | Модуль «Почты» ([ADR-045](adr/ADR-045-mail-outlook-oauth-headless-reonboarding.md)): **корпоративные O365/Workspace-ящики не подключаются** — app-passwords часто отключены администратором домена, а OAuth рабочего тенанта для headless-connector недоступен (Azure App настроен на personal-аккаунты, `OUTLOOK_TENANT=consumers`). Поддерживаются только личные Outlook (`@outlook.com`/`@hotmail.com`/`@live.com`) через OAuth и провайдеры с app-passwords. Наследует агрегаторский **TD-031** (OAuth e2e не подтверждён на реальном Azure App). | Осознанно: текущая потребность — личные ящики; корпоративный OAuth-consent (work/school tenant, admin-consent) — отдельная поверхность. | При потребности — multi-tenant Azure App (`OUTLOOK_TENANT=common`/`organizations`) + admin-consent flow; отдельный ADR (расширяет [ADR-045](adr/ADR-045-mail-outlook-oauth-headless-reonboarding.md)). Зона — **агрегатор** (Azure App) + CRM. | low |
| TD-051 | **`teams.mail_group_id` — мёртвый легаси-остаток** ([ADR-047](adr/ADR-047-mail-fix-pack.md) §0). [ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §2 предписал drop колонки **отдельной post-ETL-миграцией** — она **не выполнена**: колонка жива (`backend/app/models/team.py`), поля `mail_group_id` остались в контракте `/api/teams` (`TeamCreateRequest`/`TeamUpdateRequest`/`TeamListItem`), уникальный констрейнт `uq_teams_mail_group_id` продолжает давать `409 team_mail_group_taken`. **Для почты колонка НИЧЕГО не значит** — ни один mail-путь её не читает (владение ящиком = `mail_accounts.team_id`). | Осознанно: снятие поля из **публичного** контракта `/api/teams` + миграция — отдельная задача с риском поломать клиентов; на почту остаток не влияет (мёртвый, не «неверный»). | Миграция `DROP CONSTRAINT uq_teams_mail_group_id; DROP COLUMN mail_group_id;` + удаление поля из `schemas/team.py`, `team_service.py`, `team_repository.py` и из `04-api.md §Teams`; заодно снять `409 team_mail_group_taken`. | medium |
| TD-052 | **`mail_accounts.display_name` — производное (денормализованное) поле** ([ADR-047](adr/ADR-047-mail-fix-pack.md) §3.3): дублирует склейку `"<number> <app_name>"`, пересчитывается сервером при каждом create/update ящика. Рассинхрон возможен, если строку правят в обход сервиса (прямой SQL/миграция). | Осознанно: `display_name` — **единственная** форма имени во внешнем контракте агрегатора (`POST/PATCH /api/external/mailboxes`); хранить производное дешевле, чем менять чужой сервис или склеивать на каждом исходящем вызове. | Либо генерируемая колонка БД (`GENERATED ALWAYS AS`), либо склейка строго в момент исходящего вызова (без хранения) — когда/если агрегатор примет `number`/`app_name`. | low |
| TD-053 | **Перечень бэков в Telegram-алертах обрезается на 10** ([ADR-046](adr/ADR-046-ui-infra-fix-pack.md) §1, `MAX_ALERT_BACKENDS=10`): при большем числе бэков на сервере/ключе печатаются первые 10 (порядок `position ASC, code ASC`) + строка `… и ещё N`. | Осознанно: лимит Telegram — 4096 символов на `sendMessage`; без явного лимита Telegram обрезал бы сообщение **молча, посередине строки**. Полный список всегда виден в detail-модалке сервера/ключа (секция «Бэки»). | При росте числа бэков — либо разбивать алерт на несколько сообщений, либо выносить перечень в callback-кнопку «Показать все бэки». | low |
| TD-054 | **`PATCH /api/backends/order` и колонка `backends.position` — UI их больше не вызывает** ([ADR-046](adr/ADR-046-ui-infra-fix-pack.md) §2а): DnD на `/backends` убран, порядок карточек задаёт клиентская сортировка по `name`. Эндпоинт и колонка **сохранены** в контракте/БД. | Осознанно: удаление эндпоинта+колонки — необратимая правка контракта ради нулевой выгоды. `position` **продолжает работать**: задаёт порядок ответа `GET /api/backends` и порядок [перечня бэков в алертах](modules/backends/README.md#формат-сообщений-telegram-точно-нормативно--источник-истины). | Если решат, что порядок бэков не нужен нигде — снять эндпоинт, колонку и сортировку по `position` одной задачей (тогда пересмотреть и порядок перечня в алертах). | low |
| TD-055 | **Тип правила тега `sender_exact` недоступен для СОЗДАНИЯ из UI** ([ADR-047](adr/ADR-047-mail-fix-pack.md) §2, решение владельца): опция убрана из выпадающего списка создания правила. Тип **остаётся** в БД/контракте (CHECK `ck_mail_tag_rules_type` четырёхзначный), существующие правила (напр. канонический тег «Диспут» = `sender_exact AppStoreNotices@apple.com`) **работают и корректно отображаются** («Отправитель равен»). | Осознанно (владелец): тип избыточен для повседневной работы, но удалять его из модели нельзя — сломало бы CHECK и существующую разметку прода. | Вернуть опцию в список создания ЛИБО (при подтверждённой ненужности) мигрировать существующие `sender_exact`-правила в `sender_contains` и сузить CHECK. | low |
| TD-056 | Модуль «Почты», вкладка «Почты» ([ADR-047](adr/ADR-047-mail-fix-pack.md) §4): **in-row контрол выбора КОМАНДЫ** — нативный `ui/Select`, который **физически не переносит** текст выбранной опции. Имя команды длиннее **≈35 символов** упирается в край контрола средствами браузера (это НЕ `truncate`/`overflow-hidden` — их в коде нет, правило CLAUDE.md не нарушено). Смягчено: контрол `w-full min-w-[14rem]` растёт вместе с колонкой `w-[22rem]`; на контроле — `title` с полным именем; раскрытый список опций браузер показывает полностью. Не-admin видит статичное значение — оно переносится целиком. **⚠️ Уточнено [ADR-052](adr/ADR-052-mail-mailbox-combobox.md) (2026-07-13): примитив `ui/Combobox` теперь СУЩЕСТВУЕТ** (введён для поля поиска/выбора почты; опция панели переносится `break-words`) — прежняя формулировка «кастомный dropdown = **новый** примитив ДС, которого нет» **устарела**. Долг **не закрыт**: селектор **команды** остаётся `ui/Select` и по-прежнему не переносит длинное имя. | Осознанно: миграция in-row селектора команды на `ui/Combobox` — отдельная правка вкладки с собственными тестами; на проде имён такой длины нет. Прежнее обоснование («пришлось бы заводить примитив с нуля + менять `ui/Select` на всех страницах») **более не действует** — примитив есть, массовая миграция `ui/Select` не требуется ([ADR-052](adr/ADR-052-mail-mailbox-combobox.md): `ui/Select` остаётся дефолтом ДС). | Мигрировать **in-row селектор команды** вкладки «Почты» на существующий **`ui/Combobox`** (`mode='select'`, опции — команды). Точечная задача frontend; массовая миграция `ui/Select` **не нужна**. | low |
| TD-057 | **Нет общего примитива `ui/Collapsible`** ([ADR-049](adr/ADR-049-servers-backends-card-first-detail.md) §5). Сворачиваемый блок (кнопка-триггер + `aria-expanded`/`aria-controls` + `ChevronDown` с `rotate-180` + монтирование содержимого только при раскрытии) реализован **вручную минимум в шести местах**: `DetailInfoSection` (`DetailFields.tsx`), `BackendsDetailSection.tsx`, `CollapsibleSection` (`TeamDetailPanel.tsx`), аккордеон карточки команды (`TeamsPage.tsx`), `MailHelpAccordion` (`MailboxFormModal.tsx`), секция «Информация» формы бэка (`AddBackendModal.tsx`) — плюс два новых по [ADR-049](adr/ADR-049-servers-backends-card-first-detail.md) (секция «Бэки» на `ServerCard`, блок «Информация» на `BackendCard`). A11y-контракт дублируется, расхождения ловятся только ревью. Побочно: **UX-неоднородность** — `/backends` перешла на card-first (detail-модалки нет), а `/servers`/`/proxies`/`/ai-keys` сохраняют detail-модалку ([ADR-035](adr/ADR-035-detail-view-secret-reveal.md)). | Осознанно: заведение примитива = рефакторинг 6–8 компонентов и их тестов — раздуло бы пакет фиксов владельца ([ADR-049](adr/ADR-049-servers-backends-card-first-detail.md), альтернатива 4). Унификация всех четырёх карточных страниц под card-first потребовала бы разворота нормы ещё на трёх страницах, где detail-модалка несёт смысл (inline-edit имени сервера, reveal-строки, секция «Бэки» ключа). | Ввести `components/ui/Collapsible` с единым a11y-контрактом (`trigger`/`content`, `aria-expanded`/`aria-controls`, ленивое монтирование) и мигрировать на него все места. Отдельная задача frontend-рефакторинга. | low |
| TD-059 | **[ADR-053](adr/ADR-053-mail-timeouts-error-passthrough.md) реализован ЧАСТИЧНО: backend + devops — да, SPA и тесты — нет.** Сверено с кодом 2026-07-14 (правки не закоммичены). **✅ Сделано:** `config.py` — 4 таймаут-параметра с `ge/le` (`:157`/`:159`/`:163`/`:166`), кросс-полевой `model_validator` (`:176-201`), константы `MAIL_CLEANUP_DEADLINE_SEC` (`:30`) / `CRM_REQUEST_OVERHEAD_SEC` (`:33`) / `NGINX_PROXY_READ_TIMEOUT_SEC` (`:37`); `mail_client.py` — `MailTimeout` со `status_code` (`:92`/`:105`), `504` → `MailTimeout(status_code=504)` (`:372`), собственный таймаут → `status_code=None` (`:254`, `:303`), ctor `read_timeout_sec`+`deadline_sec` (`:142-148`), пер-вызовный override (`:236-244`), две фабрики (`:387`, `:402`); `errors.py` — 5 фабрик (`:382`, `:401`, `:414`, `:426`, `:438`); `mail_service.py` — второй клиент (`:196-202`), уборка с `deadline_sec=MAIL_CLEANUP_DEADLINE_SEC` (импорт `:30`, вызов `:972`) и catch-лист `except (MailUnavailable, MailRejected, MailTimeout)` (`:973`); `deps.py` — провязка обоих клиентов (`:411`, `:413`); `frontend/nginx/default.conf` — `proxy_read_timeout/send_timeout 120s` (`:126-127`); оба env-примера. **❌ Остаток долга:** (1) **SPA** (`frontend/src/features/mail/*`) не обрабатывает `504 mail_timeout` и новые `422`-коды и не показывает прогресс-состояние «Проверяем соединение — это может занять до двух минут…» ([ADR-053](adr/ADR-053-mail-timeouts-error-passthrough.md) §4) — без него легальное ожидание до 105 с читается как зависание; (2) **тестов нет ни одного** — вся машинная защита (кросс-полевой валидатор, различение источника таймаута, единственный источник константы уборки) не покрыта. | Не осознанное упрощение, а **остаток реализации**: backend/devops-часть закрыта в том же проходе, SPA и тесты вынесены отдельными задачами. | **`frontend`** — §4 UX: прогресс-состояние (кнопка `disabled` + спиннер + подпись «до двух минут») и обработка `504`/`422 mail_imap_failed`/`mail_smtp_failed`/`mail_invalid_host`/`502 mail_send_failed`; клиентский таймаут SPA вводить **нельзя** (§4). **`qa`** — кейсы [06-testing-strategy.md](06-testing-strategy.md#интеграционные) (⚠️ суб-секундные бюджеты инъектируются в **клиента** через override `get_mail_service`, а НЕ в `Settings`). **Отдельно (второй репозиторий `D:\BA\mail-agregator`, НЕ входит в этот TD):** их `ADR-0047` accepted, но **код к нему не приведён** — долг ведут они (`TD-055`). Кросс-репо-рассинхрон **документов** — **закрыт** (проверено грепом): их ADR несёт бюджет ЗАПРОСА `85+15+5=105 < 120`, звено `90` помечено отменённым, висячей ссылки `ADR-053 §5.1` нет. | **high** |
| TD-062 | **Ответ на письмо (`POST /api/mail/messages/{id}/reply`) НЕ РАБОТАЕТ на проде с cut-over (10.07.2026), и кросс-репозиторный контракт отправки не покрыт ни одной машинной проверкой.** Факт (проверено по коду + прямым запросом к проду 2026-07-14): CRM зовёт `POST /api/external/mailboxes/{id}/send` (`backend/app/infra/mail_client.py:225`), **которого в агрегаторе нет** (там реализован только message-scoped `POST /api/external/messages/{id}/reply`) ⇒ внешний `404` ⇒ пользователь получает ошибку (и вводящую в заблуждение: CRM маппила внешний `404` в `mail_message_not_found` — `mail_service.py:1123-1124`, хотя письмо у неё есть). Разрыв прожил 4 дня незамеченным: **нет контрактного теста/смоука, сверяющего пути и схемы клиента CRM с реальными роутами агрегатора**; ни один тест не ходит по этому пути end-to-end. Норма — [ADR-057](adr/ADR-057-mail-send-contract-fix.md); парный долг агрегатора — их `TD-059`. | Не осознанное упрощение, а **дефект**: контракт между двумя репозиториями менялся при cut-over ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md) §8) в docs, но у CRM не было ни одного гейта, проверяющего, что вторая сторона его реализовала. Класс дефекта общий: любое изменение внешнего API молча ломает CRM в рантайме. | **`backend` (CRM, немедленно):** по [ADR-057](adr/ADR-057-mail-send-contract-fix.md) §2/§3 — отдельная внутренняя схема ответа агрегатора (`{smtp_message_id}`), `MailReplyResponse.sent_id` = `mail_sent_messages.id` (`uuid`), внешний `404` → `mail_mailbox_not_found`; **по §5/§6** — `MailServerSendResult.smtp_message_id` **опционален** (`str | None`), строка `mail_sent_messages` создаётся при любом `200` (аудит не теряется), `MailReplyResponse.smtp_message_id` → `str | None`, логи reply получают обязательный контекст (`mail_account_id`/`message_id`, у `mail_send_missing_smtp_message_id` — ещё `sent_id`). **`frontend`:** тип `sent_id` в `frontend/src/types/api.ts:350` (`number` → `string`) и `smtp_message_id` (`:356`, `string` → `string | null`). **Агрегатор (их зона):** реализовать `send` — их Фаза A2.1 (`ADR-0048`). **`qa`:** контрактный тест клиента (`mail_client` строит именно те пути/тела, что описаны в [04-api.md](04-api.md) и в агрегаторском `04-api-contracts.md` §4f-send) + post-deploy smoke реальной отправки. **Закрытие — только после подтверждённой отправки письма с прода.** | **high** |

## Закрытые / устаревшие

| ID | Статус |
|----|--------|
| TD-023 | **Закрыт** (2026-07-13, frontend + architect): обрезка числового значения детали метрики на узких вьюпортах **устранена**. Прежнее «решение» (контейнерное усечение, `overflow-hidden` на под-карточке) противоречило правилу [CLAUDE.md](../CLAUDE.md) («переполнение решается размером, а не скрытием значимого контента») и было прод-багом (768: `clientWidth=72` при `scrollWidth=84` — хвост числа не читался). Фикс — **размером/раскладкой**: `overflow-hidden` и `whitespace-nowrap` сняты; padding под-карточки ужат (`px-1 … xl:px-2.5`, `frontend/src/components/MetricSubCard.tsx:50`), шаг сетки метрик ужат (`gap-2 xl:gap-3`, `frontend/src/components/ServerCard.tsx:203`); крайний случай — **перенос**, не обрезка. Формат (десятичные ГБ) и `grid-cols-3` сохранены — вариант «целые ГБ» не понадобился. Проверено фактическим рендером: 0 обрезанных значений на 390/768/1279/1280/1440 в обеих темах. Норма — [08-design-system.md §Под-карточки метрик](08-design-system.md#под-карточки-метрик). |
| TD-033 | **Закрыт** ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md)): 1:1 команда↔группа снято — ящик привязан к команде напрямую (`mail_accounts.team_id`), группы агрегатора упразднены. |
| TD-036 | **Устарел** ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md)): групповая модель ([ADR-043](adr/ADR-043-lazy-mail-group-provisioning.md)) упразднена целиком — групп в агрегаторе нет, ленивого провижининга нет, `teams.mail_group_id` для почты **мертва** (владение ящиком — `mail_accounts.team_id`), поэтому «backfill `mail_group_id`» смысла не имеет. **⚠️ Колонка при этом физически НЕ удалена** (предписанный ADR-044 §2 post-ETL-drop не выполнен) — этот остаток ведёт **[TD-051](#)**, а не TD-036. |
| TD-037 | **Устарел** ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md)): «группа без команды» невозможна — групп в агрегаторе нет. |
| TD-050 | **Закрыт** ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §6.3, 2026-07-14). Долг («селектор «Команда» в форме ящика показывает опцию «Без команды» **всем**, не-admin по клику получает `403`») оказался **хуже, чем описан**: у роли с полным `mail:*`, но **без `teams:view`**, список команд приходил **пустым** (`GET /api/teams` под `require("teams","view")` — `backend/app/api/teams.py:27-31`) ⇒ admin-only опция «Без команды» оставалась **единственной** ⇒ **создать ящик было невозможно вовсе** (прод-баг владельца 2026-07-14). **Норма:** источник команд канала не-админу — **`GET /api/auth/me`** (`mail_teams`/`sms_teams`), **не** `GET /api/teams`; опция «Без команды» в форме ящика **предлагается к выбору только admin-уровню** (исключение — «зеркало текущего состояния» в `disabled`-режиме `edit` бесхозного ящика: опция **отображается**, но выбрать её нельзя, [ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §6.3.1). Принцип: *вариант, который пользователь не вправе выбрать, не предлагается к выбору*. Правило распространено на **все** контролы со списком команд (форма ящика, резолв имени команды в таблице «Почты», `Select` переноса номера) — [ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §6.3, [08-design-system.md](08-design-system.md#единый-источник-команд-канала-на-клиенте--не-только-фильтр-нормативно-adr-055-63). |
| TD-060 | **Заведён и закрыт в один проход** ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §3.2, 2026-07-14): дыра найдена при разборе ADR-055 и **устранена его же нормой** — как «открытый» долг эта запись никогда не существовала (в реестре появляется сразу закрытой; ссылки «прежний TD-060» в ADR-055 указывают на **эту** запись, отдельной открытой строки искать не нужно). Исходная формулировка дыры: «не-админ может снять команду с номера и выбросить его из своего же scope» (самоблокировка). **Дыра оказалась шире.** Разбор показал вторую, **более опасную** сторону: `transfer_number` валидировал целевую команду **только на существование** (`backend/app/services/sms_number_service.py:94-97` — `get_existing_ids` → `sms_team_not_found`), **не** проверяя её принадлежность scope актора ⇒ носитель `sms:transfer` мог вынести попавший в его scope номер в **любую** существующую команду, включая ту, где не состоит (в связке с доп-командами/флагом «Без команды» — циклически перекладывать номера между произвольными командами). **Норма ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §3.2):** (1) номер ∈ scope → иначе `403`; (2) `team_id=null` не-админу — **только при `sms_includes_unassigned`** (иначе `403`; самоблокировка исключена — при флаге бесхозный номер остаётся видим); (3) `team_id=<uuid>` не-админу — целевая команда обязана ∈ `SmsScope.team_ids`, иначе `403` (проверка scope **первая** ⇒ несуществующая команда не-админу тоже `403` — анти-энумерация); admin-уровню несуществующая → `404 sms_team_not_found`. |
| TD-058 | **Закрыт** ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §5.1/§6, 2026-07-14): `GET /api/auth/me` отдаёт **`mail_teams`/`sms_teams`** (эффективный scope канала) и `*_includes_unassigned` ⇒ фронт наполняет фильтр «Команда» **без права `teams:view`** (прежняя причина долга снята); у актора admin-уровня `mail_teams`/`sms_teams` содержат **все** команды системы ⇒ `/api/auth/me` — **единственный** источник опций для всех акторов. Фильтр рендерится при **(команд канала + «Без команды») ≥ 2** — едино на **пяти** экранах: `/mail` (обе вкладки), `/sms` («Сообщения») и **обе Mini App**; гейт «только admin» ([ADR-036](adr/ADR-036-sms-team-filter-admin-only.md), [ADR-050](adr/ADR-050-mail-search-team-filter-personal-read-state.md) §1.2) отменён. |
| TD-034 | **Переклассифицирован** ([ADR-044](adr/ADR-044-mail-full-merge-into-crm.md)): вложения **не переносятся by design** (MinIO агрегатора убирается); при появлении требования — отдельный ADR. |
| TD-061 | **Закрыт** (2026-07-14, frontend + qa). Долг («подсказки под полями формы не связаны с контролом — примитивы выводили `aria-describedby` только при ошибке, у `ui/Combobox` атрибута не было вовсе, потребители рендерили подсказку несвязанным `<p>`») устранён **полностью, включая тесты**. **Норма** — [08-design-system.md §Подсказка под полем формы](08-design-system.md#подсказка-под-полем-формы-связывается-с-контролом-нормативно-a11y-nfr-8). **Реализация:** единый хелпер композиции `composeDescribedBy` (`frontend/src/lib/a11y.ts:10-15` — порядок «подсказка, затем ошибка»; пустой список → `undefined`, висячий IDREF исключён) + примитив подсказки `ui/FieldHint` (`frontend/src/components/ui/FieldHint.tsx:16-22`). Все **пять** примитивов формы получили проп `hint` и композируют атрибут: `ui/Input.tsx:43`/`:60`, `ui/Select.tsx:52`/`:76`, `ui/Textarea.tsx:45`/`:56`, `ui/MultiSelect.tsx:76`/`:100`, `ui/Combobox.tsx:336`/`:435` (у `Combobox` слота ошибки нет ⇒ один id; a11y-контракт примитива расширен аддитивно — [ADR-052 §Последствия](adr/ADR-052-mail-mailbox-combobox.md#последствия)). **Потребители** переведены с соседнего `<p>` на проп `hint`: `MailboxFormModal.tsx:825` (нормативная подсказка «Код приложения», [ADR-054](adr/ADR-054-mail-app-password-field.md) — ради неё долг и был заведён), `:860` («Команда»); `AddBackendModal.tsx:251`/`:263`, `AddAiKeyModal.tsx:381`, `AddProxyModal.tsx:465` («Оставьте пустым, чтобы не менять …»). Подсказки, описывающие **группу** полей или **кнопку**, связаны напрямую на владельце (`<fieldset>` `MailboxFormModal.tsx:981`/`:1027` ← `connectionHint` `:947-951`; ссылка Outlook `:914`/`:926`) — проп `hint` там неприменим (описываемый объект не поле), норма соблюдена. **Тесты (`qa`):** 40 кейсов — `frontend/src/lib/__tests__/a11y.test.ts`, `components/ui/__tests__/FieldHintContract.test.tsx`, `components/__tests__/MailboxFormModalHintsA11y.test.tsx`, `components/__tests__/SecretHintsA11y.test.tsx`; полный прогон **832 passed**. |

## Процедура
Закрытие TD — отдельная задача исполнителя со ссылкой на `TD-NNN`. При закрытии — отметить статус и сослаться на коммит/PR.
