# ADR · Реестр архитектурных решений

Architecture Decision Records. Каждое значимое решение — отдельный файл `ADR-NNN-<slug>.md`. Статусы: `accepted` / `superseded` / `deprecated`.

| ID | Решение | Статус | Дата |
|----|---------|--------|------|
| [ADR-001](ADR-001-stack-i-monolit.md) | Стек проекта и монолитная архитектура | accepted | 2026-06-28 |
| [ADR-002](ADR-002-dvuhshagovyy-auth.md) | Двухшаговый вход + JWT HS256 (единый endpoint) | accepted | 2026-06-28 |
| [ADR-003](ADR-003-prometheus-istochnik-metrik.md) | Prometheus — единственный источник метрик | accepted | 2026-06-28 |
| [ADR-004](ADR-004-file-sd-registraciya-targetov.md) | file_sd для динамической регистрации таргетов | accepted | 2026-06-28 |
| [ADR-005](ADR-005-custom-gauge-vs-grafana-embed.md) | Кастомные SVG-спидометры vs Grafana embed | accepted | 2026-06-28 |
| [ADR-006](ADR-006-async-provisioning-bez-brokera.md) | Асинхронный провижининг без брокера сообщений | accepted | 2026-06-28 |
| [ADR-007](ADR-007-shifrovanie-fernet.md) | Шифрование SSH-паролей через Fernet | accepted | 2026-06-28 |
| [ADR-008](ADR-008-admin-iz-env.md) | Учётка администратора только из `.env` (амендменты: супер-админ поверх RBAC — [ADR-021](ADR-021-rbac-users-roles.md); **системная строка-якорь в `users` — [ADR-051](ADR-051-superadmin-db-anchor-personal-state.md)**: вход/права/fallback из `.env` **не меняются**, якорь нужен только как FK-цель личного состояния) | accepted | 2026-06-28 |
| [ADR-009](ADR-009-in-backend-notifier-vs-alertmanager.md) | In-backend Telegram-нотификатор vs Alertmanager | accepted | 2026-06-30 |
| [ADR-010](ADR-010-ai-key-monitor-vnutri-backend.md) | Проверка AI-ключей внутри backend vs внешний воркер + Fernet-шифрование | accepted | 2026-07-01 |
| [ADR-011](ADR-011-poryadok-blokov-server-side-dnd-kit.md) | Порядок блоков — server-side `position` + @dnd-kit, drag по задержке. **Амендирован [ADR-046](ADR-046-ui-infra-fix-pack.md) §2а:** DnD **убран на `/backends`** (порядок — сортировка по `name`); `/servers`/`/ai-keys`/`/proxies` не затронуты | accepted | 2026-07-01 |
| [ADR-012](ADR-012-mail-read-through-proxy.md) | Модуль «Почты» — read-through-прокси без хранения (ключ на backend, HTML-изоляция sandbox-iframe). **Модель «без хранения» отменена** [ADR-044](ADR-044-mail-full-merge-into-crm.md); **действуют инварианты:** `MAIL_API_KEY` только на backend, sandbox-iframe тела письма без `allow-scripts`/`allow-same-origin`, JWT | **partially superseded by [ADR-044](ADR-044-mail-full-merge-into-crm.md)** | 2026-07-03 |
| [ADR-013](ADR-013-mail-newest-first-master-detail-inline-reply.md) | Почты — newest-first backward-пагинация, master-detail layout, inline-reply (расширяет ADR-012). **Пагинация `order`/`before_id`/`has_more` отменена** [ADR-044](ADR-044-mail-full-merge-into-crm.md) (лента из БД CRM, сортировка `internal_date DESC, id DESC`, компаундный opaque-курсор); **действуют:** master-detail, авто-выбор свежего, бесконечная лента, inline-reply | **partially superseded by [ADR-044](ADR-044-mail-full-merge-into-crm.md)** | 2026-07-04 |
| [ADR-014](ADR-014-persist-notifier-state-alert-on-first-elevated.md) | Персистентность состояния нотификатора в БД + alert-on-first-elevated (закрывает TD-019) | accepted | 2026-07-04 |
| [ADR-015](ADR-015-csp-img-src-remote-mail-images.md) | CSP `img-src` — разрешение удалённых (https) изображений писем (расширяет ADR-012, XSS-инвариант не ослаблен) | accepted | 2026-07-06 |
| [ADR-016](ADR-016-notifier-max-over-window-zone.md) | Нотификатор оценивает зону по `max_over_time` за окно опроса (ловит транзиентные всплески между опросами; UI-карточки — по-прежнему мгновенное значение; усиливает ADR-014) | accepted | 2026-07-06 |
| [ADR-017](ADR-017-dashboard-client-aggregation-mail-server-filters.md) | Страница «Дашборд» (клиентская агрегация счётчиков, без backend-агрегатора) + серверные фильтры «Почт» по ящику/команде (external ADR-0037; частично снимает TD-024) | **partially superseded by [ADR-044](ADR-044-mail-full-merge-into-crm.md)** (mail-часть: `group_id`/`GET /api/mail/teams`/проксирование фильтров отменены — фильтры `mail_account_id`+`team_id` работают в БД CRM; **действует** клиентская агрегация «Дашборда») | 2026-07-06 |
| [ADR-018](ADR-018-notifier-windowed-offline-recovery-alert-log.md) | Нотификатор: windowed offline-детект (`min_over_time` для `up`) + recovery-уведомления (`offline→online`) + durable-лог отправленных алертов (`notifier_alert_log`); расширяет ADR-016/ADR-014, пороги не меняются | accepted | 2026-07-07 |
| [ADR-019](ADR-019-proxies-availability-monitor.md) | Страница «Прокси» — реестр HTTP/SOCKS-прокси + отдельный in-backend монитор доступности (по образцу AI-ключей ADR-010, статус в БД) + Fernet-шифрование пароля + отдельные поля ввода + Telegram-алерты | accepted | 2026-07-07 |
| [ADR-020](ADR-020-backends-healthcheck-monitor.md) | Страница «Бэки» — реестр сервисов (Код/Название/Домен, `code` UNIQUE) + отдельный in-backend монитор healthcheck (по образцу прокси ADR-019, статус в БД) + Telegram-алерты. **Амендирован:** ADR-040 (добавлены связи `server_id`/`ai_key_id` + секреты `api_key`/`admin_api_key` — «без секрета/Fernet» снято), ADR-042 (домен → канон `https://<host>/`, health-URL `{domain}health` вместо `https://{домен}/health`), ADR-039 (поиск/группировка/пустое состояние) | accepted | 2026-07-07 |
| [ADR-021](ADR-021-rbac-users-roles.md) | Пользователи + роли + RBAC на все страницы (каталог прав на сервере, `users`/`roles` в БД, bcrypt-хэш паролей, `.env`-админ → несменяемый супер-админ (вход/права — вне БД; **с [ADR-051](ADR-051-superadmin-db-anchor-personal-state.md) в `users` есть его невидимая системная строка-якорь** как FK-цель личного состояния), JWT `role`/`uid`/`superadmin`, enforcement `require(page,action)`/`403 forbidden`, свежая загрузка прав из БД; амендмент ADR-008) | accepted | 2026-07-07 |
| [ADR-022](ADR-022-teams-nav-categories.md) | CRM-команды (`teams`+`user_teams` M2M, лидер+участники, дизамбигуация vs mail-`groups`) + `users.email` (заменён на `users.telegram`, [ADR-025](ADR-025-passwordless-users-login-identifier-open-first-login.md)) + каталог прав `roles`/`teams` (амендмент ADR-021 §1, роли под матрицей `roles:*`, security-инвариант эскалации) + категоризированная навигация (3 дропдауна, «Дашборд» вне меню, permission-aware дефолт). Лидер команды был обязателен — снято [ADR-026](ADR-026-teams-optional-leader-auto-transfer.md) | accepted | 2026-07-08 |
| [ADR-023](ADR-023-ui-nav-dropdown-proxy-ip-single-delete.md) | UI-пакет: вертикальный дропдаун-навигации (пункт на строку) + «квадратная» панель (радиус 6px) + карточка прокси показывает только IP (без порта/логина/пароля) + единственная кнопка «Удалить» на карточке (убран дубль в error-состоянии). Контракты/БД не затронуты. Стиль панели (фон/граница/ширина/выравнивание) переопределён — [ADR-029](ADR-029-ui-login-password-nav-team-form.md) | accepted | 2026-07-08 |
| [ADR-024](ADR-024-monitor-hard-deadline-backend-alert-grace.md) | Устойчивость мониторинга: жёсткий overall-deadline проверки прокси/бэка (`asyncio.wait_for`, `*_CHECK_DEADLINE_SEC=30`, анти-зависание) + явный `httpx.Timeout` по всем фазам; grace-порог 30 мин непрерывной недоступности бэка перед 🔴 (`BACKEND_ALERT_AFTER_SEC=1800`, поля `error_since`/`alert_sent`, миграция 0013). Immediate-модель прокси снята — grace распространён на прокси, [ADR-027](ADR-027-proxies-alert-grace.md) | accepted | 2026-07-08 |
| [ADR-025](ADR-025-passwordless-users-login-identifier-open-first-login.md) | Беспарольные пользователи (`password_hash` nullable, `password` опц. при create) + вход по Логину ИЛИ Телеграму (`users.email`→`users.telegram`, `telegram_taken`) + открытый первый вход (`password_setup_required` + limited-scope setup-token + `POST /api/auth/set-password`, миграция 0011). Амендмент ADR-021/ADR-022/ADR-008. Экран установки пароля переименован «Задайте пароль»→«Придумайте пароль» — [ADR-029](ADR-029-ui-login-password-nav-team-form.md) | accepted | 2026-07-08 |
| [ADR-026](ADR-026-teams-optional-leader-auto-transfer.md) | Команды без лидера: `teams.leader_id` nullable + FK `ON DELETE SET NULL`, `member_ids`/`leader_id` опц., авто-назначение (первый участник → лидер) и авто-передача лидерства по `user_teams.created_at`, упразднён `409 user_is_team_leader`, миграция 0012. Амендмент ADR-022. UX формы (лидер из участников) уточнён — [ADR-029](ADR-029-ui-login-password-nav-team-form.md) | accepted | 2026-07-08 |
| [ADR-027](ADR-027-proxies-alert-grace.md) | Прокси: 30-мин grace-порог алерта недоступности (`PROXY_ALERT_AFTER_SEC=1800`, поля `error_since`/`alert_sent`, миграция 0014, time-aware `evaluate_transition`) — устраняет ложные срабатывания; immediate-модель прокси снята, прокси и бэки унифицированы. Амендмент ADR-024/ADR-019 | accepted | 2026-07-08 |
| [ADR-028](ADR-028-user-status-first-login.md) | Тристатус пользователя: `users.first_login_at timestamptz NULL` (миграция 0015, идемпотентно при первом входе — парольный `login` + `set-password`), производное `UserListItem.status ∈ {"pending","active","inactive"}` («Ожидает входа»/«Активен»/«Неактивен»); «Активен» только после первого входа. Амендмент ADR-021/ADR-025 | accepted | 2026-07-08 |
| [ADR-029](ADR-029-ui-login-password-nav-team-form.md) | UI/UX-пакет: экран «Придумайте пароль» (переим. «Задайте пароль»), убраны H1+подписи страниц Users/Roles/Teams, лидер команды из выбранных участников (первый = лидер), рестайл панели нав-дропдауна (фон `bg-base`, без границы, уже, левое выравнивание) + разделитель «\|» между категориями. Контракты/БД не затронуты. Амендмент ADR-023/ADR-025/ADR-026 | accepted | 2026-07-08 |
| [ADR-030](ADR-030-sms-module-full-merge.md) | Модуль «СМС» — полное слияние SMS-агрегатора в CRM (одна БД `crm`; таблицы `sms_phone_numbers`/`sms_inbound`/`sms_deliveries`/`sms_telegram_links` с PK BIGINT + внешними FK UUID на CRM `teams`/`users`; Twilio-приём по подписи + отдельный SMS-delivery Telegram-бот с fan-out по команде/retry; отказ от Redis, привязка Mini App под JWT; новые поля номера `login`/`app_name`/`note`, системный `label`; видимость по текущей принадлежности номера; RBAC-страница `sms:view/edit/transfer/sync/delete`; `Principal.user_id`) | accepted | 2026-07-09 |
| [ADR-031](ADR-031-sms-operator-mini-app.md) | Операторская Telegram Mini App модуля «СМС» — выделенный публичный SPA-маршрут `/tg/sms` (вне `AppLayout`/RBAC-guard, без redirect на login); онбординг **беспарольный Telegram-SSO** (ревизия: `POST /api/sms/telegram/auth` валидирует initData → резолвит оператора по иммутабельному `telegram_user_id`, иначе bootstrap по `users.telegram = normalize(username)` → авто-upsert линка + выдача CRM-JWT; не сопоставлен → `403 sms_operator_not_provisioned`); просмотр номеров/сообщений через существующие JWT-эндпоинты под `sms:view` (роль оператора обязана его включать); self-hosted Telegram WebApp SDK (CSP `script-src 'self'` не ослабляется); только нативные Telegram-webview; `SMS_TELEGRAM_WEBAPP_URL → …/tg/sms`. Риск подмены telegram-ника до первого линка. Амендмент ADR-030 | accepted | 2026-07-09 |
| [ADR-032](ADR-032-sms-visibility-admin-full-catalog.md) | Видимость SMS «все команды» для admin-уровня по полному каталогу прав: `sees_all_teams ⇔ is_superadmin ИЛИ роль владеет полным каталогом прав` (симметрично для `GET /api/sms/messages` и `GET /api/sms/numbers` под `SmsScopeDep`); отклонены варианты «по имени роли» и «новое право `sms:view_all`». Амендмент ADR-030 §6 | accepted | 2026-07-09 |
| [ADR-033](ADR-033-flat-nav-theme-toggle-numbers-table.md) | UI-пакет Спринта C: плоская навигация (упразднены категории-дропдауны/`NavMenu`/разделитель «\|»; порядок `DefaultRoute` не меняется; деградация — горизонтальный скролл ряда) + светлая/тёмная тема (`data-theme` на `<html>`, дефолт тёмная, `localStorage['crm-theme']`, inline-скрипт без FOUC, полная light-палитра + тема-зависимые тени, Mini App `/tg/sms` наследует тему Telegram). **Тема-часть частично переопределена:** дефолт → СВЕТЛАЯ ([ADR-041](ADR-041-login-theme-session-ux.md)); no-FOUC-скрипт → **отдельный файл, не inline** + светлые токены на голом `:root` + self-heal ([ADR-046](ADR-046-ui-infra-fix-pack.md) §4 — фикс бага «на проде всегда тёмная») + упрощение таблицы «Номера» (убрана колонка «Действия»: перенос → авто-коммит в колонке «Команда», удаление → иконка `Trash2`; карандаш инлайн-правки виден всегда). Контракты/БД не затронуты. Амендмент ADR-022/023/029/030. **§Layout-ветка навигации частично superseded [ADR-061](ADR-061-documents-sidebar-two-panel-nav.md)** (маршрут `/documents` — второй full-bleed после `/mail`; хэдер-ряд ADR-033 сохранён) | accepted | 2026-07-09 |
| [ADR-034](ADR-034-teams-number-login-app.md) | Detail-панель `/teams` показывает `login`/`app_name` номеров команды: `TeamNumberItem += login`/`app_name` (nullable), гейт `teams:view` не меняется, набор строк не меняется. `note`/`label` остаются сужёнными (только под `sms:*`). Осознанный частичный разворот сужения ADR-030 §8 (слабо-чувствительный идентифицирующий контекст под `teams:view`). Новых колонок/миграций нет. Амендмент ADR-030 §8 | accepted | 2026-07-09 |
| [ADR-035](ADR-035-detail-view-secret-reveal.md) | Read-only detail-view (`<Entity>DetailModal`) с карандашом вместо прямой edit-модалки на карточных страницах Серверы/Прокси/ИИ-ключи/Бэки (клик → detail под `<page>:view` → `Pencil` под `<page>:edit` → существующая edit-модалка) + **on-demand reveal секретов** per-resource (`GET /api/servers/{id}/ssh-password`, `GET /api/proxies/{id}/password`, `GET /api/ai-keys/{id}/key` → `{value}`, `Cache-Control: no-store`, `decrypt_secret` in-memory): гейт `<page>:edit`, аудит-лог `secret_revealed` без значения, секрет не преднагружается и живёт только в локальном стейте модалки. `ServerListItem += ssh_user` (не секрет). Editable-scope: сервер — только `name` (репровижининг вне scope); прочие — существующие контракты. Новое право не вводится. Код `404 secret_not_set`. Амендмент ADR-011/007/010/019. **Амендирован [ADR-049](ADR-049-servers-backends-card-first-detail.md) §3 (2026-07-13):** паттерн «клик по карточке → detail-модалка → карандаш» **отменён ТОЛЬКО для `/backends`** (`BackendDetailModal` упразднена, карандаш — в блоке действий карточки); для `/servers`/`/proxies`/`/ai-keys` — **в силе**. Гарантии reveal секретов **не ослаблены** | accepted | 2026-07-09 |
| [ADR-036](ADR-036-sms-team-filter-admin-only.md) | Фильтр «Все команды» на `/sms` (вкладка «Сообщения») рендерится **только** при admin-уровне видимости SMS; для прочих ролей — не рендерится (был пустой/неактивный). Механизм: `GET /api/auth/me += sees_all_sms_teams: boolean` = backend-предикат `is_superadmin OR permissions_subset(full_catalog, permissions)` (тот же, что `get_sms_scope`) — backend как единственный источник истины (вариант A; фронт не дублирует `permissions_subset` и не тянет каталог под `roles:view`). «Все номера» остаётся всем. UI-следствие ADR-032; серверный SMS-scope — граница безопасности. Амендмент ADR-032. **⚠️ Гейт «фильтр только при admin-уровне» ОТМЕНЁН [ADR-055](ADR-055-per-channel-teams-mail-sms.md) §6** (2026-07-14): фильтр «Команда» рендерится **любому**, у кого **≥ 2 команды канала** (опции — из `GET /api/auth/me`, без `teams:view`). **В силе:** backend — единственный источник признаков видимости (фронт не дублирует `permissions_subset`), `sees_all_sms_teams` в `/me`, серверный SMS-scope как граница безопасности | **accepted (гейт «только admin» отменён [ADR-055](ADR-055-per-channel-teams-mail-sms.md))** | 2026-07-09 |
| [ADR-037](ADR-037-mini-app-tabs.md) | Операторская Mini App `/tg/sms`: две вкладки-тумблера **«Сообщения»** (дефолт) / **«Номера»** (было — только номера); лента `GET /api/sms/messages` под SSO-JWT/scope оператора (фильтры «Все номера/команды» в Mini App не показываются). Убран информационный статус-блок привязки («Telegram привязан — …» + бейдж «Привязан»); линк технически сохраняется, SSO по-прежнему обязателен. Чисто UI, контракт не меняется. Амендмент ADR-031. **⚠️ ЧАСТИЧНО ОТМЕНЁН [ADR-055](ADR-055-per-channel-teams-mail-sms.md) §6** (2026-07-14): фильтр **«Команда»** в Mini App **появляется** (обе Mini App, порог «≥ 2 команды канала»). Фильтр **«Все номера»** в Mini App — по-прежнему **не показывается** (эта часть в силе) | **accepted (фильтр «Команда» возвращён [ADR-055](ADR-055-per-channel-teams-mail-sms.md))** | 2026-07-09 |
| [ADR-038](ADR-038-mail-headless-integration.md) | **[ОТМЕНЁН — не реализовывать]** Headless-интеграция CRM ↔ mail-агрегатор: read-through-прокси ADR-012 расширен до **read+write без хранения** (mailboxes CRUD/test/sync, tags CRUD/rules/apply — прокси в external write API агрегатора `ADR-0039`/`ADR-0040`); связь ящик↔команда через **`teams.mail_group_id INTEGER NULL UNIQUE`** (миграция 0018, 1:1, источник истины владения остаётся в агрегаторе `mail_accounts.group_id`, локального кэша каталога ящиков НЕТ, backfill ручной); **`MailScope(sees_all_teams, group_ids)`** + `get_mail_scope` по образцу `SmsScope`/ADR-032 (вне scope: чтение→пусто/анти-энумерация, мутация→403; `group_ids` из `user_teams`→`mail_group_id`) + `GET /api/auth/me += sees_all_mail_teams`; RBAC `CATALOG["mail"]` `("view",)` → **`("view","create","edit","delete","sync","tags")`** (`reply` остаётся под `view`); IMAP/SMTP-креды **транзитом** в агрегатор (шифрование AES-256-GCM ТАМ, Fernet CRM не задействуется), пароль не логируется/не в ответах, эндпоинты записи `Cache-Control: no-store`; ретраи write — только `Connect*` (анти-двойная-запись). Ограничения: 1:1 команда↔группа ([TD-033](../100-known-tech-debt.md)), вложения недоступны ([TD-034](../100-known-tech-debt.md)). Амендмент ADR-012/013/017. **Пережили супессию** (перенесены в актуальные docs): каталог прав `mail:*`, `sees_all_mail_teams`, транзит кредов + `no-store`, connect-only-ретраи write | **superseded by [ADR-044](ADR-044-mail-full-merge-into-crm.md)** (модель «без хранения» + групп-индирекция отменены — почта хранится в БД CRM, ящик привязан к команде через `mail_accounts.team_id`) | 2026-07-09 |
| [ADR-039](ADR-039-ui-server-inline-edit-backends-search-empty-sms-label.md) | **[ЧАСТИЧНО ОТМЕНЁН [ADR-046](ADR-046-ui-infra-fix-pack.md) §2: группировка по `name` и пустое состояние «только `AddBackendCard` без текста» УПРАЗДНЕНЫ; действуют инлайн-редактирование сервера, поиск по бэкам, лейбл `sms → «СМС»`]** UI-пакет: **инлайн-редактирование сервера** прямо в `ServerDetailModal` (карандаш переключает поле «Название» в редактируемый режим, Сохранить/Отмена, `PATCH name`; отдельная `AddServerModal mode='edit'` для сервера упразднена; прочие сущности — карандаш→модалка) + страница «Бэки»: **клиентский поиск** по `code`/`name`/`domain` (плейсхолдер «Поиск по бэкам…», по образцу `/sms`), **пустое состояние без текста-подсказки** (только `AddBackendCard`) и **группировка карточек по одинаковому `name`** (`code` UNIQUE, `name` НЕ уникален → дубли `name` кластеризуются: контейнер с заголовком «`name` · N», singletons — обычные карточки; поверх `position`-порядка/DnD) + **лейбл `sms → «СМС»`** в матрице прав `/roles` (`PAGE_LABEL`). Контракты/БД не затронуты. Амендмент ADR-035/ADR-020/ADR-030 | accepted | 2026-07-09 |
| [ADR-040](ADR-040-backend-relations-secrets-reverse-lookup.md) | Бэк расширен связями и секретами: `backends += server_id`/`ai_key_id` (FK `servers`/`ai_keys`, nullable, `ON DELETE SET NULL`) + `api_key_encrypted`/`admin_api_key_encrypted` (**секреты**, Fernet at-rest; в API — только `has_api_key`/`has_admin_api_key`; on-demand reveal `GET /api/backends/{id}/api-key` и `/admin-api-key` под `backends:edit`, `no-store`, аудит, `404 secret_not_set` — по образцу ADR-035), миграция `0019`. плюс **необязательные НЕсекретные** `git`/`note` (`text NULL`, в обычных ответах). `BackendListItem += server_id/server_name/ai_key_id/ai_key_name/has_api_key/has_admin_api_key/git/note`; `BackendCreate/UpdateRequest += server_id/ai_key_id/api_key/admin_api_key/git/note` (несуществующий FK → `422`). Reverse-lookup: `ServerListItem`/`AiKeyListItem += backend_count` + ленивые `GET /api/servers/{id}/backends` и `GET /api/ai-keys/{id}/backends` → `BackendRef{code,name,domain}` под `<page>:view` (паттерн счётчиков команд). TD-035 (UI-очистка секретов). Амендмент ADR-020/ADR-035/ADR-007. **Амендирован [ADR-049](ADR-049-servers-backends-card-first-detail.md) §2 (2026-07-13):** секция «Бэки» **сервера** переехала из `ServerDetailModal` **на `ServerCard`** (счётчик `backend_count`, ленивый список по раскрытию — эндпоинт/схема/порядок **не менялись**); у **ИИ-ключа** секция осталась в detail-модалке | accepted | 2026-07-09 |
| [ADR-041](ADR-041-login-theme-session-ux.md) | Login/theme/session UX-пакет: **(10)** дефолт темы **СВЕТЛАЯ** (разворот дефолта ADR-033: нет сохранённого выбора → `light`, НЕ `prefers-color-scheme`; страница логина — светлая; сохранённый выбор приоритетен) + **(11)** плейсхолдер поля входа **«Логин или @username»** и приём Telegram-идентификатора **и с `@`, и без** (сервис нормализует ведущий `@` перед сопоставлением с `users.telegram`) + **(12/16)** **персистентная + мульти-вкладочная сессия**: JWT в `localStorage` (переживает закрытие браузера И шарится между вкладками того же origin; синхронная регидрация auth-стора до guard → новая вкладка/ПКМ-«открыть в новой вкладке» не редиректит на `/login`), автовыход по TTL JWT (`jwt_expires_min=1440`, 24 ч); фиксы 12 и 16 — одна правка auth-персиста; трейдофф localStorage (XSS-поверхность) принят для админ-панели. Контракты БД не затронуты. Амендмент ADR-033/025/002/021 | accepted | 2026-07-09 |
| [ADR-043](ADR-043-lazy-mail-group-provisioning.md) | **[ОТМЕНЁН — не реализовывать]** Ленивый провижининг почтовой группы «команда = почтовая группа»: селектор «Почтовая группа» убран из **создания** команды (`mail_group_id` удалён из `TeamCreateRequest`, сохранён в `PATCH` для ручной привязки к существующей группе); группа в агрегаторе создаётся **лениво** — по первому добавлению почты в команду с `mail_group_id IS NULL` (`ensure_team_mail_group`: `POST /api/external/teams`→**CAS-запись** `mail_group_id`→создание ящика; порядок «CAS-привязка до ящика» делает mailbox-fail самоизлечимым; **инвариант «одна команда — ровно одна группа»** через CAS `UPDATE ... WHERE mail_group_id IS NULL` — гонка не создаёт второй привязки, проигравшая ветка берёт группу победителя и реклеймит свою пустую сироту `DELETE`; row-lock через сетевой вызов **отклонён** (исчерпание пула async-соединений; CAS держит соединение суб-мс); connect-only-ретрай против двойного создания); write-эндпоинты ящика (`POST /api/mail/mailboxes`, `PATCH .../{id}` перенос) ссылаются на команду по **`team_id` (UUID)** вместо `group_id` (чтение `MailMailbox.group_id` не меняется); **`MailScope += team_ids`**: **создание** ящика — по членству (`team_id ∈ team_ids`), **перенос между командами — ТОЛЬКО admin-уровень** (`sees_all_teams`, не членство — требование владельца; без нового `mail:transfer`), просмотр всех почт — admin-уровень; actionable-пустое-состояние команды без почты; идемпотентность по имени группы отвергнута (владелец: «связывать по имени нельзя, только по id»; `groups.name` не UNIQUE). Частично закрывает [TD-036](../100-known-tech-debt.md); группа-без-команды — [TD-037](../100-known-tech-debt.md). Парный mail-агрегатор `ADR-0042`. Амендмент ADR-038 §2/§3, ADR-022/026. **Пережили супессию:** `MailScope.team_ids`, `team_id` в write-контракте ящика, «перенос — только admin». Колонка `teams.mail_group_id` — мёртвый легаси-остаток ([TD-051](../100-known-tech-debt.md)) | **superseded by [ADR-044](ADR-044-mail-full-merge-into-crm.md)** (групп в агрегаторе нет: `ensure_team_mail_group`/CAS/ленивый провижининг упразднены) | 2026-07-10 |
| [ADR-042](ADR-042-backend-domain-canonical-https.md) | Канонизация домена бэка к форме **`https://<host>/`**: любой ввод (`https://host/`, `https://host`, `host`) → единый канон `https://<host>/` (снять схему/путь, извлечь host lower-case, собрать `https://`+host+`/`). CHECK `ck_backends_domain` → `'^https://[^\s/]+/$'`, миграция `0020_backends_domain_canon` (backfill голых доменов; в проде 0 бэков). **Health-URL строится дописыванием `health`** (`domain + "health"` → `https://host/health`), а НЕ склейкой `https://{domain}/health` (иначе битый `https://https://…//health`). Амендмент ADR-020 | accepted | 2026-07-09 |
| [ADR-044](ADR-044-mail-full-merge-into-crm.md) | Полный перенос модуля «Почты» в CRM: агрегатор — чистый mail-connector (IMAP/SMTP/**push нового письма в CRM**), письма/теги/Telegram-уведомления **хранятся в БД CRM** (разворот «без хранения» ADR-012/038); **ящик закреплён за командой напрямую** (`mail_accounts.team_id`, групп-индирекция и `teams.mail_group_id` упразднены, **TD-033 закрыт**); новые таблицы `mail_accounts`/`mail_messages`/`mail_tags`/`mail_tag_rules`/`mail_message_tags`/`mail_telegram_links`/`mail_telegram_notifications`/`mail_user_settings` (миграция `0021`) + `mail_sent_messages` (`0022`); push-приёмник `POST /api/mail/ingest` (HMAC `MAIL_PUSH_SECRET` + timestamp-окно, идемпотентность `ON CONFLICT`) + status-канал `POST /api/mail/mailbox-status`; фоновый `MailDispatcherService` (asyncio, без Redis: проходы новые/recovery/mailbox-down); Telegram — основной бот webhook/SSO/callback + 4 push-бота по `_TEAM_ID` + Mini App `/tg/mail` без экрана логина; лента по `internal_date DESC, id DESC` + компаундный keyset-курсор `(internal_date, id)`; `MailScope(sees_all_teams, team_ids)` (поле `group_ids` удалено); reply через агрегатор `POST /api/external/mailboxes/{id}/send`. **Отменяет** ADR-038/ADR-043. Парный mail-агрегатор `ADR-0043`/`ADR-0044`. **Амендирован [ADR-047](ADR-047-mail-fix-pack.md) §1 (2026-07-11):** `mail_tags.is_builtin` **упразднён** (миграция `0023`) — колонки/поля `is_builtin` в `MailTagFull` НЕТ, правило «встроенный тег нельзя удалить (`409`)» **отменено** (удалить можно ЛЮБОЙ тег), **`seed_builtin_tags` в lifespan УБРАН** (воскрешал удалённые теги при рестарте; первичный сев перенесён в data-миграцию). Плюс `mail_accounts += number/app_name` (миграция `0024`). **Follow-up §12 (docs-sync) — ВЫПОЛНЕН** [ADR-047](ADR-047-mail-fix-pack.md) §0; остаток «`teams.mail_group_id` не дропнут» — [TD-051](../100-known-tech-debt.md). **§3 (status-канал) уточнён агрегаторским `ADR-0046`** (2026-07-13): «на каждое изменение статуса синка» = исчерпывающий перечень точек hook H1–H7 / не-hook N1–N6 на стороне агрегатора; `last_synced_at` = время последней **УСПЕШНОЙ** синхронизации (у сбоящего ящика стареет — норма); OAuth needs-consent отражается маркером в `last_sync_error`. **Контракт CRM (эндпоинт/тело/коды/`down_alert_sent_at`) не меняется**. **Амендирован [ADR-055](ADR-055-per-channel-teams-mail-sms.md) (2026-07-14):** §7 — `MailScope` += `includes_unassigned`, `team_ids` = `user_teams ∪ user_channel_teams['mail']` (правила §4 «создание с `team_id=null` и перенос — только admin» **НЕ развёрнуты**). **Амендирован [ADR-056](ADR-056-mail-mini-app-mailbox-context.md) (2026-07-14):** §2 — `MailAccountRef += number`/`app_name`/`team`. **§8 (контракт reply/send) амендирован [ADR-057](ADR-057-mail-send-contract-fix.md):** ответ агрегатора — `{smtp_message_id}` без `sent_id`; публичный `MailReplyResponse.sent_id` — `uuid` из `mail_sent_messages` CRM; внешний `404` = «ящик не найден» → `mail_mailbox_not_found`; **на проде reply не работает до парного релиза** ([TD-062](../100-known-tech-debt.md)) | accepted (амендирован ADR-047/ADR-055/ADR-056/ADR-057; §3 уточнён агрегаторским ADR-0046) | 2026-07-10 |
| [ADR-045](ADR-045-mail-outlook-oauth-headless-reonboarding.md) | Восстановление добавления/переподключения **Outlook-ящиков по OAuth** из CRM в headless-модели + инструкция **«Как добавить почту?»**: authorize-URL генерирует **агрегатор** по запросу CRM (`POST /api/external/mailboxes/oauth/authorize`, реюз `OutlookOAuthService`+PKCE/Redis), Microsoft редиректит на **агрегатор** (`GET /api/external/mailboxes/oauth/callback`, обмен `code`→токены + AES-GCM только там, owner=`crm-service`, без `group_id`); CRM инициирует (`POST /api/mail/mailboxes/oauth/authorize {team_id}`, гейт `mail:create` + правила команды ADR-044 §4; ссылка открывается в OctoBrowser, **не** auto-redirect) и привязывает ящик к команде через server-to-server HMAC-уведомление `POST /api/mail/oauth/ingest {crm_state, mail_account_id,…}` (реюз `MAIL_PUSH_SECRET`; `crm_state` — stateless HMAC-подписанный `{team_id, initiator, exp}`, без новой таблицы); нормативный UI-текст инструкции/Outlook-секции — `08-design-system.md`. **Координация демонтажа:** амендмент mail-агрегатор `ADR-0044` §7/Phase A3/Phase G (сохранить `OutlookOAuthService` headless + `OUTLOOK_REDIRECT_URI`), **закрывает агрегаторский TD-052**. Парный `ADR-0045`. Амендмент ADR-044 §4. **⚠️ §5 «Оговорка (реальность формы, не-admin)» ОТМЕНЕНА [ADR-055](ADR-055-per-channel-teams-mail-sms.md) §6.3** (2026-07-14): опция **«Без команды» в селекторе «Команда» — только admin-уровню**, команды не-админу — из `GET /api/auth/me` (не `GET /api/teams`); прежнее «показывать всем, гейтинг = UX-долг TD-050» развёрнуто (на проде это блокировало создание ящика ролью без `teams:view`). Вариант B («OAuth не строже ручного создания») — в силе | **accepted (§5-оговорка отменена [ADR-055](ADR-055-per-channel-teams-mail-sms.md))** | 2026-07-10 |
| [ADR-046](ADR-046-ui-infra-fix-pack.md) | **UI/infra-пакет** (фиксы 1–4 батча владельца): **(1)** перечень бэков в Telegram-алертах об ОШИБКАХ сервера (`warning`/`critical`/`offline`) и ИИ-ключа (`key error`) — блок «Бэки:» из строк `Бэк "<name>" [<code>] <domain>` (реюз `_backend_block`), источник — `backends.server_id`/`backends.ai_key_id` (ADR-040), порядок `position ASC, code ASC`, лимит **10** + `… и ещё N`; **recovery НЕ расширяются**; CHECK `ck_notifier_alert_log_kind` не меняется (новых `kind` нет, меняется только `message`) — миграций нет. **(2)** `/backends`: группировка-кластеры «Имя · N» и **DnD убраны** (разворот ADR-039) → **одна плоская сетка**, сортировка `name` (ru, ci) + tie-break `code`; `backends.position` и `PATCH /api/backends/order` **остаются в БД/API**, UI их не использует ([TD-054](../100-known-tech-debt.md)); DnD `/servers`/`/ai-keys` не тронут. Кнопка **«+ Добавить» → правая зона заголовка** на Бэки/Серверы/ИИ-ключи/Прокси (образец — «Обновить» на `/ai-keys`), пунктирные `Add*Card` **удалены** (в т.ч. из пустого состояния — норма ADR-039 «empty = только `AddBackendCard` без текста» отменена). Detail-модалки: видны **только идентификаторы** (Бэк — Код/Название/Домен; Сервер — Название/IP; ИИ-ключ — Название/Провайдер; Прокси — Название/Хост/Порт), остальное — в **свёрнутом блоке «Информация»** (паттерн `BackendsDetailSection`). **(4)** Пустое поле в detail **не рендерится** (прочерк «—» упразднён); секрет без значения (`has_*=false`) — строка не рендерится. **(3)** **Фикс тёмной темы на проде** (root cause: CSP `script-src 'self'` блокирует inline no-FOUC-скрипт → `data-theme` не проставляется, а тёмные токены сидят на голом `:root`): no-FOUC-скрипт **вынесен в отдельный статический файл** (`script-src` остаётся `'self'`, `'unsafe-inline'` НЕ вводится), **светлая тема — дефолт на голом `:root`**, тёмные — под `[data-theme='dark']`, **self-heal** в `useTheme()`; devops — `index.html` с `Cache-Control: no-cache`, хешированные ассеты `immutable`, **⚠️ `add_header` во вложенном `location` отменяет унаследованные → CSP/security-заголовки продублировать побайтово**. Амендмент ADR-039/ADR-035/ADR-040/ADR-041/ADR-033/ADR-011. **⚠️ §2в ЧАСТИЧНО РАЗВЁРНУТ [ADR-049](ADR-049-servers-backends-card-first-detail.md) (2026-07-13):** для **Сервера** — «Пользователь»/«Пароль» подняты в **главный блок**, блок «Информация» в `ServerDetailModal` **упразднён**, секция «Бэки» переехала **на карточку**; для **Бэка** — `BackendDetailModal` **упразднена**, блок «Информация» переехал **на карточку**. Для **ИИ-ключа** и **Прокси** §2в действует **без изменений**; §1, §2а, §2б, §3, §4 — **в силе целиком** | accepted (§2в частично развёрнут ADR-049) | 2026-07-11 |
| [ADR-047](ADR-047-mail-fix-pack.md) | **Mail-пакет** (фиксы 5–10 батча владельца) + **закрытие обязательного follow-up [ADR-044](ADR-044-mail-full-merge-into-crm.md) §12**: mail-docs (`modules/mail/README.md`, `04-api.md §Mail`, `03-data-model.md`) синхронизированы с фактическим кодом (описывали отменённую read-through-прокси-модель ADR-012/038/043 — `group_id`/`MailTeam`/`GET /api/mail/teams`/«без БД»); `teams.mail_group_id` объявлен **мёртвым легаси-остатком** ([TD-051](../100-known-tech-debt.md)), мёртвая фабрика `mail_group_not_found` удаляется. **(5)** **`mail_tags.is_builtin` УПРАЗДНЯЕТСЯ** (миграция **`0023`**): DROP COLUMN, ветка `409` при удалении тега снята — **удалить можно ЛЮБОЙ тег**; **seed builtin-тегов в lifespan УБИРАЕТСЯ** (иначе удалённый тег воскресает при рестарте — корень фикса), первичный сев 10 канонических тегов+правил перенесён в **data-миграцию** (`ON CONFLICT (name) DO NOTHING`, данные вшиты в миграцию); слово «встроенный» из UI убрано. **(6)** Нормативные лейблы правил: `subject_contains`→«Тема письма», `body_contains`→«Текст письма», `sender_contains`→«Отправитель»; **`sender_exact` убран из выпадающего списка UI**, но остаётся в БД/контракте и корректно отображается («Отправитель равен») — [TD-055](../100-known-tech-debt.md). **(7)** `mail_accounts += number/app_name` (text NULL, миграция **`0024`** + backfill-разбор `display_name`: ведущая числовая часть с перечислениями → «Номер», остаток → «Приложение»); форма ящика — два поля вместо «Отображаемого имени»; `display_name` — **производное** (склейка `"<number> <app_name>"`, считает сервер, клиент его не шлёт), сохраняется как единственная форма имени во внешнем контракте агрегатора ([TD-052](../100-known-tech-debt.md)); **исходящий payload в агрегатор строится БЕЛЫМ СПИСКОМ** (закрыта ловушка `model_dump(exclude={"team_id"})` — `number`/`app_name` наружу не уходят никогда). **(8)** Колонка «Команда» расширяется (`truncate`/`overflow-hidden` на значении команды **запрещены** — переполнение решается размером). **(9)** Новый рендер строки ящика (референс `screen/1.jpg`): строка 1 — статус + «Номер» + крупное жирное значение + «Приложение» + значение **`ui/Pill tone="accent"`** (новый примитив не вводится); строка 2 — email. **(10)** Тело письма следует теме CRM (хардкод `#161A22`/`#E6E9EF` снят; литералы по теме, т.к. iframe не наследует CSS родителя), **билдер srcDoc — единый источник** для `MailDetail` и Mail Mini App; изоляция НЕ ослаблена (`sandbox=""` без `allow-scripts`/`allow-same-origin`, CSP не тронута). Амендмент ADR-044 §2/§5/§12, ADR-012/ADR-015 (инварианты сохранены). **Амендирован [ADR-048](ADR-048-teams-mailbox-count-mail-row.md) §2 (2026-07-13):** утверждение §3.2 «`TeamMailboxItem` не меняется» **развёрнуто** — схема расширена `number`/`app_name`, `display_name` в строке detail-панели `/teams` не рендерится | accepted (амендирован ADR-048) | 2026-07-11 |
| [ADR-048](ADR-048-teams-mailbox-count-mail-row.md) | **`/teams`: счётчик почт + строка почты в detail-панели.** **(1)** `TeamListItem += mailbox_count: integer` (`COUNT(mail_accounts WHERE team_id = teams.id)`, может быть `0`) — **строго по паттерну `number_count`** ([ADR-030](ADR-030-sms-module-full-merge.md)): батч `MailAccountRepository.count_by_teams` для списка + одиночный `count_by_team` для тел `201`/`200`; чип **«N почт»** на карточке команды. **(2)** Список почт команды — **переиспользуется существующий** `GET /api/teams/{id}/mailboxes` ([ADR-044](ADR-044-mail-full-merge-into-crm.md) §4, гейт `teams:view`), **новый эндпоинт НЕ вводится**; схема `TeamMailboxItem += number`/`app_name` (`string \| null`). `display_name` в схеме остаётся, но в строке **не рендерится** (производное от `number`+`app_name`, [TD-052](../100-known-tech-debt.md)). **(3)** Нормативный рендер строки: статус-`Badge` → `email` → «Номер» + значение **теми же классами, что на `/mail`** (`text-lg font-bold`) → «Приложение» пилюлей **`ui/Pill tone="accent"`** (новый примитив НЕ вводится); пустое значение → пара «лейбл+значение» не рендерится; обрезка значений запрещена (перенос `flex-wrap`). **(4)** Видимость — существующий гейт **`teams:view`**, без `MailScope` (симметрично [ADR-034](ADR-034-teams-number-login-app.md): счётчик — агрегат уже раскрытого списка; `number`/`app_name` — слабо-чувствительный идентифицирующий контекст; креды/статус синка по-прежнему только под `mail:*`). **Миграций нет** (`mail_accounts.number`/`app_name` — `0024`; `ix_mail_accounts_team_id` — `0021`). Амендмент ADR-034/ADR-044 §4/**ADR-047 §3.2** (разворачивается «`TeamMailboxItem` не меняется»). **ADR-047 §5 НЕ амендируется** — рендер строки ящика на `/mail` переиспользуется как референс визуального языка и остаётся в силе | accepted | 2026-07-13 |
| [ADR-049](ADR-049-servers-backends-card-first-detail.md) | **UI-пакет `/servers` + `/backends` (card-first).** **(1) `/servers` detail-модалка:** **Пользователь** (`ssh_user`) и **Пароль** (reveal) поднимаются в **главный блок** к Названию/IP; **свёрнутый блок «Информация» в `ServerDetailModal` УПРАЗДНЯЕТСЯ**; обе строки рендерятся всегда (`ssh_user`/`ssh_password_encrypted` — `NOT NULL`, флаг `has_password` у сервера **не вводится**). **(2) `/servers` карточка:** секция **«Бэков: N»** (по уже существующему `ServerListItem.backend_count`) переезжает из detail-модалки **на `ServerCard`**; список — **ленивый по раскрытию** через уже существующий `GET /api/servers/{id}/backends` → `BackendRef{code,name,domain}` ([ADR-040](ADR-040-backend-relations-secrets-reverse-lookup.md) §4, порядок не меняется); **преднагрузка списков при рендере сетки ЗАПРЕЩЕНА** (N+1); `backend_count=0` → строка рендерится, но не раскрывается; триггер обязан **гасить всплытие** (иначе откроется detail-модалка) и **не быть drag-ручкой** ([ADR-011](ADR-011-poryadok-blokov-server-side-dnd-kit.md) DnD на `/servers` в силе). **(3) `/backends` карточка:** свёрнутый блок **«Информация»** (Сервер / ИИ-ключ / API KEY (reveal) / ADMIN API KEY (reveal) / Git / Примечания) переезжает **на `BackendCard`**; все значения **уже** в `BackendListItem` → раскрытие **не делает ни одного запроса**; **`BackendDetailModal` УПРАЗДНЯЕТСЯ** (файл удаляется), карандаш `Pencil` (гейт `backends:edit`) переезжает **в блок действий карточки** → `AddBackendModal mode="edit"`. **(4) Секреты:** гарантии reveal **НЕ ослабляются** — секрет **не преднагружается** (только флаги `has_*`), значение — **только по клику на глаз**, гейт `<page>:edit`, `Cache-Control: no-store`, аудит `secret_revealed`; **массовая преднагрузка секретов при раскрытии карточек ЗАПРЕЩЕНА**. **Миграций нет, контракты API не меняются** (изменения — чисто frontend). **Разворачивает [ADR-046](ADR-046-ui-infra-fix-pack.md) §2в** (строки «Сервер» и «Бэк» таблицы «Состав видимой зоны»); амендмент [ADR-035](ADR-035-detail-view-secret-reveal.md) (detail-модалка отменена **только для `/backends`**) и [ADR-040](ADR-040-backend-relations-secrets-reverse-lookup.md) §5 (место секции «Бэки» сервера). **`/ai-keys` и `/proxies` НЕ затронуты** — их detail-модалки и норма ADR-046 §2в в силе. Долг [TD-057](../100-known-tech-debt.md) (нет общего примитива `ui/Collapsible`) | accepted | 2026-07-13 |
| [ADR-050](ADR-050-mail-search-team-filter-personal-read-state.md) | **Mail-пакет: поиск/фильтр на вкладке «Почты» + ЛИЧНАЯ прочитанность писем.** **(1) Вкладка «Почты»:** **клиентский** поиск (`ui/Input` + `Search`, плейсхолдер **«Поиск по почтам…»**) по трём полям — **`number`**, **`app_name`**, **`email`** (подстрока, ci); **клиентский** фильтр по команде (`ui/Select`, «Все команды» + команды из `GET /api/teams` + «Без команды»), рендерится **только при `me.sees_all_mail_teams === true`** — та же норма, что на вкладке «Сообщения» ([ADR-036](ADR-036-sms-team-filter-admin-only.md)); пустой результат — «Ничего не найдено». Оба фильтра **клиентские** (каталог ящиков грузится целиком — пагинации в контракте нет) ⇒ **backend по фиксу 1 НЕ меняется**; порядок: серверный `is_active` → поиск → команда. **(2) Прочитанность — ЛИЧНАЯ (per-user):** новая таблица **`mail_message_reads`** (`user_id uuid` FK `users` CASCADE, `message_id bigint` FK `mail_messages` CASCADE, `read_at timestamptz`, **PK `(user_id, message_id)`** + **обязательный** `ix_mail_message_reads_message_id` под CASCADE-удаление), миграция **`0025_mail_message_reads`** (revision id 23 симв. ≤32, [ADR-047](ADR-047-mail-fix-pack.md) §3.5). Контракт: **`MailMessage += is_unread: boolean`** (персональное производное), **`GET /api/mail/messages += unread: boolean?`** (серверный фильтр), **`POST`/`DELETE /api/mail/messages/{id}/read` → `204`** (идемпотентны), гейт **`mail:view`** (прецедент: `reply` — под `view`; нового права нет), вне `MailScope` → **`404 mail_message_not_found`** (анти-энумерация). **Пометка ПРИ ОТКРЫТИИ** — в вебе (смена выбранного письма, **включая авто-выбор самого свежего**) **и в Mini App `/tg/mail`** (**ДА** — Mini App несёт **обычный CRM-JWT с `uid`**, `MailTelegramService.auth`; спец-эндпоинта не нужно). **Возврат в «непрочитано» — ЕСТЬ** (`DELETE`, кнопка «Отметить непрочитанным» в шапке детали; в Mini App её нет). **§2.5 (супер-админ из `.env` — `403`/`is_unread=false`/пустая страница/скрытые контролы) ОТМЕНЁН — [ADR-051](ADR-051-superadmin-db-anchor-personal-state.md):** прочитанность работает и под консольным супер-админом (у него системная строка-якорь в `users`), контролы рендерятся. **Производительность (нормативно):** `is_unread` — **батч-лукап по PK** на страницу (**не** JOIN в keyset-запрос, **не** N+1); фильтр `unread=true` — **анти-джойн `NOT EXISTS` ВНУТРИ** keyset-запроса (клиентская фильтрация **запрещена** — сломала бы курсор, как тумблер «С тегами»); **badge-счётчик непрочитанных НЕ вводится**. UI: непрочитанное — **жирная тема + точка `--accent`** + sr-only «Непрочитано» (**новый примитив не вводится**; `Badge dot` не переиспользуется — его палитра статусная); фильтр **«Непрочитанные»** — **серверный** (сбрасывает пагинацию); открытие письма при активном фильтре **НЕ удаляет** его из текущего списка. **Разворачивает** non-goal [modules/mail](../modules/mail/README.md) «пометка прочитано/непрочитано» (папки/архив — остаются вне scope); амендмент [ADR-044](ADR-044-mail-full-merge-into-crm.md) §2. Долг [TD-058](../100-known-tech-debt.md). **⚠️ §1.1 амендирован [ADR-052](ADR-052-mail-mailbox-combobox.md) (2026-07-13) — ТОЛЬКО КОНТРОЛ:** «`ui/Input` + `Search`, новый примитив не вводится» **отменено** → поле поиска на вкладке «Почты» = новый примитив **`ui/Combobox`** с выпадающим списком почт (иконка `ChevronDown`), выбор из списка сужает таблицу до одной строки. **Правило поиска (три поля `number`/`app_name`/`email`, подстрока, ci, без `display_name`), клиентская фильтрация и все нормативные строки — В СИЛЕ**; §2 (прочитанность) не затронут. **⚠️ §1.2 (гейт team-фильтра «только при `sees_all_mail_teams`») ОТМЕНЁН [ADR-055](ADR-055-per-channel-teams-mail-sms.md) §6** (2026-07-14): фильтр «Команда» рендерится при **≥ 2 командах канала** (опции не-админу — из `/api/auth/me`); **[TD-058](../100-known-tech-debt.md) закрыт**. Клиентская природа фильтра и опция «Без команды» — в силе | accepted (§2.5 отменён ADR-051; контрол §1.1 заменён ADR-052; **гейт §1.2 отменён ADR-055**) | 2026-07-13 |
| [ADR-051](ADR-051-superadmin-db-anchor-personal-state.md) | **Системный якорь супер-админа в `users` — личное состояние работает и под консольным `admin`.** **Требование владельца:** личная прочитанность писем ([ADR-050](ADR-050-mail-search-team-filter-personal-read-state.md)) обязана работать под `.env`-супер-админом (он ходит на прод под ним); прочитанность **остаётся личной**. **Решение:** супер-админу заводится **системная строка-якорь** в `users` — константный **`SUPERADMIN_USER_ID = 00000000-0000-0000-0000-000000000001`**, зарезервированный `username` **`superadmin@system`** (символ `@` отвергается `validate_identity_name` ⇒ имя невозможно создать через API), **`is_system = true`**, **locked** bcrypt-хэш случайного секрета (`password_hash IS NULL` ЗАПРЕЩЁН — иначе «открытый первый вход» [ADR-025](ADR-025-passwordless-users-login-identifier-open-first-login.md) отдал бы setup-token и дал эскалацию), роль-заглушка под FK (**правами НЕ наделяет**). **`Principal.user_id` становится НЕ-опциональным** (`uuid.UUID`): супер-админ → константа **без запроса в БД** (fallback-инвариант [ADR-008](ADR-008-admin-iz-env.md) сохранён), JWT **не меняется** (`uid` супер-админу не добавляется). **Миграция `0026_users_is_system`** (20 симв. ≤32) — **только схема** (`users.is_system` + частичный уникальный `uq_users_system_singleton`, **объявленный в `User.__table_args__`** — миграция зеркалит, иначе схема тестов `create_all` разошлась бы с продом; `downgrade()` сначала `DELETE FROM users WHERE is_system`, иначе останется учётка-призрак с ролью `admin`). **Строку создаёт идемпотентный bootstrap** `ensure_superadmin_anchor` (`INSERT … ON CONFLICT DO NOTHING`): резолв роли **самодостаточен** (`admin` → самая ранняя → **создать `admin`, если ролей нет**), вызов — `lifespan` на проде (миграции идут раньше, в entrypoint) и **фикстура сразу после `create_all`** в тестах. **Невидимость якоря — choke point РЕЗОЛВА субъекта, `UserRepository`:** методы-резолверы (`list_all`/`get_by_id`/`get_with_teams`/`get_by_username`/`get_by_telegram`/`get_existing_ids`/`delete_by_id`) фильтруют `is_system` (`get_by_id` переписывается с `session.get()` на `select(...)`) ⇒ якоря нет в `GET /api/users` (`PATCH`/`DELETE` по его id → **`404`**), он не может быть лидером/участником команды (**`422`**), не резолвится при логине и Telegram-SSO; методы уникальности (`exists_by_*`) видят все строки. **`users` читают и мимо `UserRepository`** (`RoleRepository`, fan-out `MailTelegramLinkRepository.team_recipients`/`sees_all_candidates`, `SmsTelegramLinkRepository.recipients_for_team`) — там якорь отсекает INNER JOIN на Telegram-линк — поэтому **все три fan-out-выборки ОБЯЗАНЫ нести явный `WHERE NOT users.is_system`** (defense-in-depth: `sees_all_candidates` отбирает получателей **по правам роли**, а роль якоря — `admin`) + явный **ИНВАРИАНТ: у якоря нет строк в `user_teams`/`mail_telegram_links`/`sms_telegram_links`/`mail_user_settings`**; замена INNER→LEFT без фильтра запрещена. **Bootstrap сам коммитит свою транзакцию** (роль + якорь = один коммит). **Роли:** `user_count` якорь **исключает**, гард `is_in_use` — **включает** ⇒ роль `admin` не удаляется (**`409 role_in_use`**). **Границы (security):** `POST /api/sms/telegram/link` и `GET`/`PATCH /api/mail/me/settings` для супер-админа — **по-прежнему `403`**, но по security-основанию (Telegram-SSO дал бы беспарольный, неотзываемый из UI путь к admin-уровню в обход `ADMIN_PASSWORD`); bootstrap-учётка остаётся **console-only**. **Смена `ADMIN_USER`/`ADMIN_PASSWORD` — no-op** (идентичность на константе). **ОТМЕНЯЕТ [ADR-050](ADR-050-mail-search-team-filter-personal-read-state.md) §2.5 целиком**; амендмент [ADR-008](ADR-008-admin-iz-env.md)/[ADR-021](ADR-021-rbac-users-roles.md) («в `users` не попадает» → попадает системный якорь). UI: индикатор/фильтр «Непрочитанные»/«Отметить непрочитанным» рендерятся **всем** с `mail:view`; тумблер «Уведомления» у супер-админа **остаётся скрыт** | accepted | 2026-07-13 |
| [ADR-052](ADR-052-mail-mailbox-combobox.md) | **Единый combobox выбора/поиска почты на обеих вкладках `/mail` — новый примитив `ui/Combobox`.** **Требование владельца:** в обеих вкладках — поиск по почтам; клик по полю раскрывает список **всех** почт, ввод фильтрует список; поиск — по **Номеру / Приложению / самой почте**. **(1) Примитив `ui/Combobox`** (`frontend/src/components/ui/Combobox.tsx`) — **своя** реализация, **без новой зависимости** (нативный `<select>` фильтровать опции вводом физически не умеет; combobox-библиотека не добавляется — как `Select`/`MultiSelect`/`Textarea`). Контролируемый, **два независимых состояния**: `value` (выбор) + `query` (текст); фильтрация опций — **внутри** примитива по `ComboboxOption.keywords`; опция `pinned` (сброс) фильтром **не отсекается**. **Два режима:** `mode='select'` (текст эфемерный: фильтрует только список, при закрытии без выбора возвращается к лейблу выбранной опции; ввод выбор **не** сбрасывает) и `mode='search'` (текст — **значимое** состояние страницы; ввод **сбрасывает** выбор). **При ОТКРЫТИИ список показывается ЦЕЛИКОМ** (фильтрация — с первого введённого символа, флаг `dirty`) — буквальное требование владельца. **Очистка (§1.1а):** есть `pinned`-опция сброса → `X`/`Escape` ≡ **выбор этой опции** (`onChange(null)` НЕ эмитится), `X` при ней **не рендерится**, `placeholder` не задаётся ⇒ **«нет фильтра» = РОВНО ОДНО состояние** (`value=''`, текст «Все почты»); нет `pinned`-сброса → `onChange(null)` + пустое поле. **A11y обязателен и нормирован ПОЭЛЕМЕНТНО** (`role="combobox"`/`aria-expanded`/`aria-controls`/`aria-haspopup`/`aria-autocomplete`/`aria-activedescendant`; `role="listbox"`/`role="option"`/`aria-selected`; клавиши ↓/↑/Home/End/Enter/Escape-открыт/Escape-закрыт/Tab/печать; клик вне) — это первый **не-нативный** интерактивный примитив ДС. Портал не вводится; визуал — токены `Input`/`Select`, новых токенов нет. **(2) Вкладка «Сообщения»:** `ui/Select` «Почта» → `ui/Combobox` `mode='select'`; **семантика фильтра НЕ меняется** (серверный `mail_account_id`, `queryKey`, сброс пагинации, AND с «Команда»; опция **«Все почты»** — `pinned`). **Ввод фильтрует только список — ленту меняет только ВЫБОР.** **(3) Вкладка «Почты» — ГИБРИД (решение владельца):** `ui/Input`+`Search` → `ui/Combobox` `mode='search'`; **ввод → фильтрует ТАБЛИЦУ по ВСЕМ совпадениям** (таблица **не** схлопывается — поведение [ADR-050](ADR-050-mail-search-team-filter-personal-read-state.md) §1.1 сохранено), **выбор из списка → таблица сужается до ОДНОЙ строки** (быстрый переход); текст и выбор **взаимоисключающи**; сброс — `X`/`Escape`. Опции — из **того же** серверно-отфильтрованного (`is_active`) набора, что и таблица. **AND-комбинируемость** с сегментом активности и фильтром команды сохранена **в обе стороны** (§3.1а): **смена сегмента НЕ сбрасывает выбор/текст** — выпавший из набора выбранный ящик даёт **пустую таблицу** «Ничего не найдено» (штатное пустое пересечение), лейбл остаётся в поле, выход — явный (`X`/`Escape`); **авто-сброса чужих фильтров нет** ни в одном направлении. **(4) Правило поиска — ЕДИНОЕ во всех трёх местах** (списки обеих вкладок + таблица): **ровно три поля** `number`/`app_name`/`email`, подстрока, ci, `trim()`; **`display_name` НЕ входит**; **единый источник — ОДИН модуль** `features/mail/mailboxSearch.ts` (дублировать предикат ЗАПРЕЩЕНО — прецедент раздвоенного билдера `srcDoc`). **Разворачивает** [ADR-050](ADR-050-mail-search-team-filter-personal-read-state.md) §1.1 (**только контрол** — правило поиска и строки в силе) и §1.3 (порядок фильтров), а также норму [08 §Дропдауны](../08-design-system.md#дропдауны-почта-и-команда-серверные) «**«Почта»** — примитив `Select`». Уточняет (**не закрывает**) [TD-056](../100-known-tech-debt.md). **Backend/БД/миграции/RBAC/контракт API — НЕ меняются** (правка чисто frontend); `ui/Select` **не упраздняется** (в т.ч. фильтр «Команда» на обеих вкладках) | accepted | 2026-07-13 |
| [ADR-053](ADR-053-mail-timeouts-error-passthrough.md) | **Раздельные таймауты CRM → агрегатор, overall-deadline + проброс истинной причины отказа проверки ящика (прод-баг «502 вместо 422»).** **Разворот нормы «единый `MAIL_API_TIMEOUT_SEC` (10 с) достаточен для всех вызовов»** (амендмент [ADR-044](ADR-044-mail-full-merge-into-crm.md) §4 и [ADR-012](ADR-012-mail-read-through-proxy.md)): **(1)** две категории × два параметра — быстрые пути `MAIL_API_TIMEOUT_SEC=10` (read) / `MAIL_API_DEADLINE_SEC=30` (overall), **mail-server-пути** (`test`/create/`PATCH`/reply, где агрегатор идёт на удалённый IMAP/SMTP) `MAIL_API_MAILSERVER_TIMEOUT_SEC=75` / `MAIL_API_MAILSERVER_DEADLINE_SEC=85`. Паттерн [ADR-024](ADR-024-monitor-hard-deadline-backend-alert-grace.md) переиспользуется **обеими половинами**: явный `httpx.Timeout` **по фазам** (`connect` 5 / `write` 10 / `pool` 5 / `read` = бюджет) **И** overall hard-deadline `asyncio.wait_for` вокруг всего вызова — **per-phase лимиты + до 3 попыток ретрая суммарной границы НЕ дают** (запрос ушёл бы за `proxy_read_timeout` nginx → HTML-`504` вместо JSON CRM = возврат бага). Цепочка **одного вызова** `45 < 60 (nginx агрегатора) < 75 (read CRM) < 85 (overall CRM)`; **инвариант — НА ЗАПРОС, а не на вызов** (§1.2.1): один HTTP-запрос может делать **несколько** вызовов (`POST /mailboxes` при провале вставки зовёт компенсирующий `delete` сироты — короткий deadline **15 с**, константа `MAIL_CLEANUP_DEADLINE_SEC`, объявленная **один раз** в `config.py` и **импортируемая** `mail_service.py` (литерал запрещён — иначе валидатор стерёг бы своё зеркало, а не применяемое значение); `MailTimeout` обязан быть в её catch-листе, иначе подменит исходную ошибку), поэтому нормировано `Σ overall всех вызовов + внепробная работа CRM (≤5) < proxy_read_timeout nginx CRM (120)`; худший путь `85+15+5=105 < 120`. Машинная защита — `ge/le` на **каждом** параметре (вкл. быструю пару) **плюс кросс-полевой `model_validator`** (`ge/le` кросс-полевой порядок не выражают: `read=80`+`overall=76` проходят границы и ломают цепочку); **(2)** причина отказа доходит до UI: `422 imap_login_failed`/`smtp_login_failed`/`invalid_host` агрегатора → `422 mail_imap_failed`/`mail_smtp_failed`/`mail_invalid_host` CRM, `502 smtp_failed` (reply) → `502 mail_send_failed`; прочие **не-2xx** (401/403/`3xx`/…) — catch-all `502 mail_unavailable` (транспорт шлёт в `MailRejected` любой не-2xx ниже 500, кроме 429). Для этого **правится транспортный контракт** `infra/mail_client.py`: отдельное исключение `MailTimeout` **с полем `status_code: int | None`** (`504` = таймаут ОТ агрегатора, `None` = собственный таймаут CRM — иначе §2.1 нереализуема) + перенос `status_code`/`error_code` во **все** не-2xx ветки, включая `5xx` (сейчас всё ≥500 схлопывается в безликий `MailUnavailable` — §2 иначе нереализуема); **(3)** реальный таймаут ≠ «сервис недоступен» → новый код **`504 mail_timeout`** (не ретраится: анти-двойная-запись [ADR-038](ADR-038-mail-headless-integration.md) §1 сохранена); `504` **ОТ** агрегатора → `504 mail_timeout` на **любом** пути, собственный таймаут CRM на **быстром** пути → `502 mail_unavailable`; **(4)** nginx CRM `/api/`: **`proxy_read_timeout 120s`** (по бюджету **запроса** 105 с; прежнее 90 нормировало один вызов и на пути create+компенсация давало 115 > 90 = возврат бага). Ретрай-политика write (только `Connect*`) НЕ менялась. **Реализовано частично — [TD-059](../100-known-tech-debt.md):** backend (`config.py` с `ge/le` + кросс-полевым валидатором, `mail_client.py` с `MailTimeout.status_code` и двумя клиентами, 5 фабрик в `errors.py`, `mail_service.py`) и devops (nginx `120s`, оба env-примера) — **сделаны**; **SPA (§4) и тесты — нет**. Парная норма агрегатора (hard-deadline теста 45 с) **уже принята во втором репо** — их `ADR-0047`, долг `TD-055`; имя их env CRM не назначает | accepted | 2026-07-14 |
| [ADR-054](ADR-054-mail-app-password-field.md) | **Поле пароля ящика: лейбл «Код приложения» + перестановка под «Адрес почты».** Лейбл `«Пароль (IMAP)»` (`MailboxFormModal.tsx:981`) **отменён** → **«Код приложения»** (провайдеры требуют **app password**, а не пароль от почты; аккордеон-инструкция свёрнут, а лейбл виден всегда). Поле поднято с **последней** позиции формы **на вторую — сразу под «Адрес почты»**; **порядок полей формы ящика стал нормативным** ([08-design-system.md](../08-design-system.md#вкладка-почты-нормативно)): аккордеон → «Адрес почты» → **«Код приложения»** → «Номер»+«Приложение» → «Команда» → Outlook-секция → IMAP → SMTP → «Активна» (edit). **«SMTP-пароль (опц.)» НЕ переименовывается** (другое понятие — отдельные креды SMTP; плейсхолдер приведён к словарю: «По умолчанию — Код приложения»). Подсказки: add — «Не пароль от почты, а пароль приложения (app password)…», edit — «Оставьте пустым, чтобы не менять код приложения.»; ошибка поля — «Укажите код приложения». **Контракт API НЕ меняется** (поле запроса остаётся `password`; `400 details[].field="password"` маппится на новый лейбл); БД/RBAC/таймауты не тронуты — правка **чисто frontend + docs**. Осознанное созвучие «Код приложения» (секрет) ↔ «Приложение» (`app_name`) — по прямому требованию владельца; митигация — разные зоны формы и разный тип ввода. Ломает 3 qa-теста по старому лейблу (штатный хендофф на `qa`) | accepted | 2026-07-14 |
| [ADR-055](ADR-055-per-channel-teams-mail-sms.md) | **Отдельные команды для СМС и Почты у пользователя (per-channel scope) + фильтр по командам от 2 команд на пяти экранах.** **(1) Модель — union:** `effective_channel_teams = user_teams ∪ user_channel_teams[channel]`; базовые команды входят в scope **обоих** каналов всегда (снять нельзя), блок канала задаёт **добавку**. Новая таблица **`user_channel_teams(user_id, channel ∈ {mail,sms} CHECK, team_id)`** (PK-тройка, обе FK CASCADE, `ix_user_channel_teams_team_id`) + колонки **`users.mail_includes_unassigned`/`sms_includes_unassigned`** (`boolean NOT NULL DEFAULT false`) — «Без команды» = **флаг** доступа к объектам с `team_id IS NULL` (в PK `NULL` недопустим). Миграция **`0027_user_channel_teams`** (23 симв. ≤32); **backfill НЕ нужен** — при пустых добавках scope тождествен сегодняшнему (нулевая регрессия). **Инвариант нормализации:** сервис **вычитает** базовые `team_ids` из присланных `*_extra_team_ids` ⇒ снятие команды в основном блоке безусловно снимает доступ в обоих каналах. **(2) Scope:** `MailScope`/`SmsScope` += **`includes_unassigned: bool`**; предикат чтения/мутации — `obj.team_id IN team_ids OR (includes_unassigned AND obj.team_id IS NULL)`; прямое сравнение с `team_ids` в новом коде = дефект. Бесхозный ящик/номер становится доступен не-админу **только** при `includes_unassigned` (было — `403` всегда). **(3) Права:** доп-команда даёт **и видимость, и действия** (решение владельца; read-only отвергнут). **Сохранены без разворота:** создание ящика с `team_id=null` и **перенос** ящика между командами — **только admin-уровень** ([ADR-044](ADR-044-mail-full-merge-into-crm.md) §4). **(4) API:** `GET /api/auth/me += mail_teams`/`sms_teams` (`TeamRef[]`; не-админ — **эффективный** scope канала, **admin-уровень — ВСЕ команды системы**, `[]` НЕ отдаётся ⇒ `/me` — **единственный** источник опций команд канала на клиенте) и `mail_includes_unassigned`/`sms_includes_unassigned` (при `sees_all_*` → `true`); Users CRUD += `mail_extra_teams`/`sms_extra_teams` (ответ, **только добавка**) и `*_extra_team_ids`/`*_extra_includes_unassigned` (запрос, `PATCH` — полная замена набора); ленты `GET /api/mail/messages` и `GET /api/sms/messages` += **`no_team: boolean?`** (взаимоисключающ с `team_id` → иначе `400 validation_error`; вне scope → пустая страница). Каталог прав/предикат admin-уровня/анти-энумерация **не меняются**. **(5) UI:** в форме пользователя — два **свёрнутых** блока **«СМС»** и **«Почты»** (ручной collapsible-паттерн, [TD-057](../100-known-tech-debt.md); новый примитив НЕ вводится): чекбоксы всех команд + «Без команды»; базовая команда — **checked+disabled** («из блока «Команды»»), реактивна к основному блоку; отправляется только добавка. Фильтр «Команда» рендерится при **`(команд канала + «Без команды») ≥ 2`** (ветки «`sees_all` → всегда» НЕТ) — **едино** на **пяти** экранах: `/mail` (обе вкладки), `/sms` («Сообщения») и **обе Mini App** (`/tg/mail`, `/tg/sms`); опции — **только из `/api/auth/me`** у **любого** актора (`GET /api/teams` для фильтра не нужен никому; в Mini App запрещён). **(6) Telegram fan-out НЕ расширяется** на доп-команды (осознанная асимметрия). **Разворачивает** [ADR-036](ADR-036-sms-team-filter-admin-only.md) (гейт «только admin»), [ADR-050](ADR-050-mail-search-team-filter-personal-read-state.md) §1.2 и [ADR-037](ADR-037-mini-app-tabs.md) (в части «фильтров в Mini App нет»; «Все номера» в Mini App по-прежнему нет), а также **§5-оговорку [ADR-045](ADR-045-mail-outlook-oauth-headless-reonboarding.md)** («Без команды» показывать всем). **(7) Инвариант нормализации — на ОБОИХ путях записи в `user_teams`** (§2.3): users CRUD (вычитание базовых из добавок) **и teams CRUD** (`POST`/`PATCH /api/teams` c `member_ids` → удалить совпадающие `user_channel_teams`) — иначе «висящий» доступ к почте/СМС команды после исключения из неё. **(8) Асимметрия флага «Без команды»** (§3.1, принята владельцем осознанно): в СМС бесхозные номера рождаются **массово** из `POST /sms/numbers/sync` ⇒ `sms_includes_unassigned=true` = доступ **ко всему ещё не распределённому потоку** номеров с **полными** правами (правка/удаление/перенос); в почте бесхозные ящики заводит только админ. Митигация — обязательная UI-подсказка объёма, а не урезание прав. **(9) Перенос номера** (§3.2): целевая команда не-админу обязана ∈ scope (прежде — только существование) → `403`; `team_id=null` не-админу — только при `sms_includes_unassigned`. **(10) Источник команд канала на клиенте** (§6.3): **любой** контрол со списком команд у **любого** актора наполняется из `/api/auth/me` (фильтр, форма ящика, таблица «Почты», дропдаун переноса ящика, `Select` переноса номера); `GET /api/teams` остаётся только на страницах «Пользователи»/«Команды». Опция «Без команды» **предлагается к выбору** только тому, кто вправе её выбрать. **§6.3.1 (редакция 3, 2026-07-14) — исключение «зеркало текущего состояния»:** недоступную опцию можно **отобразить** при **всех трёх** условиях (она = текущее значение объекта; выбор невозможен — контрол `disabled` или обработчик no-op при `next === current`; без неё `<select>` показал бы **чужое** значение). Режим `add` и любой **enabled**-контрол под исключение **не подпадают** (там и жил прод-баг). Места: селектор «Команда» формы ящика в `edit` под не-админом у **бесхозного** ящика; оборонительное зеркало текущей команды в `Select` переноса номера. **ЗАКРЫВАЕТ [TD-058](../100-known-tech-debt.md), [TD-050](../100-known-tech-debt.md) и [TD-060](../100-known-tech-debt.md)** | accepted | 2026-07-14 |
| [ADR-056](ADR-056-mail-mini-app-mailbox-context.md) | **Mini App почты: «Номер · Приложение · Команда» в каждом письме.** Требование владельца «Код + Название + Команда, как у СМС» сопоставлено с **фактической** схемой (полей `code`/`name` у ящика нет): **Код → `mail_accounts.number` («Номер»)**, **Название → `mail_accounts.app_name` («Приложение»)**, **Команда → `team_id` → имя команды**; лейблы «Код»/«Название» в почте **вводить запрещено** (расщепило бы словарь ящика). **Контракт расширяется** (аддитивно): `MailAccountRef += number`/`app_name`/**`team: MailTeamRef \| null`** (`MailTeamRef = {id,name}` — зеркало `SmsTeamRef`; **LEFT JOIN `teams`** в том же запросе ленты, **N+1 запрещён**) — сегодня API этих полей **не отдаёт** (`backend/app/schemas/mail.py:30-35`), а имя команды на клиенте недостижимо (`GET /api/teams` под `teams:view`). **Миграций нет** (`number`/`app_name` — `0024`, `team_id` — `0021`). **UI Mini App** (`MailMiniAppCard` + деталь): пилюля команды **`ui/Pill tone="green"`** (`team.name`) / **`tone="neutral"`** «Команды нет» + ряд пилюль **«Номер: …»** (`tone="accent"`) и **«Приложение: …»** (`tone="yellow"`); пустое значение → **`'-'`** (как `orDash` в SMS — образец `SmsMessageCard`); **новый примитив НЕ вводится**. Строка **«Получено на: {display_name} <{email}>» ОТМЕНЕНА** → **«Получено на: {email}»** (`display_name` = склейка `number`+`app_name`, [TD-052](../100-known-tech-debt.md) — печатать дважды нельзя). **Десктоп `/mail` НЕ меняется** (владелец просил Mini App); `MailScope`/RBAC/sandbox-изоляция/reply/прочитанность не тронуты | accepted | 2026-07-14 |
| [ADR-057](ADR-057-mail-send-contract-fix.md) | **Контракт отправки письма после cut-over + восстановление reply на проде** (амендмент [ADR-044](ADR-044-mail-full-merge-into-crm.md) §8; парный агрегаторский `ADR-0048`). **Прод-баг:** CRM зовёт `POST /api/external/mailboxes/{id}/send` (`backend/app/infra/mail_client.py:225`), которого в агрегаторе **нет** ⇒ **ответ на письмо не работает с cut-over 10.07.2026** (внешний `404`; CRM маппила его в `mail_message_not_found` — `mail_service.py:1123-1124` — и вводила в заблуждение). **Норма:** **(1)** ответ агрегатора сужен до **`{ smtp_message_id }`** — `sent_id` там **невозможен** (его производил `INSERT` в `sent_messages`, которую агрегатор дропает); **(2)** публичный **`MailReplyResponse.sent_id` = `uuid`** = `mail_sent_messages.id` **самой CRM** (`models/mail_sent_message.py:35-37`), тип `int` → `string` (единственный потребитель — тип в `frontend/src/types/api.ts:350`, значение нигде не читается); **(3)** сцепка «публичная схема CRM = схема ответа внешнего сервиса» разорвана — сырой ответ агрегатора парсится отдельной внутренней схемой; **(4)** внешний `404` от `send` = **ящик не найден** → **`404 mail_mailbox_not_found`** (не `mail_message_not_found`). **Восстановление — fix-forward** (аддитивный релиз агрегатора A2.1 + парная правка CRM), **временный вызов `/messages/{id}/reply` ЗАПРЕЩЁН**: id писем CRM ≠ id агрегатора после cut-over (`preserve id` был только на ETL; новые письма CRM вставляет своим `BIGSERIAL` через `ON CONFLICT DO NOTHING` — `nextval` расходуется и на конфликтах штатных ре-push'ей) ⇒ риск ответа **по чужому письму**; плюс ретенция агрегатора 30 дней и их гейт `EXTERNAL_REPLY_ENABLED=false`. Reply → send **без потери функции** (threading/дефолты/scope формирует CRM; IMAP-APPEND в «Sent» сохранён). Заведён [TD-062](../100-known-tech-debt.md). **Амендмент §5–§7 (2026-07-14, находка `backend-reviewer`):** внутренняя схема ответа агрегатора **толерантна** — `smtp_message_id` **опционален**; `200` без него больше **не** даёт `502` **после уже состоявшейся отправки** (иначе письмо ушло бы, а строки `mail_sent_messages` не появилось бы — при **nullable** колонке `models/mail_sent_message.py:53`): факт отправки пишется всегда, публичный **`MailReplyResponse.smtp_message_id` → `string | null`**, неполнота ответа — в лог. Обязанность агрегатора возвращать поле не снята. Нормирован **контекст логов** reply: `mail_reply_mailbox_missing_in_aggregator` += `mail_account_id`/`message_id`, `mail_send_missing_smtp_message_id` += `sent_id`. Принцип: схема, парсящая ответ **после необратимого side-effect**, не строже модели данных | accepted | 2026-07-14 |
| [ADR-058](ADR-058-mail-accounts-dedup-orphan-remediation.md) | **Ремедиация осиротевших строк `mail_accounts` после dedup-merge** (амендмент [ADR-044](ADR-044-mail-full-merge-into-crm.md) §2; следствие демонтажа агрегатора, фаза C). После схлопывания 4 легаси-дублей email агрегатор удалил проигравшие ящики (id **73/85/140/145**), а CRM зеркалит каталог **по agg-id как PK** (`models/mail_account.py:38`, уникальности по email нет) ⇒ в CRM остались 4 «ящика-призрака», которые агрегатор больше не пушит, а авто-реконсиляции `mail_accounts` в CRM нет (`_reconcile_orphans` чинит только telegram-линки — `mail_dispatcher_service.py:344-349`). **Решение:** **(1)** сироты 73/85/140/145 **HARD-DELETE** (санкция merge: проигравшая команда теряет ящик); альтернативы «оставить неактивным»/«пере-привязать письма к survivor»/«только `team_id=null`» отклонены; **(2)** механизм — **alembic data-миграция** `0028_mail_dedup_orphan_cleanup` (30 симв. ≤ 32; `down_revision=0027_user_channel_teams`), `upgrade` = один `DELETE … WHERE id IN (73,85,140,145)`, идемпотентна (no-op где строк нет), DDL нет, `downgrade` = no-op (**необратимо**; санкционирован карв-аутом [03-data-model.md §2](../03-data-model.md#2-рабочий-downgrade--обязателен-карв-аут-необратимые-чисто-dml-миграции) для чисто-DML миграций — прецедент `0016_backfill_team_leaders`); **(3)** граф зависимостей (claims-from-code): на `mail_accounts.id` ссылаются **только** `mail_messages` и `mail_sent_messages`, обе **`ON DELETE CASCADE`**, транзитивно всё ниже (`mail_telegram_notifications`/`mail_message_tags`/`mail_message_reads`) — тоже CASCADE ⇒ один `DELETE` сносит поддерево атомарно, ручной порядок не нужен; **(4)** survivor'ы 18/126/28/71 — **другие id**, не затронуты; **удалять по email ЗАПРЕЩЕНО** (нет уникальности, проигравший и survivor делят адрес — снесло бы survivor'а); **(5)** прогон на **КОПИИ прод-данных CRM** обязателен; **hard-delete исторического архива** проигравших ящиков (письма/reply/теги/прочитанность) — **отдельная необратимая потеря данных**: **санкция владельца ПОЛУЧЕНА** (2026-07-15) при условии **предварительного точечного снапшота** удаляемых строк (§5.1, «сначала снапшот, потом удалить», ответственный orchestrator/devops) ⇒ открытого gate санкции нет, предпосылка исполнения — снапшот + прогон на копии ([TD-063](../100-known-tech-debt.md)). PII (email) в ADR/миграцию/реестры не выносится — удаление по id | accepted (санкция получена при условии снапшота §5.1 — [TD-063](../100-known-tech-debt.md)) | 2026-07-15 |
| [ADR-059](ADR-059-documents-module.md) | **Гринфилд-модуль «Документы»** (Notion/Kaiten-подобный менеджер знаний в БД CRM). Единая self-referencing таблица **`document_nodes`** (`node_type ∈ {folder,document}`, `parent_id` дерево, `content_md`, `owner_id`, `visibility_mode ∈ {inherit,restricted}`, `content_version bigint`, `deleted_at`) + link-таблица **`document_node_roles`** (composite PK, образец `user_channel_teams`). **Enforcement — permission-based, НЕ owner-based** (`owner_id` — только автор для отображения); каталог += страница `documents:view/create/edit/delete/**share**`. **Видимость по РОЛЯМ** (`role_id`, НЕ команды): наследование **вычисляемое** (рекурсивный CTE вверх до ближайшего `restricted`-предка; полностью `inherit` до корня → узел публичен внутри модуля); admin-уровень (`is_superadmin OR permissions_subset(full_catalog,permissions)`) видит всё; невидимая нода → `404 document_node_not_found` (анти-энумерация). **Soft-delete** (`deleted_at`) — осознанный отход от hard-delete репо ради RAG-tombstone (первый soft-delete в проекте). `role-refs` под `documents:share` (не admin-gated `/api/roles`). Индекс `(updated_at,id)` уточнён под внешний keyset (вкл. tombstones). TD-064/065/066/067 | accepted | 2026-07-17 |
| [ADR-060](ADR-060-documents-external-readonly-api-key.md) | **Внешний read-only API-ключ модуля «Документы»** (RAG). Статический **`X-API-Key`**, хранение — только env **`DOCUMENTS_API_KEY`** (класс `MAIL_API_KEY`: не в БД/логах/ответах/URL), сравнение **constant-time `hmac.compare_digest`**; порядок (образец mail_ingest): пустой секрет → `503 documents_external_not_configured` → неверный/нет заголовка → `401 not_authenticated`. **НЕ HMAC над телом** (read-only GET без тела — подписывать нечего). **Машина видит ВСЕ узлы** (обходит per-role фильтр), но каждый ответ несёт **`visibility_role_ids[]`** + `content_version` (RAG фильтрует по роли сам; публичный → `[]`). Эндпоинты: `GET /` (keyset `(updated_at,id)` ASC + `include_deleted`), `GET /{id}` (удалённый → `410 document_node_gone`), `GET /{id}/access`, `GET /changes?since=`. Привязку ключа к роли/scope отклонили (преждевременная сложность, NFR-1) | accepted | 2026-07-17 |
| [ADR-061](ADR-061-documents-sidebar-two-panel-nav.md) | **Двухпанельный сайдбар-layout `/documents`** (частичный разворот плоской навигации [ADR-033](ADR-033-flat-nav-theme-toggle-numbers-table.md)). Хэдер-навигация ADR-033 **сохраняется**; добавляется пункт «Документы» (#10, `documents:view`), `DefaultRoute`-порядок дополнен `documents` в конце. `/documents` — **второй full-bleed маршрут** (после `/mail`) с двухпанельным layout (сайдбар `TreeView` + панель контента/`DocumentEditor`); `isFullBleed = startsWith('/mail') || startsWith('/documents')`. Разворачивает **только layout-ветку** ADR-033 «все не-full-bleed, кроме `/mail`». Примитивы: kebab (`@radix-ui/react-dropdown-menu` — уже в deps), `TreeView`, сайдбар-shell | accepted | 2026-07-17 |
| [ADR-062](ADR-062-documents-wysiwyg-tiptap.md) | **WYSIWYG-редактор документов на TipTap** (`@tiptap/react`+`@tiptap/starter-kit`+`@tiptap/extension-link`+`tiptap-markdown`) — **новая frontend-зависимость**, осознанный отход от NFR-1. Хранение остаётся **каноничным markdown** в `content_md` (редактор сериализует ProseMirror ↔ markdown; БД/RAG видят только markdown; редактор заменяем без миграции данных). Свой WYSIWYG на `contenteditable` отклонён (объём/риск). `@radix-ui/react-dropdown-menu` новой зависимостью НЕ является. **Поправка §2 (2026-07-18):** граница расширена `@tiptap/extension-link` (официальное расширение TipTap) — без него markdown-ссылки теряют URL при round-trip; пункт тулбара «ссылка» сохранён. **Амендирован [ADR-068](ADR-068-documents-image-attachments.md) (2026-07-22):** перечень перестал быть закрытым — действующий состав **шесть пакетов** (+`@tiptap/extension-image`, +`@tiptap/pm` 2.27.2 — явный пин ранее транзитивного ProseMirror-бандла под прямые импорты `@tiptap/pm/*`) | accepted | 2026-07-17 (поправка 2026-07-18, амендмент 2026-07-22) |
| [ADR-063](ADR-063-documents-editor-cache-lifecycle-focus.md) | **«Документы»: контракт кэша узла после `PATCH`, жизненный цикл `DocumentEditor` и фокус по клику** (пакет фиксов действующей нормы; backend/модель/внешний RAG-контур **не меняются**). **§A Кэш:** ответ `PATCH /api/documents/nodes/{id}` несёт **`content_md: null`** (мутация, не чтение; единственный read-канал контента — `GET /nodes/{id}`) ⇒ класть ответ в кэш узла **целиком запрещено** — он **сливается** поверх предыдущего значения (`content_md` — из тела запроса при строгом `!== undefined`, иначе из прежнего кэша; нет прежнего значения → частичная запись не сеется). Правило покрывает и сохранение контента, и **переименование** (общий хук); инвалидация — только в ветке `409`. Возврат `content_md` в ответе `PATCH` **отклонён** (удвоение тела до 1 МБ ради данных, известных клиенту). **§B Жизненный цикл — разворот нормы:** «keyed родителем по `id:content_version`» (реализация [ADR-062](ADR-062-documents-wysiwyg-tiptap.md)) **ОТМЕНЕНА** — ключ ремоунта **только `id`**; контент ресинкается при расхождении `content_version` с **базовой версией текущего маунта** (внешняя правка / рефетч после `409`), базовая версия обновляется в колбэке мутации ⇒ собственное сохранение ресинк не вызывает, курсор и скролл сохраняются. Optimistic-lock ([TD-064](../100-known-tech-debt.md)) **не меняется**. **§C Фокус:** клик по **любому** месту области документа при `documents:edit` ставит курсор (координаты зажимаются в прямоугольник области ⇒ клик правее колонки → конец **той же** строки; фолбэк — конец документа); клик внутри текста, drag-выделение и клик по ссылке не меняются; в режиме просмотра фокус не ставится (трейд-офф: протяжка-выделение **из полей** не работает). Колонка контента **центрируется**, текст остаётся по левому краю; **выравнивание текста как функция редактора НЕ вводится** (невыразимо в каноничном markdown, формат `content_md` фиксирован RAG-контрактом [ADR-060](ADR-060-documents-external-readonly-api-key.md)). **§D Локализация каталога прав:** сервер отдаёт только технические ключи, локализация — на фронтенде (норма не меняется); **инвариант** — каждый ключ каталога обязан иметь русскую подпись, фолбэк «сырой ключ» не допустим как конечное состояние UI (`documents → «Документы»`). **Почему отдельный ADR, а не поправка к ADR-062:** поправка in-place применяется при **расширении границы того же решения**, здесь — **разворот** ранее принятой нормы + контракт, действующий для любого редактора; репо-практика фикс-пакетов — отдельный ADR ([ADR-046](ADR-046-ui-infra-fix-pack.md)/[ADR-047](ADR-047-mail-fix-pack.md)/[ADR-057](ADR-057-mail-send-contract-fix.md)/[ADR-058](ADR-058-mail-accounts-dedup-orphan-remediation.md)). Заведён **[TD-068](../100-known-tech-debt.md)** (матрица `/roles` рендерит только CRUD ⇒ `documents:share`/`mail:sync`/`mail:tags`/`sms:transfer`/`sms:sync` не выдаются через UI). | accepted | 2026-07-20 |
| [ADR-064](ADR-064-design-tokens-channel-format.md) | **Цветовые токены ДС в channel-формате `rgb(var(--x) / <alpha-value>)`** — системный фикс невидимой в светлой теме `danger`-кнопки и **~25** «тихо» сломанных alpha-мест (границы карточек ошибок `border-status-red/70`, тонировки `bg-status-red/5`/`bg-accent/15`, фокус-кольца `ring-accent/40`/`ring-status-red/40`, полупрозрачные фоны `bg-surface-1/40`/`bg-bg-base/80`, `disabled:bg-accent/60`). **Root cause:** токены хранили готовый цвет (`--status-red: #DC2626`, конфиг `var(--status-red)`) → Tailwind 3.4 собирал невалидное `rgb(#DC2626 / .9)` и **опускал свойство** (прозрачный фон). **Решение:** 15 токенов из `colors` → space-separated RGB-триплет в `index.css` (обе темы) + `rgb(var(--x) / <alpha-value>)` в `tailwind.config.ts`; сплошная заливка без альфы продолжает работать (`<alpha-value>`→`1`). **Цена (весь `frontend/src`, не только `index.css` — конверсия глобальна):** сырые потребители конвертируемого токена как готового цвета обёртываются в `rgb(var(--x))` — и в `index.css` (§C.1), и вне его в `.ts/.tsx` (§C.2: инлайн-стили `main.tsx`/`Pill.tsx`, SVG-атрибуты `Gauge.tsx`, `color-mix()` `MailTagChip.tsx`, JS-константа `ZONE_COLOR` в `zones.ts`). НЕ конвертируются тени, gauge-градиент-стопы и Telegram-нативные `--tg-*`. **Инвариант:** новый цветовой токен — только channel-формат. Локальный фикс `Button` (A) отклонён (чинит 1 из ~25 мест, оставляет мину). Механизм темы не затронут. Новой зависимости нет. | accepted | 2026-07-21 |
| [ADR-065](ADR-065-users-flat-list-team-chips.md) | **Страница `/users` — плоский список с чипами команд** вместо группировки по командам. Один элемент на пользователя (без дублирования), команды — **чипами** из `user.teams` (фолбэк «Без команды»), сортировка по `username`. Сохранены: роль, тристатус-бейдж, «Без пароля», клик→edit-модалка, кнопки add/edit, `useTeams` (нужен `AddUserModal`). Backend-контракт `GET /api/users` (плоский `items[]`) и модалки **не меняются** — правка чисто клиентская. Прежняя норма «сгруппирован по командам, пользователь дублируется в каждой секции» ([ADR-021](ADR-021-rbac-users-roles.md)/[ADR-022](ADR-022-teams-nav-categories.md)) — **отменена** в части раскладки. | accepted | 2026-07-21 |
| [ADR-066](ADR-066-tailwind-config-in-app-tsproject.md) | **`tailwind.config.ts` включён в файловый список app-проекта** (`tsconfig.app.json`, `include`: `["src"]`→`["src","tailwind.config.ts"]`) — фикс **TS6307** упавшей чистой прод-сборки (`tsc -b && vite build`). **Root cause:** токен-контракт-тест `Button.tokens.test.ts` ([ADR-064](ADR-064-design-tokens-channel-format.md)) статически импортирует `tailwind.config.ts` для non-vacuous сверки реального конфига; тест под `src` входит в `tsconfig.app.json`, а конфиг вне `src` не входил в файловый список ни одного composite-проекта → в build-режиме `tsc -b` это TS6307 (локально маскировалось инкрементальным `tsBuildInfo`-кешем; поймала чистая Docker-сборка). **Выбор app-проекта:** импортёр-тест уже в нём; `tailwind.config.ts` тянет только `import type { Config }` (type-only) — новых типов/ошибок в app-проект не добавляет; node-проект — не импортёр, добавление туда TS6307 не сняло бы. Отклонены: убрать импорт из теста (вернул бы vacuous-тест, §B), отдельный tsconfig под конфиги (оверинжиниринг, §C). **§Урок:** инкрементальный `tsBuildInfoFile` маскирует TS6307 локально ⇒ эталон type-сборки = чистая сборка без кеша (удалить `node_modules/.tmp/*.tsbuildinfo` / Docker-сборка). Логика [ADR-064](ADR-064-design-tokens-channel-format.md)/[ADR-065](ADR-065-users-flat-list-team-chips.md) не меняется. | accepted | 2026-07-21 |
| [ADR-067](ADR-067-server-ssh-key-auth.md) | **Вход на целевой сервер по приватному SSH-ключу — второй способ РЯДОМ с паролем** (парольный вход сохраняется). **(1) Модель:** `servers += auth_method text NOT NULL DEFAULT 'password' CHECK IN ('password','key')`, `ssh_private_key_encrypted`/`ssh_key_passphrase_encrypted bytea NULL` (Fernet, тот же `FERNET_KEY` — [ADR-007](ADR-007-shifrovanie-fernet.md)); **`ssh_password_encrypted` → nullable**; согласованность материала держит **CHECK `ck_servers_auth_material`** (ровно один способ; passphrase — только с ключом), а не только сервис. **(2) Миграция `0031_servers_ssh_key_auth`** (25 симв.): `server_default='password'` на `ADD COLUMN` делает **backfill излишним** (старые строки уже удовлетворяют CHECK, который добавляется последним) ⇒ полная совместимость старых записей и старого тела запроса. **`downgrade()` рабочий, но ЛОССИ:** key-серверы в старой схеме непредставимы ⇒ `DELETE FROM servers WHERE auth_method='key'` (заглушка-пароль запрещена — провижининг пошёл бы с заведомо неверным паролем); откат требует pre-deploy `pg_dump`. **(3) Контракт `POST /api/servers`:** `auth_method` **опционален (default `password`)** ⇒ ломающего изменения нет; валидация — ровно один способ (лишнее/недостающее поле → `422` с точным `field`), лимиты (`ssh_private_key` ≤ `SSH_KEY_MAX_BYTES`=16 КБ, passphrase 1–256), нормализация CRLF→LF + завершающий `\n`, **реальный разбор ключа `cryptography`** (не regex — тот пропускает битую base64 и переносит ошибку в `status=error`), но **процедурой из 4 шагов**, т.к. наивный `load_*(password=…)` требуемых исходов не даёт (на 43.x все негативные ветки — один `ValueError`, лишний пароль к незашифрованному ключу молча игнорируется): **(1)** `is_encrypted` определяется **структурно**, ветки сверху вниз (armor `ENCRYPTED PRIVATE KEY` → да; `… PRIVATE KEY` + `Proc-Type: 4,ENCRYPTED`/`DEK-Info` → да; **`PRIVATE KEY`/`RSA`/`EC`/`DSA PRIVATE KEY` БЕЗ `Proc-Type` → НЕТ** — самый частый, незашифрованный PKCS#1/PKCS#8/SEC1; `ciphername` в `openssh-key-v1`), catch-all «неразбираемое» → `422 ssh_private_key`; **(2)** кросс-проверка `is_encrypted` ↔ наличие фразы даёт оба «passphrase»-исхода **без участия библиотеки**; **(3)** загрузка, ветка отказа выбирается по **уже известному** `is_encrypted`, а не по типу/тексту исключения; **(4)** тип ключа — RSA/ECDSA/**Ed25519**, **DSA отвергается на валидации** (deprecated в cryptography 43, отключён в OpenSSH ≥7.0 ⇒ пере-сериализация падает; иначе ключ прошёл бы форму и умер на провижининге). Текст исключения наружу и в лог **не пробрасывается**. Схемы ответа += `auth_method` (флаги `has_*` не вводятся — CHECK делает их избыточными). `PATCH` по-прежнему только `name`. **(4) Reveal — СУЖЕНИЕ [ADR-035](ADR-035-detail-view-secret-reveal.md):** эндпоинтов раскрытия ключа/passphrase **НЕ вводится** (write-only секреты); `GET /api/servers/{id}/ssh-password` при `auth_method='key'` → **`404 secret_not_set`**. Обоснование: радиус поражения ключа — весь парк хостов (пароль — один сервер); у сервера `PATCH` правит лишь `name` ⇒ симметрии «edit уже управляет секретом» нет ([Q-SEC-5](../99-open-questions.md)); рабочего сценария «набрать ключ руками» не существует; half-reveal passphrase бессмыслен. **(5) Провижининг:** passphrase **снимается в памяти** (пере-сериализация в незашифрованный OpenSSH-PEM) → файл `0600` через `os.open(O_CREAT\|O_EXCL, 0o600)` внутри `private_data_dir` (`0700`) → `ansible_ssh_private_key_file`; `ansible_password` в key-режиме не задаётся; удаление в `finally`. Штатный `ansible_runner(ssh_key=…)` **отклонён** — на зашифрованном ключе виснет на приглашении `ssh-add` до `job_timeout` (добавление сервера → ложный `provisioning timeout`). Требование devops: `private_data_dir` — `mkdtemp(dir=**`ANSIBLE_PRIVATE_DATA_ROOT`**)`, каталог заведён в Dockerfile (`chown app:app`, `0700`) и перекрыт **`tmpfs` с `mode: 0o1777`** (⛔ поле числовое `uint32`: значение **в кавычках** отвергается на разборе и роняет `docker compose config`/`up` целиком, а голое `1777` разберётся **десятично** → `0o3361`, что равносильно `1700`; ⛔ `1700`/`0o3361` ломает `mkdtemp` под non-root `app` и убивает **весь** провижининг, включая парольный; ⛔ `tmpfs` на `/tmp` не монтируется — там спулится multipart). Новый `error_message` `"SSH key unusable"`. `sshpass` остаётся обязательным (password-режим). **(6) UI:** переключатель «Пароль / SSH-ключ» в `AddServerModal` (add; переключение **очищает** поля другого режима), строка «Способ входа» в `ServerDetailModal`; при `key` — маска `••••••••` **без кнопки-глаза** (первый не-раскрываемый секрет: глаз не рендерится ни при каком праве). Заведены [TD-072](../100-known-tech-debt.md)/[TD-073](../100-known-tech-debt.md). **Реализовано 2026-07-22** (миграция `0031`, `app/domain/ssh_keys.py`, key-ветка провижининга, UI-переключатель); **тесты зелёные 2026-07-23** (149 кейсов; runtime-smoke/состав образа/реальный плейбук — не прогонялись) | accepted | 2026-07-22 |
| [ADR-068](ADR-068-documents-image-attachments.md) | **Изображения в документах: файлы на volume + таблица `document_attachments` + авторизованная отдача** (решение владельца по хранению — файлы на диске + метаданные в БД, ссылка `![alt](/api/documents/attachments/{id})`; жесты — кнопка тулбара и **Ctrl+V**, drag-and-drop не вводится). **(1) Путь строится ТОЛЬКО из машинных значений** — `${DOCUMENTS_ATTACHMENTS_DIR}/<id[0:2]>/<id[2:4]>/<id>.<ext>`, `<ext>` из **`mime`** по константному whitelist-маппингу; **`filename` в пути не участвует вовсе** ⇒ path traversal невозможен конструктивно (санитайзинг имён исторически обходится); плюс defensive `realpath`-containment. Права **`0600`/`0700`** — намеренно строже `file_sd` (`0644`/`0755`): там второй читатель под другим uid (Prometheus), здесь читатель один. `checksum` = sha256 → **`ETag`** + контроль целостности; UNIQUE не вводится (дедуп с refcount усложнил бы copy/GC). **(2) Эндпоинты:** `POST /nodes/{id}/attachments` (**`documents:edit`** — вложение есть часть контента существующего узла, а `create` в модуле = «создать узел»), `GET /attachments/{id}` (`documents:view` + **тот же CTE видимости узла-владельца**), `DELETE /attachments/{id}` (`documents:edit`). Все негативные исходы отдачи → **один** код `404 document_attachment_not_found` (анти-энумерация). Валидация: размер ≤ `DOCUMENTS_MAX_IMAGE_BYTES`=5 МБ (обрыв по мере чтения), тип — **по magic bytes, не по `Content-Type` клиента**, whitelist ровно `png/jpeg/webp/gif`; **SVG исключён нормативно** (активный документ с нашего origin = XSS, не закрывается ни `nosniff`, ни CSP страницы); отказ → `422 document_attachment_invalid`. Порядок: `INSERT`→`flush`→атомарная запись файла→`commit` (строки без файла не бывает; обратная сирота — GC). **(3) Отдача — авторизованный `fetch` + `blob:`-URL, а НЕ `<img src>` напрямую:** JWT живёт в `localStorage` ⇒ `<img>` уходит без `Authorization`, а «починка» анонимным доступом = утечка картинки в обход видимости узла. Цена — **CSP `img-src` += `blob:`** (пассивный, same-origin, созданный нашим же JS; `script-src 'self'` не тронут) + обязательный `URL.revokeObjectURL`. Отклонены: анонимный UUID-URL (obscurity, неотзываемо), подписанный токен в query (секрет в access-логах — прямой запрет 05-security), cookie-сессия (CSRF-контур с нуля), `data:` (+33 % памяти, синхронная конверсия). **(4) `allowBase64: false`** — `data:`-картинки в `content_md` **запрещены** (иначе Ctrl+V съедал бы лимит 1 МБ base64 и обходил хранилище); любая картинка проходит через upload. **(5) Soft-delete** узла файлы не трогает, отдача → `404`; физуборка — с GC tombstones ([TD-067](../100-known-tech-debt.md)). **«Создать копию»** копирует вложения **физически** (новые `id`+файлы) и **переписывает ссылки в `content_md` копии** литеральной заменой `<old_uuid>`→`<new_uuid>` (regex по markdown запрещён); shared-файлы с refcount отклонены (копия перестала бы быть независимой). **(6) Внешний RAG-контур [ADR-060](ADR-060-documents-external-readonly-api-key.md) НЕ расширяется** — бинарников наружу нет, `ExternalDocumentNode` без `attachments[]`: вложения **иммутабельны** (замена = новый `id` = новый `content_md` = `content_version+1`) ⇒ версия и водяной знак уже полностью описывают состав узла ([TD-074](../100-known-tech-debt.md)). **(7) Кэш:** `private, max-age=300, must-revalidate` + `ETag`/`304`; **`public` запрещён** (ответ зависит от прав ⇒ shared-кэш отдал бы картинку постороннему). **(8) Deploy:** новый volume `documents-attachments` (только backend), **бэкап становится двухкомпонентным и согласованным** (`pgdata` + volume). Новая зависимость `@tiptap/extension-image`. Миграция **`0032_document_attachments`**. **Частично закрывает [TD-065](../100-known-tech-debt.md)** (изображения закрыты; произвольные файлы — нет). **Реализовано 2026-07-22** (миграция `0032`, три эндпоинта, копирование вложений при copy, TipTap image + blob-резолвер, tmpfs/volume/CSP `blob:`); в манифест добавлен и **`@tiptap/pm`** (амендмент [ADR-062](ADR-062-documents-wysiwyg-tiptap.md) — шесть пакетов); **тесты зелёные 2026-07-23** (backend 1590 passed на реальном Postgres, frontend 941/941; runtime-smoke не прогонялся); [TD-077](../100-known-tech-debt.md) (`409 user_in_use`) реализован и покрыт, сужен до отсутствия передачи владения | accepted | 2026-07-22 |
| [ADR-069](ADR-069-backend-users-page-admin-contract.md) | **Страница «Пользователи бэков» + универсальный CRM Admin API contract v1.** CRM — **прокси без хранилища** к admin-эндпоинтам бэков: ключ из `admin_api_key_encrypted` ([ADR-040](ADR-040-backend-relations-secrets-reverse-lookup.md)) расшифровывается в памяти → заголовок `X-Admin-Key`; собственных таблиц/миграций нет. Контракт v1 (6 read + 2 write, ISO 8601 UTC, `limit/offset`, сортировка `registered_at DESC` нормативна, опциональные блоки = `null`): эталон — `BA/crm-admin-api-contract.txt`, сводка — [modules/backend-users](../modules/backend-users/README.md). **Два префикса** (`/api/billing/admin` / `/v1/admin`) — автоопределение с кэшем в памяти, без поля в БД. Режим «Все приложения»: fan-out (семафор 5) + merge-пагинация окном ≤1000; упавший источник → `errors[]` (partial data), при явном `backend_id` — транзит точной ошибки. RBAC: новая страница каталога `backend-users` (`view`/`edit`), admin-операции под `edit` + аудит `backend_admin_action`. Идемпотентность: токены — НЕ идемпотентны (UI блокирует двойной сабмит); план — идемпотентен по `grant_id` (генерируется при открытии модалки). Маршруты `/backend-users`, `/backend-users/:backendId/:userId`. Бэки (вкл. 232-claude-backend) реализуют контракт отдельными задачами в своих репозиториях | accepted | 2026-07-23 |
