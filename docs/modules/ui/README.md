# Модуль `ui` — Страница «Серверы» и спидометры

Статус: `spec-ready` · Исполнитель: frontend

## Scope
SPA: двухшаговый вход, страница «Серверы» (сетка карточек), кастомные SVG-спидометры, карточка «+ Добавить» + модалка. Дизайн — [08-design-system.md](../../08-design-system.md), по референсу [`docs/assets/reference.png`](../../assets/reference.png). API — [04-api.md](../../04-api.md).

## Компоненты (нормативно)
- `LoginPage` — двухшаговый вход ([modules/auth](../auth/README.md), [08-design-system.md](../../08-design-system.md#экран-входа-двухшаговый)). **Только карточка с формой — без заголовка продукта, подзаголовка и логотипа над формой.** Плейсхолдер поля идентификатора — **«Логин или @username»** (было «admin или @username»); Telegram-ник принимается с/без `@` ([ADR-041](../../adr/ADR-041-login-theme-session-ux.md)). Экран рендерится в **светлой** теме по умолчанию (дефолт светлый, [ADR-041](../../adr/ADR-041-login-theme-session-ux.md)).
- `ServersPage` — **адаптивная сетка 1 → 2 → 3 колонки** (`grid-cols-1 md:grid-cols-2 xl:grid-cols-3`, gap 24px, до 3 карточек в ряд); данные через TanStack Query с `refetchInterval = VITE_POLL_INTERVAL_MS` (15000) на запрос `GET /api/servers` (см. стратегию polling ниже).
- `ServerCard` — шапка (иконка, имя, статус-точка «В сети/Не в сети», «Аптайм», «Обновлено») + 3 под-карточки в ряд (`grid-cols-3`) + **внизу сворачиваемая секция «Бэков: N»** (`backend_count`; ленивый `GET /api/servers/{id}/backends` **только по раскрытию** — [ADR-049](../../adr/ADR-049-servers-backends-card-first-detail.md) §2, [ADR-040](../../adr/ADR-040-backend-relations-secrets-reverse-lookup.md)). **Клик по карточке = read-only `ServerDetailModal`**, где **сразу видны четыре строки: Название / IP / Пользователь / Пароль** (`••••••••` + глаз-reveal под `servers:edit`); **блок «Информация» в модалке упразднён, секции «Бэки» в модалке больше нет** ([ADR-049](../../adr/ADR-049-servers-backends-card-first-detail.md) §1). **Карандаш в detail → inline-редактирование `name` прямо в модалке** (Сохранить/Отмена, `PATCH name`; **не** отдельная `AddServerModal` — [ADR-039](../../adr/ADR-039-ui-server-inline-edit-backends-search-empty-sms-label.md)); **зажатие ~200 мс + движение = перетаскивание** (@dnd-kit). Кнопка **Удалить** **и триггер «Бэков: N»** — `stopPropagation` (триггер к тому же **не drag-ручка**). Паттерн — [08-design-system.md](../../08-design-system.md#detail-view-карточных-страниц-read-only--карандаш--edit-adr-035).
- `MetricSubCard` — заголовок метрики + `Gauge` + абсолютные значения (CPU — `N ядер`, RAM/SSD — `used/total ГБ`).
- `Gauge` — кастомный SVG, дуга 270°, цвет по `usageToZone(value)`, **в центре только число (без `%`, без подписи «Usage», без меток `0%`/`100%`)**, анимация, `role="meter"` (ARIA сохраняет проценты). Спецификация — [08-design-system.md](../../08-design-system.md#компонент-gauge-кастомный-svg).
- ~~`AddServerCard`~~ — **УПРАЗДНЕНА** ([ADR-046](../../adr/ADR-046-ui-infra-fix-pack.md) §2б). Пунктирные карточки-плейсхолдеры (`AddServerCard`/`AddAiKeyCard`/`AddProxyCard`/`AddBackendCard`) удалены; добавление — кнопкой **«Добавить»** в [правой зоне заголовка страницы](../../08-design-system.md#заголовок-страницы-и-правая-зона-действий-нормативно). Историческое описание: glass/blur, по центру «+ Добавить», клик → модалка.
- `AddServerModal` — Radix Dialog. **Режим add:** 4 поля (Название, IP, Пользователь, Пароль), валидация, loading, обработка `409`/`422`. **Режим edit для сервера БОЛЬШЕ НЕ используется** ([ADR-039](../../adr/ADR-039-ui-server-inline-edit-backends-search-empty-sms-label.md)): редактирование `name` — **inline прямо в `ServerDetailModal`** (карандаш → редактируемое поле «Название» + Сохранить/Отмена, `PATCH /api/servers/{id} {name}`). Режим `add` (создание) — без изменений. UI-детали — [08-design-system.md](../../08-design-system.md#инлайн-редактирование-сервера-в-detail-view-adr-039).
- **Перестановка серверов** — единый `SortableContext` (@dnd-kit) поверх сетки; на `onDragEnd` — оптимистичное обновление порядка + `PATCH /api/servers/order {ids}`; при ошибке — откат и инвалидация `GET /api/servers`. Активация drag — `PointerSensor` delay ~200 мс (**короткий клик открывает read-only `ServerDetailModal`**, edit — по карандашу в detail; зажатие тащит — [ADR-035](../../adr/ADR-035-detail-view-secret-reveal.md)).

## Требования
1. Тёмная тема по умолчанию, токены/типографика/сетка строго из [08-design-system.md](../../08-design-system.md).
2. Цвет дуги — ТОЛЬКО по нагрузке (зелёный<80 / жёлтый 80–90 / красный>90), одинаково для CPU/RAM/SSD; единая функция `usageToZone` (совпадает с backend).
3. Состояния: loading(skeleton), empty, provisioning(pending/installing с прогрессом), error(провижининг), offline, hover/focus/disabled, toast (sonner).
4. **Стратегия polling (нормативно):**
   - **Routine-метрики:** один запрос `GET /api/servers` через TanStack Query с `refetchInterval = VITE_POLL_INTERVAL_MS` (15000 мс). Список уже содержит метрики всех серверов → **per-card `GET /api/servers/{id}/metrics` в routine-цикле НЕ используется** (избегаем N запросов). `GET /api/servers/{id}/metrics` зарезервирован на будущее (детальное обновление одной карточки) и на Этапе 1 фронтом не вызывается в цикле.
   - **Provisioning-статус:** `GET /api/servers/{id}/status` опрашивается (interval ~2–3 с) **только для карточек в статусе `pending`/`installing`** и прекращается при переходе в `online`/`error`. Для `online`/`error` статус-polling не запускается.
   - После `POST /api/servers` (202) карточка появляется в `pending`; запускается status-polling до `online`/`error`, затем карточка обновляется из общего `GET /api/servers`.
5. **Drill-down ссылки из карточки на Grafana НЕТ** (удалена на Этапе 1 — [ADR-005, поправка](../../adr/ADR-005-custom-gauge-vs-grafana-embed.md#поправка-2026-06-30--удаление-drill-down-ссылки-из-карточки)). Конфигурация `VITE_GRAFANA_URL` удалена. Grafana доступна администратору напрямую через `/grafana`; карточка внешних ссылок не содержит.
6. a11y: focus-ring, aria для gauge/модалки, контраст ≥AA, `prefers-reduced-motion` (NFR-8).
7. Формат uptime: секунды → `Nд Nч Nм` (рус.). Метрики/IP/числа — моношрифт.
8. На `401` — редирект на `/login`.
9. **Локализация UI — русский.** Все пользовательские строки строго по [словарю локализации](../../08-design-system.md#локализация-ui-русский-словарь-строк): статусы («В сети»/«Не в сети»/«Ожидание»/«Установка…»/«Ошибка»), «Аптайм»/«Обновлено», относительное время («только что»/«N мин назад»/«N ч назад»/«N дн назад»), единицы (`ГБ`, `ядра` с формами мн.ч.), кнопки, empty state, toast, ошибки. Технические идентификаторы API (`provision_status`, коды ошибок, `unit`) не локализуются — переводится только отображение.
10. **CPU detail — всегда число ядер** (`unit:"cores"`, `value:null`, `total`=ядра → UI «N ядер»); GHz не отображается. RAM/SSD — `used/total ГБ`. **На узких вьюпортах** (планшет-портрет ≤~823px, 2 колонки страницы) строка детали метрики с десятичными ГБ не помещается в под-карточку при `grid-cols-3` → значение **контейнерно усекается** (`overflow-hidden` на под-карточке метрики), без наезда на соседние метрики; на десктопе видно целиком ([TD-023](../../100-known-tech-debt.md), [08-design-system.md](../../08-design-system.md#под-карточки-метрик)).
11. **Detail-view по клику + inline-edit + drag-and-drop** ([ADR-035](../../adr/ADR-035-detail-view-secret-reveal.md)/[ADR-039](../../adr/ADR-039-ui-server-inline-edit-backends-search-empty-sms-label.md) амендмент [ADR-011](../../adr/ADR-011-poryadok-blokov-server-side-dnd-kit.md), состав — **[ADR-049](../../adr/ADR-049-servers-backends-card-first-detail.md) §1–§2**, [08-design-system.md](../../08-design-system.md#detail-view-карточных-страниц-read-only--карандаш--edit-adr-035)): **короткий клик по карточке → read-only `ServerDetailModal`**, где **сразу, без сворачивания, видны четыре строки — Название / IP / Пользователь (`ssh_user`) / Пароль** (`••••••••` + глаз-reveal под `servers:edit`). **Свёрнутый блок «Информация» в модалке сервера УПРАЗДНЁН, секция «Бэки» из модалки УБРАНА** ([ADR-049](../../adr/ADR-049-servers-backends-card-first-detail.md) §1 — разворот [ADR-046](../../adr/ADR-046-ui-infra-fix-pack.md) §2в). **Секция «Бэков: N» живёт ВНИЗУ `ServerCard`** (счётчик `backend_count` из `GET /api/servers`; ленивый `GET /api/servers/{id}/backends` **только по раскрытию**; `backend_count=0` → не раскрывается; **преднагрузка списков для сетки запрещена**). **Редактирование — по иконке `Pencil` вверху справа модалки: переключает `name` в inline-редактируемое поле ПРЯМО в detail-модалке** (Сохранить/Отмена, `PATCH /api/servers/{id} {name}`; карандаш виден под `servers:edit`), **без отдельной `AddServerModal`** ([ADR-039](../../adr/ADR-039-ui-server-inline-edit-backends-search-empty-sms-label.md)). Зажатие ~200 мс + движение → перетаскивание (@dnd-kit `PointerSensor` delay 200 мс, tolerance 5 px), вся карточка — хват. **DnD-механика не меняется.** Порядок сохраняется `PATCH /api/servers/order`. Кнопка **Удалить** **и триггер секции «Бэков: N»** не инициируют drag/detail/edit (`stopPropagation`; триггер **не является drag-ручкой** — [ADR-049](../../adr/ADR-049-servers-backends-card-first-detail.md) §2). Клавиатурная перестановка — опциональна ([TD-022](../../100-known-tech-debt.md)); detail/inline-edit/delete/раскрытие секции доступны с клавиатуры. Список рисуется в порядке `position` из `GET /api/servers`.
12. **Дефолт темы — СВЕТЛАЯ + сессия в `localStorage`** ([ADR-041](../../adr/ADR-041-login-theme-session-ux.md)): (а) при отсутствии `localStorage['crm-theme']` активна светлая тема (включая экран `/login`), no-FOUC inline-скрипт `localStorage.getItem('crm-theme') || 'light'`, подписки на `prefers-color-scheme` нет ([08-design-system.md](../../08-design-system.md#темизация-светлаятёмная-adr-033)); (б) плейсхолдер поля входа — **«Логин или @username»** (Telegram-ник принимается с/без `@`); (в) access-JWT и `crm.auth.*` в **`localStorage`** (не `sessionStorage`) → сессия переживает закрытие браузера (фикс 12) и шарится между вкладками (фикс 16); **auth-стор регидрируется из `localStorage` синхронно при загрузке SPA ДО резолва маршрута**, поэтому `ProtectedRoute` не редиректит на `/login` в новой вкладке; автовыход — по истечении TTL JWT (24 ч). Security-трейдофф localStorage — [05-security.md](../../05-security.md#jwt). **Уточнено [ADR-046](../../adr/ADR-046-ui-infra-fix-pack.md) §4 (фикс бага «на проде всегда тёмная»):** no-FOUC-скрипт — **НЕ inline**, а отдельный статический файл (`/theme-init.js`), т.к. прод-CSP `script-src 'self'` inline не исполняет (`'unsafe-inline'` **не вводится**); **светлые токены — на голом `:root`**, тёмные — под `[data-theme='dark']` (отсутствие атрибута = светлая, не тёмная); `useTheme()` самолечится, если атрибут не проставлен.

## DoD
- [ ] Визуальное соответствие референсу и enterprise-уровню ([08-design-system.md](../../08-design-system.md)).
- [ ] Все состояния UI реализованы.
- [ ] Цвет дуги строго по зоне нагрузки.
- [ ] Unit/компонентные + E2E тесты ([06-testing-strategy.md](../../06-testing-strategy.md)) зелёные; coverage ≥70 % (Gauge/зоны ≥90 %).
- [ ] lint/typecheck/format/build проходят.

## Changelog
- 2026-06-28: спецификация создана (architect, bootstrap).
- 2026-06-29: UI-изменения — сетка до 3 колонок; gauge без `%`/«Usage»/меток `0-100%`; CPU detail всегда «ядра»; локализация UI на русский (словарь строк).
- 2026-06-30: удалена drill-down ссылка на Grafana из карточки + конфиг `VITE_GRAFANA_URL` ([ADR-005, поправка](../../adr/ADR-005-custom-gauge-vs-grafana-embed.md#поправка-2026-06-30--удаление-drill-down-ссылки-из-карточки)).
- 2026-07-01: редактирование сервера по клику (`AddServerModal` режим edit, `PATCH name`) + drag-and-drop перестановка карточек (@dnd-kit, `PATCH /api/servers/order`); @dnd-kit добавлен в стек ([ADR-011](../../adr/ADR-011-poryadok-blokov-server-side-dnd-kit.md)).
- ~~2026-07-01: деталь метрики с десятичными ГБ на узких вьюпортах контейнерно усекается (`overflow-hidden` на под-карточке)~~ — **отменено 2026-07-13** (см. ниже).
- 2026-07-06: **Разделение скролл-контейнера и контейнера ширины для не-mail маршрутов** (architect; docs). Фикс двух связанных багов `AppLayout` с одним корнем (не-mail `<main>` был и центрированным max-w-контейнером, и скролл-контейнером): «панель скролла» посреди `/servers` и разная ширина `/servers`↔`/ai-keys` из-за скроллбара, съедавшего ширину max-w-контейнера. Теперь не-mail `<main>` — полноширинный `overflow-y-auto` (скролл у края окна), ширину `mx-auto max-w-[1400px] px-6 py-8` держит внутренний `<div>` вокруг `<Outlet/>` (scroll-independent). `/mail` full-bleed без изменений. Спецификация — [08-design-system.md «Full-bleed layout»](../../08-design-system.md#full-bleed-layout-нормативно); правка `AppLayout.test.tsx` — для qa (assert'ы классов ширины переносятся с `<main>` на внутренний `<div>`).
- 2026-07-06: **Фикс контейнерного/фантомного скролла `<main>` на не-mail** (architect; docs). Диагноз по **фактическому рендеру** (headless, `scrollHeight/clientHeight`): shell форсировал `h-screen`/`overflow-hidden` для всех маршрутов, не-mail `<main>` был `overflow-y-auto` → контейнерный скроллбар внутри `<main>` на реальных высотах окна (1536×730: `scrollH=764>clientH=668`). Фикс — **два режима shell по маршруту**: `/mail` — прежний `h-screen` flex-column (без изменений); **не-mail** — **обычный поток документа** (shell `flex min-h-screen flex-col`, header `sticky top-0 z-30`, `<main>` — простой контейнер без `flex-1`/`min-h-0`/`overflow-y-auto`, ширину держит внутренний `<div className="mx-auto max-w-[1400px] px-6 py-8">`). `/servers`/`/ai-keys` — нативный body-скролл (или его отсутствие, если влезает), скроллбар не прячется через `overflow:hidden`. Спецификация — [08-design-system.md «Full-bleed layout»](../../08-design-system.md#full-bleed-layout-нормативно); правка `AppLayout.test.tsx` (assert'ы по режимам) — для qa.
- 2026-07-13: **Обрезка числовых значений детали метрики устранена; норма в docs исправлена** (frontend + architect). Значение (`786.3/913.8 ГБ`) обязано читаться **полностью** на всех штатных вьюпортах; `overflow-hidden` на под-карточке и `whitespace-nowrap` на строке значения **сняты**, переполнение решено **размером/раскладкой** (`px-1 … xl:px-2.5`; `grid-cols-3 gap-2 xl:gap-3`), крайний случай — перенос, не обрезка. Формат и `grid-cols-3` не менялись; десктоп не затронут. Проверено фактическим рендером (390/768/1279/1280/1440, обе темы). **[TD-023](../../100-known-tech-debt.md) закрыт**; норма — [08-design-system.md §Под-карточки метрик](../../08-design-system.md#под-карточки-метрик).
