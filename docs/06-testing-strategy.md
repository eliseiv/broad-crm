# 06 · Стратегия тестирования

## Пирамида тестов

```
        /\
       /E2E\        немного: критичные пути (вход → список → добавление)
      /------\
     /  Интег \      API + БД + замоканные Prometheus/Ansible
    /----------\
   /   Unit      \   много: services, crypto, PromQL-маппинг, зоны, gauge-математика
  /--------------\
```

## Coverage gate

- Backend: строки ≥ **80 %** (`pytest --cov=app`), критичные модули (`auth`, `crypto`, `monitoring` маппинг, зоны, `notifier` функция перехода/классификации сообщений) — ≥ **90 %**.
- Frontend: строки ≥ **70 %** (Vitest), компоненты `Gauge`, логика зон — ≥ **90 %**.
- **В CI на Этапе 1 — только backend-гейты** (`ruff`/`mypy`/`pytest --cov-fail-under=80`); проверяются в GitHub Actions ([07-deployment.md §CI/CD](07-deployment.md#cicd)), падение ниже порога — красный билд.
- **Frontend вне CI на Этапе 1** (решение пользователя): линт/typecheck/сборка frontend в CI **не выполняются** — SPA собирается на прод-сервере в `docker compose build` (proxy-образ), что само по себе валидирует сборку. **Эта валидация надёжна именно из-за чистоты контекста:** прод-сборка `tsc -b && vite build` идёт без кеша `tsBuildInfo`, тогда как локальный инкрементальный `tsBuildInfoFile` (`node_modules/.tmp/*.tsbuildinfo`) **маскирует** ошибки состава composite-проекта TypeScript (в частности **TS6307** — файл вне файлового списка проекта). Эталон проверки type-сборки — чистая сборка без кеша (удалить `*.tsbuildinfo` перед `tsc -b` либо Docker-сборка); прецедент — [ADR-066](adr/ADR-066-tailwind-config-in-app-tsproject.md). Тестовые гейты frontend (`vitest` ≥70 %, Gauge/зоны ≥90 %, Playwright e2e) — целевые пороги, но на Этапе 1 **не подключены к CI** ([TD-015](100-known-tech-debt.md)); их добавляет `qa` и возвращает в CI на этапе тестирования.
- Frontend-пороги (≥70 %, Gauge/зоны ≥90 %) остаются нормативной целью качества; применяются, когда тест-стек frontend появится в кодовой базе.

## Backend (pytest + pytest-asyncio + pytest-cov)

### Unit
- **auth**: генерация/валидация JWT (срок, подпись, невалидный токен), constant-time сравнение, rate-limit счётчик.
- **crypto**: Fernet encrypt→decrypt round-trip; неверный ключ → ошибка; ciphertext ≠ plaintext.
- **monitoring**: маппинг ответов Prometheus → схема метрик; вычисление `zone` на границах (79.9→green, 80→yellow, 90→yellow, 90.1→red); деградация при пустом/ошибочном ответе.
- **provisioning**: переходы `provision_status` (pending→installing→online/error); формирование `targets/<id>.json`; имена/санитизация.
- **notifier** (≥90 %, чистая функция перехода): матрица эскалаций — `green→yellow`/`green→red`/`yellow→red` → алерт; `red→yellow`/`*→green` и `offline→online` → молча; `online→offline` → offline-алерт; первая встреча online → база без алерта; возврат `offline→online` в нагрузке → переалерт (база `green`); `online` без метрик → пропуск без алерта; `PrometheusUnavailable` → state не тронут; формат всех 3 сообщений и `int(usage_percent)` (floor); опциональность (нет токена → задача не стартует). См. [modules/notifier](modules/notifier/README.md).
- **`is_admin_level`** (чистая, `app/domain/permissions.py`): супер-админ → true; `role=="admin"` → true; полный каталог (включая extra-действия и `broadcast`) → true; кириллическое «Админ» с неполным каталогом (нет `documents.share` или нет `broadcast`) → false; урезанная роль → false.
- **`GET /api/auth/me` += `is_admin_level` ([ADR-078](adr/ADR-078-me-is-admin-level.md)):** супер-админ → `true`; сид `admin` → `true`; роль «Админ» с `full_catalog_permissions()` → `true`; та же роль без `documents.share` или без `broadcast` → `false`; урезанная роль → `false`. Поле обязательное, `bool`.

### Интеграционные
- API + тестовая PostgreSQL (testcontainers или отдельная схема). Prometheus и Ansible — **замоканы** (httpx mock / fake runner).
- Сценарии:
  - `POST /api/auth/login` верные/неверные креды → 200/401; rate-limit → 429.
  - Защищённый эндпоинт без токена → 401.
  - `POST /api/servers` → 202, запись в БД (пароль зашифрован, plaintext отсутствует), запущена фоновая задача; дубликат `ip` → 409; невалидный IP → 422.
  - `GET /api/servers` с замоканным Prometheus → корректные метрики/зоны; при недоступном Prometheus → 200 с `metrics=null`.
  - `GET /api/servers/{id}/metrics` при **недоступном самом Prometheus** (соединение/таймаут) → 502.
  - `GET /api/servers/{id}/metrics` при **доступном Prometheus, но `up==0` ИЛИ отсутствии метрики** → 200 с `online=false`, `cpu`/`ram`/`ssd` `detail.value=null` (`usage_percent`/`detail` не подставляются ложными значениями), **без 502** (см. [04-api.md](04-api.md#доступность-метрик-up0--отсутствие-данных-vs-prometheus-down), согласовано с [modules/monitoring/README.md](modules/monitoring/README.md) п.6).
  - `GET /api/servers/{id}/status` отражает текущий статус.
  - `DELETE` → 204, запись удалена, `targets/<id>.json` удалён; повтор → 404.
  - **Серверы — вход по SSH-ключу ([ADR-067](adr/ADR-067-server-ssh-key-auth.md)).** Каждый кейс — отдельный тест; ключи для фикстур **генерируются в рантайме** через `cryptography` (в репозиторий приватные ключи не коммитить даже тестовые).
    - `POST /api/servers` **без** `auth_method` со `ssh_password` → `202`, в БД `auth_method='password'` (обратная совместимость старого тела — регресс-гейт).
    - `auth_method='key'` + валидный незашифрованный ключ → `202`; в БД `ssh_private_key_encrypted` **не пуст**, `ssh_password_encrypted IS NULL`, plaintext ключа в строке отсутствует.
      - **Отдельным кейсом — PEM-armor без `Proc-Type` (регресс-гейт шага 1, [ADR-067](adr/ADR-067-server-ssh-key-auth.md) §3 п.4):** незашифрованные `-----BEGIN RSA PRIVATE KEY-----` (PKCS#1, `ssh-keygen -m PEM`) и `-----BEGIN PRIVATE KEY-----` (PKCS#8) **без** passphrase → `202`, `is_encrypted=false`. Самый частый формат ввода; реализация, где catch-all ловит его раньше PEM-ветки, отдаст `422` — тест обязан это ловить.
    - `auth_method='key'` + ключ **с** passphrase + верная фраза → `202`; неверная фраза → `422`, `details[].field == "ssh_key_passphrase"`; фраза не передана → `422`, тот же `field`; фраза передана к **незашифрованному** ключу → `422`, тот же `field`. ⚠️ Последние два исхода **обязаны обеспечиваться шагом 2 процедуры** (структурная кросс-проверка `is_encrypted` ↔ наличие фразы), а не поведением `cryptography`: на 43.x лишний пароль к незашифрованному ключу может быть **молча проигнорирован** — тест на этом кейсе и есть гейт, что шаг 2 реализован.
    - **Типы ключей — по кейсу:** RSA-2048/4096, ECDSA P-256, **Ed25519** → `202` (и провижининг-мок получает `ansible_ssh_private_key_file`); **DSA-2048 → `422`, `field="ssh_private_key"`** (отвергается шагом 4 **на валидации**; без него ключ прошёл бы форму и упал уже на провижининге — регресс-гейт).
    - Оба материала сразу (`ssh_password` + `ssh_private_key`) → `422` с `field` **лишнего** поля; ни одного → `422` с `field` **недостающего**.
    - Мусор вместо ключа / **публичный** ключ (`ssh-rsa AAAA…`) / ключ с испорченной base64-серединой (заголовки целы!) → `422`, `field == "ssh_private_key"`. Последний кейс — гейт против «валидации regex по BEGIN/END».
    - Ключ длиннее `SSH_KEY_MAX_BYTES` → `422` (проверка **до** разбора).
    - **Ключ с `\r\n` и без завершающего `\n`** → `202`, а в БД после расшифровки — нормализованная форма (LF + завершающий `\n`).
    - **Ответ и логи не содержат материала:** тело `202` — только `auth_method`, plaintext ключа/фразы отсутствует в ответе **и** в перехваченных structlog-записях (включая ветки `422`: текст исключения `cryptography` наружу не идёт).
    - `GET /api/servers/{id}/ssh-password` для key-сервера → **`404 secret_not_set`**; для password-сервера → `200`.
    - **Эндпоинтов `/ssh-key` и `/ssh-key-passphrase` не существует** → `404` (контракт-гейт против «доброжелательного» добавления reveal в будущем).
    - `PATCH /api/servers/{id}` с полями способа входа игнорирует их / отвергает: `auth_method` в БД не меняется.
    - **CHECK БД работает:** прямой `UPDATE` строки в состояние «пароль + ключ» / «key без ключа» → `IntegrityError` (проверка, что граница живёт в БД, а не только в сервисе).
  - **Документы — вложения ([ADR-068](adr/ADR-068-documents-image-attachments.md)).** Каталог хранения — временный (`DOCUMENTS_ATTACHMENTS_DIR` в tmp_path).
    - Загрузка валидного png/jpeg/webp/gif → `201`; на диске появился файл по пути **из `id`+`mime`**, права `0600`, `checksum` в ответе = sha256 содержимого.
    - **Подделка типа:** SVG/HTML/ELF с `Content-Type: image/png` → **`422 document_attachment_invalid`** (гейт против доверия клиентскому `Content-Type`). Отдельный кейс — честный `image/svg+xml` → тот же `422`.
    - Превышение `DOCUMENTS_MAX_IMAGE_BYTES` → `422`; проверяется, что чтение **оборвано** (файл на диске не создан).
    - Загрузка в **папку** → `422 validation_error`; в невидимый по роли узел → `404 document_node_not_found`.
    - **Отдача под чужой ролью:** пользователь без видимости узла → **`404 document_attachment_not_found`** (не `403` и не `200`) — ключевой security-кейс. Тот же код для несуществующего `id` и для вложения soft-deleted узла (**три случая — один код**).
    - Заголовки отдачи: `Content-Type` = сохранённый `mime`; `ETag` = `checksum`; повтор с `If-None-Match` → `304`; **`Cache-Control` содержит `private` и НЕ содержит `public`** (регресс-гейт shared-кэша).
    - `POST /nodes/{id}/copy` документа с двумя картинками → у копии **новые** `id` вложений, **новые** файлы на диске, и в `content_md` копии ссылки указывают на новые `id` (старых `uuid` в тексте не осталось).
    - Soft-delete узла → файлы на диске **на месте**, отдача → `404`.
    - `DELETE /attachments/{id}` → `204`, строка и файл удалены; повтор → `404`.
    - **Внешний контур не отдаёт байты:** `GET /api/external/documents/attachments/{id}` → `404` (роут не зарегистрирован); `GET /api/external/documents/{id}` возвращает `content_md` со ссылками **как есть** и **без** поля `attachments`.
    - **Удаление автора → `409 user_in_use`, НЕ `500`** ([04-api.md](04-api.md#delete-apiusersid), [TD-077](100-known-tech-debt.md)): пользователь, загрузивший вложение (и отдельным кейсом — создавший документ), `DELETE /api/users/{id}` → `409 user_in_use`; **лидерство его команд не передано** (транзакция откачена целиком); узел в состоянии soft-delete блокирует так же, как живой. **Покрыто** (`backend/tests/integration/test_users_delete_in_use_api.py`, 2026-07-23): 17 кейсов, включая гейт по `pg_constraint` «FK **не** `DEFERRABLE`» — при отложенном констрейнте `RESTRICT` сработал бы на `commit`, перехват прошёл бы мимо и вернулся бы `500`.
  - **Mail — таймауты и причина отказа ([ADR-053](adr/ADR-053-mail-timeouts-error-passthrough.md), регресс прод-бага; агрегатор замокан `httpx`-mock'ом).** Кейсы **поэлементно**, каждый — отдельный тест.
    - **⚠️ Суб-секундные бюджеты инъектируются В КЛИЕНТА, а НЕ в `Settings` — нормативно.** Прод-числа (read 75 / overall 85 / 30) в CI **не используются**: задержка мока в десятки секунд сделала бы прогон минутным. Но **через `Settings` их подменить нельзя** — точка инъекции только одна, клиентская:
      - **Почему не `Settings`.** `Settings` — `pydantic_settings.BaseSettings` ([`config.py:15,42`](../backend/app/config.py)), и границы `Field(ge=…, le=…)` валидируются **и при явном конструировании** (kwargs проходят через тот же валидатор, что и env) — «обойти `ge/le` явным конструированием `Settings`» **невозможно**. Вдобавок все четыре поля таймаутов — **`int`** (`mail_api_timeout_sec: int = Field(default=10, ge=1, le=30)` — [`config.py:157`](../backend/app/config.py); `mail_api_deadline_sec` — [`:159`](../backend/app/config.py); `mail_api_mailserver_timeout_sec: … ge=61, le=80` — [`:163`](../backend/app/config.py); `mail_api_mailserver_deadline_sec: … ge=76, le=85` — [`:166`](../backend/app/config.py)), т.е. `0.3` не проходит и **по типу**. А на **легальном** конфиге mail-server-пары (`ge=61` / `ge=76`) кейсы «агрегатор молчит дольше read-бюджета» и «исчерпание overall-deadline» потребовали бы **реального ожидания 61+ с** — ровно того многосекундного sleep, который здесь и запрещается.
      - **Реальная точка инъекции — конструктор клиента.** Бюджеты `MailClient` приходят **параметрами конструктора** — `read_timeout_sec: float` и `deadline_sec: float` ([`mail_client.py:142-148`](../backend/app/infra/mail_client.py)), а не читаются из `Settings` внутри; суб-секундные значения там легальны. Поэтому:
        - **Интеграционные кейсы:** через `app.dependency_overrides` подменяется **`get_mail_service`** ([`deps.py:402`](../backend/app/api/deps.py), зарегистрирован как `MailServiceDep = Annotated[MailService, Depends(get_mail_service)]` — [`deps.py:458`](../backend/app/api/deps.py)) — override возвращает `MailService(session=…, client=…, settings=…, mail_server_client=…)` ([`mail_service.py:196-202`](../backend/app/services/mail_service.py)) с **обоими** клиентами, собранными на **суб-секундных** бюджетах (напр. read `0.3` с, overall `0.5` с; компенсирующая уборка — ещё меньшим). ⚠️ **Подменять в `dependency_overrides` сами фабрики `get_mail_client`/`get_mail_server_client` — НЕ работает**: `get_mail_service` вызывает их **прямым вызовом** ([`deps.py:411,413`](../backend/app/api/deps.py)), а не через `Depends`, а `dependency_overrides` перехватывает только то, что FastAPI резолвит как `Depends`. (Если провязку когда-нибудь переведут на `Depends(get_mail_client)` / `Depends(get_mail_server_client)`, override фабрик станет равноправной точкой — но по действующему коду нормативна подмена `get_mail_service`.)
        - **Unit-кейсы транспорта:** `MailClient` конструируется **напрямую** с суб-секундными `read_timeout_sec`/`deadline_sec` ([`mail_client.py:142-148`](../backend/app/infra/mail_client.py)) — `Settings` не участвует вовсе.
      - «Задержка > бюджета» моделируется **относительно инъектированного** значения (мок отвечает через `2× бюджет`). Проверяется **поведение** — `error.code` фактического ответа и число исходящих запросов, — а **не абсолютные секунды**.
      - **`ge/le` прод-конфига в тестах НЕ обходятся и обходить их не нужно:** они защищают прод-конфиг и **сами являются предметом проверки** в кейсах §«Инвариант конфига» ниже, где `Settings` конструируется обычным конструктором с невалидными числами **именно ради `ValidationError`**.
    - `POST /api/mail/mailboxes/test`, агрегатор отвечает `422 {"error":{"code":"imap_login_failed"}}` **с задержкой больше read-бюджета** (моделирует прод-случай «ответ через ~31 с при бюджете 10 с») → CRM отдаёт **`422` с `error.code == "mail_imap_failed"`** (проверять именно поле `error.code` ответа, **не** текст `message`), **а не `502`**. Это прямой регресс прод-бага. Ключевое: read-бюджет mail-server-клиента **больше** задержки мока, поэтому ответ доходит.
    - То же с `smtp_login_failed` → `422 mail_smtp_failed`; с `invalid_host` → `422 mail_invalid_host`; с нераспознанным кодом → `422 unprocessable` (fallback).
    - Агрегатор «молчит» дольше **read-бюджета инъектированного mail-server-клиента** (в тесте — суб-секундного; прод-норма — `MAIL_API_MAILSERVER_TIMEOUT_SEC`) → **`504` с `error.code == "mail_timeout"`**, **ретрая нет** (mock фиксирует **ровно 1** исходящий запрос — анти-двойная-запись).
    - **Overall-deadline инъектированного клиента** (в тесте — суб-секундный; прод-норма — `MAIL_API_MAILSERVER_DEADLINE_SEC`) **— отдельный кейс:** мок «зависает» так, что суммарное время (в т.ч. ретраи `ConnectError` + backoff) превышает deadline → **`504 mail_timeout`**, ответ отдан **в пределах deadline** (проверяется, что обработчик вернулся раньше, чем истёк бы per-phase read) — граница запроса гарантирована, а не только фазовая.
    - Агрегатор вернул `504` → `504 mail_timeout` (**и на быстром пути тоже** — `DELETE`/`sync`/`oauth-authorize`, [ADR-053](adr/ADR-053-mail-timeouts-error-passthrough.md) §2.1); вернул `503`/`429` → `502 mail_unavailable`; `ConnectError` → 3 попытки → `502 mail_unavailable`.
    - **Быстрый путь, собственный таймаут CRM** (мок молчит дольше бюджетов инъектированного **быстрого** клиента — в тесте суб-секундных; прод-норма — `MAIL_API_TIMEOUT_SEC`/`MAIL_API_DEADLINE_SEC`) → **`502 mail_unavailable`**, **не** `504` (§2.1).
    - `POST /api/mail/mailboxes` и `PATCH /api/mail/mailboxes/{id}` (смена кредов) — те же 422-коды и `504 mail_timeout` (они тоже mail-server-пути).
    - `POST /api/mail/messages/{id}/reply`: агрегатор `502 {"error":{"code":"smtp_failed"}}` → **`502 mail_send_failed`** (не `mail_unavailable`) — проверяет, что транспорт донёс `status_code`+`error_code` из **5xx**-ветки, а не схлопнул её.
    - **Источник таймаута различим (unit транспорта, [ADR-053](adr/ADR-053-mail-timeouts-error-passthrough.md) §1.3 п.2):** агрегатор ответил `504` → поднят `MailTimeout` с **`status_code == 504`**; агрегатор молчит дольше read-бюджета / исчерпан overall-deadline → `MailTimeout` с **`status_code is None`**. Без этого поля кейсы «`504` от агрегатора на быстром пути → `504`» и «собственный таймаут на быстром пути → `502`» взаимно неисполнимы.
    - **Компенсирующая уборка сироты (`POST /api/mail/mailboxes`, [ADR-053](adr/ADR-053-mail-timeouts-error-passthrough.md) §1.2.2) — поэлементно:**
      - агрегатор создал ящик (`201`), вставка в каталог CRM падает → мок фиксирует **второй** исходящий вызов `DELETE /api/external/mailboxes/{id}`, клиенту уходит **исходная** ошибка вставки (не ошибка уборки);
      - компенсирующий `DELETE` сам **таймаутит** (мок молчит) → ответ клиенту — по-прежнему **исходная** ошибка (`MailTimeout` **не** пролетает наружу и не подменяет её), в логе — `mail_create_orphan_cleanup_failed`;
      - компенсирующий `DELETE` укладывается в **свой короткий deadline** (переопределённый малым значением, как в ⚠️ выше), **меньший**, чем deadline быстрой категории — уборка не съедает полный бюджет.
    - **Сборка клиента (unit):** mail-server-клиент собран с `MAIL_API_MAILSERVER_TIMEOUT_SEC`+`MAIL_API_MAILSERVER_DEADLINE_SEC`, быстрый — с `MAIL_API_TIMEOUT_SEC`+`MAIL_API_DEADLINE_SEC`; таймаут задан как `httpx.Timeout` **по фазам** (`connect` 5 / `write` 10 / `pool` 5 / `read` = бюджет), не одиночным float; весь вызов обёрнут `asyncio.wait_for`; **пер-вызовный override overall-deadline** применяется (компенсация).
    - **Инвариант конфига (unit, [ADR-053](adr/ADR-053-mail-timeouts-error-passthrough.md) §1.3 п.7) — поэлементно, `ge/le` и кросс-полевой валидатор проверяются ОТДЕЛЬНО:**
      - `ge/le`: `MAIL_API_MAILSERVER_TIMEOUT_SEC=60` (≤ потолка агрегатора) → **ValidationError**; `MAIL_API_MAILSERVER_DEADLINE_SEC=86` (> `le=85`) → **ValidationError**; **быстрая пара**: `MAIL_API_DEADLINE_SEC=120` (> `le=60`) → **ValidationError**, `MAIL_API_TIMEOUT_SEC=31` (> `le=30`) → **ValidationError**;
      - **кросс-полевой** (`model_validator`) — случай, который `ge/le` пропускают: `MAIL_API_MAILSERVER_TIMEOUT_SEC=80` **и** `MAIL_API_MAILSERVER_DEADLINE_SEC=76` (**обе** границы соблюдены, но read > overall) → **ValidationError**; то же для быстрой пары (`MAIL_API_TIMEOUT_SEC=30`, `MAIL_API_DEADLINE_SEC=10`) → **ValidationError**;
      - **бюджет запроса**: конфиг, при котором `MAIL_API_MAILSERVER_DEADLINE_SEC + MAIL_CLEANUP_DEADLINE_SEC + CRM_REQUEST_OVERHEAD_SEC ≥ NGINX_PROXY_READ_TIMEOUT_SEC` (т.е. `… + 15 + 5 ≥ 120`) → **ValidationError**; валидный дефолт (`85 + 15 + 5 = 105 < 120`) → `Settings` собирается;
      - **единственный источник константы уборки** ([ADR-053](adr/ADR-053-mail-timeouts-error-passthrough.md) §1.2.2/§1.3 п.7б): deadline, фактически применяемый компенсирующей уборкой, **равен `config.MAIL_CLEANUP_DEADLINE_SEC`**. Кейс — **спай на клиенте**: при провале вставки уборка зовёт `delete_mailbox(mailbox_id, deadline_sec=…)` ([`mail_service.py:972`](../backend/app/services/mail_service.py)), и тест ассертит, что переданный `deadline_sec` **идентичен** импортированной из `config.py` константе (а не «равен 15» — сверять с литералом бессмысленно, литерал в `mail_service.py` [запрещён](adr/ADR-053-mail-timeouts-error-passthrough.md#121-компенсирующая-уборка--отдельный-короткий-deadline)). ⚠️ **Патчить `config.MAIL_CLEANUP_DEADLINE_SEC` бесполезно** — `mail_service.py` связывает имя на импорте (`from app.config import MAIL_CLEANUP_DEADLINE_SEC`, [`mail_service.py:30`](../backend/app/services/mail_service.py)); если подмена значения всё же нужна, патчить следует `app.services.mail_service.MAIL_CLEANUP_DEADLINE_SEC`. Без этого кейса кросс-полевой валидатор стерёг бы **своё** зеркало, пока фактический deadline уборки разошёлся бы с ним, — «машинная защита» стала бы ложной.

- **Per-channel scope команд ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md)) — поэлементно** (по каждому каналу отдельно; «работает почта» **не засчитывается** за СМС и наоборот):
  - **union-семантика:** пользователь в команде A + доп-команда B (`user_channel_teams`) → лента/каталог отдают объекты **обеих**; после `PATCH /api/users/{id}` с `team_ids` **без** A команда A исчезает из scope **обоих** каналов (копии в добавке не остаётся — инвариант вычитания, [ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §2.3);
  - **вычитание базовой команды:** `POST`/`PATCH` с `mail_extra_team_ids`, содержащим команду из `team_ids`, → **не ошибка**, в `user_channel_teams` строка **не создаётся** (проверять по ответу `mail_extra_teams` и по БД);
  - **`includes_unassigned`:** без флага — бесхозный ящик/номер (`team_id IS NULL`) в чтении **отсутствует**, мутация → **`403`**; с флагом — виден и мутируется (`204`/`200`);
  - **не разворачивается:** `POST /api/mail/mailboxes` с `team_id=null` не-админом → **`403` даже при `mail_includes_unassigned=true`**; `PATCH` со сменой `team_id` не-админом (в т.ч. в свою доп-команду) → **`403`**;
  - **`no_team`:** `GET /api/mail/messages?no_team=true` и `GET /api/sms/messages?no_team=true` — только бесхозные; **`team_id` + `no_team=true` вместе → `400 validation_error`**; `no_team=true` без флага у не-админа → **пустая страница** (не `403`);
  - **`GET /api/auth/me`:** у не-админа `mail_teams`/`sms_teams` — **эффективный** набор (базовые ∪ добавка, сортировка по `name`); **у актора admin-уровня (`sees_all_<channel>_teams=true`) — ВСЕ команды системы** (**не** `[]` — [ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §5.1) и `*_includes_unassigned=true`;
  - **миграция `0027`:** `upgrade`/`downgrade` прогоняются; **без backfill** scope существующего пользователя не меняется (регрессионный кейс: до и после миграции лента одинакова);
  - **нормализация на пути teams CRUD (SECURITY-кейс, [ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §2.3 путь 2 — обязателен, отдельно от users-пути):** `X` получает доп-команду `B` (блок «Почты») → `PATCH /api/teams/{B}` с `member_ids`, включающим `X` (**строка `user_channel_teams (X, mail, B)` обязана исчезнуть**) → `PATCH /api/teams/{B}` с `member_ids` **без** `X` ⇒ у `X` **нет** доступа к почте команды `B` (лента пуста, мутация ящика `B` → `403`). Без нормализации — «висящий» доступ. Тот же кейс для `POST /api/teams` (создание команды сразу с участником, у которого эта команда — добавка) и для канала `sms`;
  - **перенос номера ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §3.2) — по кейсу на КАЖДУЮ проверку:** (а) номер вне scope → `403`; (б) не-админ, `team_id=null` **без** `sms_includes_unassigned` → **`403`**; (в) не-админ, `team_id=null` **с** флагом → `200`, номер остаётся видимым; (г) не-админ, целевая команда **вне** scope (существующая, но чужая) → **`403`**; (д) не-админ, целевая команда **несуществующая** → **`403`** (не `404` — анти-энумерация); (е) admin-уровень, несуществующая команда → **`404 sms_team_not_found`**; (ж) не-админ, целевая команда — его **доп-команда** → `200`;
  - **каталог ящиков ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §5.3):** `GET /api/mail/mailboxes` у не-админа с `mail_includes_unassigned=true` **содержит** бесхозные ящики (`team_id: null`), без флага — **не содержит**. Контракт (query/схема) при этом не менялся.
- **`MailAccountRef` ([ADR-056](adr/ADR-056-mail-mini-app-mailbox-context.md)):** `GET /api/mail/messages` отдаёт `mail_account.number`/`app_name`/`team`; ящик без команды → **`team: null`** (LEFT JOIN, письмо **не пропадает** из ленты); число SQL-запросов на страницу ленты **не растёт с числом писем** (нет N+1).

### Backend Users + Backend Economics ([ADR-069](adr/ADR-069-backend-users-page-admin-contract.md) / [ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md))

Сценарии ниже **заводили покрытие обоих модулей с нуля** — до волны [ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) собственных тестов у них не было. Фактическое покрытие на дату — **не здесь**, а в датированных домах ([modules/backend-economics §Состояние](modules/backend-economics/README.md#состояние-замер-на-замороженном-дереве-2026-08-07), [README §Статусы модулей](README.md#статусы-модулей)); этот раздел задаёт **что обязано быть покрыто**, а не что покрыто. Приоритет — **регресс-гейты**, каждый из которых стережёт конкретный уже осмысленный способ сломаться; «общий happy path» ни один из них не заменяет.

**Клиент бэков (`infra/backend_admin_client.py`) — по кейсу на КАЖДУЮ семантику 404** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §4):
- **известный** префикс + `404` на `PATCH /products/{id}` → **`backend_admin_extension_not_supported`**, и **НЕ** `backend_user_not_found` (гейт против копипаста метода с зашитой «пользовательской» семантикой);
- **известный** префикс + `404` на `GET /users/{id}` → **`backend_user_not_found`** (не сломано существующее поведение);
- **холодный кэш** (первый вызов процесса) + запрос **расширенного** пути (`GET /pricing`) на бэке, у которого контракт живёт по `/v1/admin`: префикс **определяется и кэшируется через v1-probe `GET /products`**, расширенный вызов уходит уже по определённому префиксу; **`backend_admin_not_supported` при этом НЕ возникает** (регресс-гейт: без разведения probe и расширения префикс не кэшировался бы вовсе);
- **холодный кэш + путь `/users/{id}` НЕСУЩЕСТВУЮЩЕГО пользователя** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §4а.1, закрытие латентного дефекта v1): probe `GET /products` проходит, префикс кэшируется, `404` от `/users/{id}` даёт **`backend_user_not_found`**, а **НЕ `backend_admin_not_supported`**. Обязательный кейс — до probe этот путь возвращал именно второе;
- **цена probe:** при холодном кэше на любой (в т.ч. v1) путь уходит **ровно два** upstream-запроса — probe `GET /products` и запрошенный (ассертить число и порядок вызовов); при тёплом — **один**;
- **`/products` недоступен, `/users` работает** → **`502 backend_admin_not_supported`** (страница «Юзеры бэков» блокируется осознанно: `/products` обязателен в v1 и является probe-путём);
- `409` от бэка → **`backend_admin_conflict`** (а не безликое «Ошибка бэка (HTTP 409)»);
- `400` от бэка на `PATCH` с неизвестным `product_id` → **`backend_admin_bad_request`** с текстом бэка (не `404`, не «расширение не поддерживается»);
- **`422` от бэка ⇒ ТОТ ЖЕ `backend_admin_bad_request`** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §7.3) — обязательный кейс на межрепозиторный стык: у контрагента отказ валидации тела (границы/точность) это **`422`**, а не `400`. ⛔ Негативный ассерт: ответ CRM **НЕ** `502` и в сообщении **НЕТ** строки «Ошибка бэка (HTTP 422)». Три подкейса по формату `detail`: (а) строка → транзитом; (б) **список** FastAPI (`[{loc,msg,type}]`) → в сообщении `msg` первого элемента (+ поле из `loc`); (в) формат неизвлекаем → нормативный фолбэк «Бэк отверг значение: не прошло проверку на стороне бэка»;
- **обязательность полей `capabilities` — по кейсу на КАЖДУЮ строку критерия** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §7.2): ответ `/capabilities` **без `cache_effective_after_seconds`**, **без `contract_version`** и **без `limits`** (три отдельных кейса) — **валиден**, даёт **непустой** `capabilities` (а НЕ `schema_mismatch` ⇒ `null`) и **карандаши остаются**; в кейсе без `limits` дополнительно: клиентская проверка границ не выполняется, `PATCH` **уходит**. ⛔ Обратный кейс — ответ **без `features`** ⇒ `capabilities: null` ⇒ страница read-only (**fail-closed, так и задумано**). Пара кейсов «`limits` нет — работаем / `features` нет — read-only» и есть проверка критерия: обязательность неиспользуемого или имеющего безобидное отсутствие поля молча превращает штатную деградацию в отказ функции;
- ответ `GET /products` с **> 500** элементами → **`backend_admin_unavailable`** (contract mismatch);
- **`X-Admin-Key` не попадает в логи** ни на успехе, ни на любой ветке ошибки; на `PATCH` уходит заголовок **`X-Admin-Actor`** формата `crm:<uuid>`.
- ⚠️ **Подменять фабрику сервиса, а не клиент:** клиент создаётся прямым вызовом внутри сервиса, поэтому `app.dependency_overrides` на сам клиент **не сработает** — override ставится на фабрику/зависимость, которая этот сервис отдаёт. Тест, «замокавший» клиент через `dependency_overrides`, зелёный молча и ничего не проверяет.

**API `/api/backend-economics/*` — RBAC поэлементно:**
- роль с `backend-users:["view","edit"]` и **без** `backend-economics` → **`403` на КАЖДОМ** новом пути (по кейсу на путь: `GET /backends`, `GET`/`PATCH /products`, `GET`/`PATCH /pricing`) — доказывает, что новый ключ **не алиас** `backend-users:edit`;
- `backend-economics:["view"]` без `edit` → `GET` **`200`**, `PATCH` **`403`**;
- `GET /backends` доступен держателю `backend-economics:view` **без** `backends:view` (селектор не зависит от чужого права);
- **`backend_user_not_found` недостижим** с этого роутера — ни один путь его не порождает (кейс: `404` от бэка на расширенном пути даёт `backend_admin_extension_not_supported`).

**Аудит:**
- успешный `PATCH` продукта → событие `backend_admin_action` с действием **`product_tokens_updated`** и **дельтой** (`1000->1500`) из `previous_tokens`; для тарифа — **`pricing_updated`**;
- **толерантность ШИРЕ трёх полей ([ADR-073](adr/ADR-073-products-archive-and-price-columns.md) §8 п.3):** `200` с телом-**массивом** и `200` с **не-JSON** — два отдельных кейса на каждом `PATCH`-эндпоинте: аудит (`product_archived_updated` / `pricing_updated`) **записан** (⛔ обязательный ассерт: событий НЕ ноль), исключение разбора **не опережает** запись. Кейсы обязательны отдельно от «без трёх полей»: опциональность полей закрывает схему, но не ветки разбора до неё;
- **толерантность ответа `PATCH` — на ОБОИХ эндпоинтах** ([ADR-073](adr/ADR-073-products-archive-and-price-columns.md) §8 п.4). Для **`PATCH …/pricing/{tariff_id}`** — свой кейс: ответ `200` без `previous_tokens`/`changed`/`effective_after_seconds` ⇒ CRM отвечает **`200`** (⛔ не `502`) **И аудит `pricing_updated` записан**. Кейс обязателен отдельно от продуктовых: реализовать толерантность только у продукта — ровно то расхождение, из-за которого норма и уточнялась;
- **толерантность ответа `PATCH …/products/{product_id}` — ЧЕТЫРЕ кейса, все на archived-only правке:** ответ `200` **без `previous_tokens`**, **без `changed`**, **без `effective_after_seconds`** и **без всех трёх сразу** ⇒ CRM отвечает **`200`** (⛔ негативный ассерт: **не `502`**) **И аудит `product_archived_updated` записан** — второй ассерт обязателен, потому что прежде исключение поднималось **до** записи аудита, и дефект выглядел бы как «архив переключён, следа нет»;
- **архив ([ADR-073](adr/ADR-073-products-archive-and-price-columns.md)):** `PATCH` с одним `archived` валиден (поле значимое, `400 validation_error` **не** возникает); эндпоинт `GET …/products` возвращает **и архивные** (ассертить наличие элемента с `archived: true` в ответе CRM — фильтрация не серверная); ответ бэка **без** поля `archived` валиден и не даёт `502`; аудит пишется действием **`product_archived_updated`** с деталью `archived=<new>` и **НЕ** `product_tokens_updated` (негативный ассерт);
- **`detail` называет ИЗМЕНЁННОЕ поле** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §10) — три кейса: тело только с `tokens` → в `detail` есть `tokens=<prev>-><new>` и **НЕТ** `avatar_tokens`; тело только с `avatar_tokens` → есть `avatar_tokens=<new>` и **⛔ НЕТ `tokens=…`** (обязательный негативный ассерт: запись `tokens=1000->1000` при неизменённом `tokens` = дефект); тело с обеими → **обе** части;
- **отказ бэка (любой не-2xx) ⇒ аудит НЕ пишется** (обязательный кейс: лог не должен утверждать об изменении, которого не было);
- в событии **нет** значения admin-ключа.

**Capabilities и деградация — по кейсу на КАЖДЫЙ исход подзапроса `/capabilities`** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §7.1; «`null` = фич не подтверждено», а не только «404»):
- **`404`**, **таймаут**, **транспортный сбой без ответа** (DNS/отказ соединения/TLS), **`3xx`-редирект** (клиент ходит с `follow_redirects=False`), **`401`/`403`**, **`400`/`409`**, **`500`**, **невалидный JSON**, **JSON разобран, но не по схеме** — **ДЕВЯТЬ отдельных кейсов, по одному на КАЖДУЮ строку закрытого перечня `reason`** (перечень — [ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §7.1; кейсов ровно столько, сколько строк, — это и есть проверка полноты), и в КАЖДОМ: ответ CRM — **`200`**, `capabilities: **null**`, **`items` отдан целиком**. Провал всего списка в `502` из-за необязательного подзапроса = дефект (обязательный негативный ассерт: код ответа **не** `502`);
- в каждом из этих кейсов причина неуспеха **попадает в лог ИМЕНОВАННЫМ событием** — ассертить **`backend_admin_capabilities_unavailable`** и поля `backend_id` + `reason` (`not_found` / `timeout` / `transport` / **`redirect`** / `rejected` / `http_4xx` / `http_5xx` / `bad_json` / `schema_mismatch` — **по кейсу своё значение, все девять различны**; ассерт «`reason` есть» без сверки значения **не засчитывается**: именно подмена значения ближайшим и была дефектом), **а НЕ свободный текст сообщения**: текстовый ассерт ломается от любой переформулировки и вырождается в «что-то залогировано» ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §7.1). Отдельно ассертить, что **значение `X-Admin-Key` в событие не попало**;
- `features` **без** `products.write_tokens` → отражено в ответе; проверка, что признак записи **выводится из `features`, а не из наличия поля `tokens`**;
- **`limits` ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §7.2 — дом нормы; собственного номера инварианта у `limits` нет) — заморожены ИМЕНА КЛЮЧЕЙ И ТИПЫ, но НЕ значения:** ответ с полным набором (`product_tokens_max`, `product_avatar_tokens_max`, `tariff_tokens_max`, `tariff_decimal_places`) транслируется **побуквенно** (переименование/схлопывание ключей = дефект); ответ **без части ключей** и **без `limits` целиком** валидны и не дают `502`; **незнакомый ключ игнорируется** (не `502`). ⛔ **Ассертить конкретные ЧИСЛА границ ЗАПРЕЩЕНО** (`product_tokens_max == 500000` и т.п.): значения — runtime-данные каждого бэка, у реальной реализации они другого порядка; такой ассерт красный на легитимных данных. Проверять только имена ключей, типы и правило обработки;
- бэк уровня v1 (`404` на `PATCH`/`pricing`) → `502 backend_admin_extension_not_supported`, при этом **страница «Юзеры бэков» продолжает работать** (регресс-гейт: вызов расширения не ломает v1-функции).

**Совместимость v1 — обязательные кейсы** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §1.1):
- ответ бэка **без** новых полей (`tokens`/`avatar_tokens`/`grantable`/`updated_at` в `products`; `tokens_spent`/`provider_cost_usd`/`refunded` в `requests`) валиден и **не** даёт `502`; отсутствующее поле нормализуется в **`null`** (ассертить именно `null` в ответе CRM, а не отсутствие ключа) — **включая `refunded`: его тип `bool | null`, и `null` ≠ `false`** ([04-api.md](04-api.md#backend-users));
- **асимметрия `/pricing`:** элемент `pricing` **без** `tokens` (или без `tariff_id`/`kind`) → **`502 backend_admin_unavailable`** (contract mismatch), а **не** `null`. Кейс обязателен: он стережёт, чтобы «опциональность» продуктов не была скопирована на тарифы;
- `GET /api/backend-users/{id}/products` **не шлёт** параметр `scope` (умолчание `grantable` сохраняет поведение формы «Установить план»), а `GET /api/backend-economics/{id}/products` шлёт **`scope=all`** (ассертить строку query фактического upstream-запроса).

### Контрактные
- Проверка соответствия ответов схемам из [04-api.md](04-api.md) (Pydantic-схемы = нормативный контракт; тест сверяет ключевые поля и коды ошибок).
- При расхождении docs↔код — `blame: "spec"` или `blame: "code"` в отчёте qa.

### Broadcast и knowledge-bot ([ADR-076](adr/ADR-076-knowledge-bot-broadcast-and-admin-level.md))

Интеграционные (тестовая PostgreSQL; Telegram Bot API и HTTP бота — **замоканы**):

- `POST /api/external/knowledge-bot/link` с валидным `X-API-Key`: bootstrap по `username` → 200 + строка в `knowledge_bot_links`; повтор → тот же `started_at`, `dead_at IS NULL`.
- Неизвестный telegram / неактивный / системный якорь → `404 user_not_linked`, строки нет.
- Пустой `DOCUMENTS_API_KEY` → `503`; неверный ключ → `401`.
- `GET /api/external/documents/user-access/{id}`: шаг 1 (knowledge-линк) побеждает sms/mail; query `username` включает bootstrap.
- `GET /api/users`: `bot_started=true` только при активном knowledge-линке; mail/sms-линк **не** поднимает флаг.
- `GET /api/broadcasts/audience` под `broadcast:view` без `roles:view` — роли непусты; системный якорь и неактивные не в счётчиках.
- `POST /api/broadcasts`: `all=true` + `role_ids` → `422`; пустой текст / >4096 → `422`; пустой токен → `503 knowledge_bot_not_configured`.
- Fan-out: два линка одного user_id → одно сообщение (дедуп); пользователь роли без линка → только `skipped_not_started`; мок 403 Telegram → `dead_at` проставлен, `failed++`, ответ `200`; частичный успех → `200` с `sent`/`failed`.
- `POST /api/broadcasts` без `broadcast:send` → `403`; Users API для роли «Админ» с полным каталогом после backfill → не `403`.
- Контракт-тест каталога: последний ключ `broadcast` с `("view","send")`; сид `admin` после миграции `0037` покрывает полный каталог.

## Frontend (Vitest + Testing Library; Playwright для E2E)

### Unit / компонентные
- **Gauge**: при `usage=65,zone=green` отрисован зелёный градиент и число `65` (**без знака `%`, без подписи «Usage», без меток `0%`/`100%`** — проверить их отсутствие в DOM); ARIA сохраняет процент (`aria-valuenow=65`); смена зоны при 85 (yellow), 95 (red); анимация не ломает значение.
- **Логика зон**: чистая функция `usageToZone(value)` — те же границы, что и backend.
- **ServerCard**: рендер статуса «В сети»/«Не в сети» (точка), uptime-форматтер (`1323120s` → `15д 7ч 32м`; формула `d=n//86400; h=n%86400//3600; m=n%3600//60` ⇒ 15·86400 + 7·3600 + 32·60 = 1323120), подпись «Обновлено».
- **Локализация**: статусы провижининга (`pending`→«Ожидание», `installing`→«Установка…», `online`→«В сети», `error`→«Ошибка»); относительное время (59с→«только что», 120с→«2 мин назад», 7200с→«2 ч назад»); формы мн.ч. ядер (1→«1 ядро», 2→«2 ядра», 5→«5 ядер», 8→«8 ядер», 11→«11 ядер», 22→«22 ядра»); единицы `ГБ`. Словарь — [08-design-system.md](08-design-system.md#локализация-ui-русский-словарь-строк).
- ~~**AddServerCard**~~ (**упразднена** — [ADR-046](adr/ADR-046-ui-infra-fix-pack.md) §2б; тест заменяется на «кнопка «Добавить» в правой зоне заголовка при `servers:create`, клик открывает модалку»): blur/glass-стиль, клик открывает модалку.
- **AddServerModal**: валидация полей (имя/IP/user/пароль), состояние loading, ошибки 409/422 от API. **Переключатель «Пароль / SSH-ключ» ([ADR-067](adr/ADR-067-server-ssh-key-auth.md) §6):** по умолчанию активен «Пароль»; выбор «SSH-ключ» показывает «Приватный ключ» + «Парольная фраза (опц.)» и **скрывает** «Пароль»; **переключение туда-обратно ОЧИЩАЕТ поля другого режима** (обязательный кейс — иначе в тело уйдут оба материала и сервер вернёт `422`, которого пользователь не создавал); в payload — ровно один материал + `auth_method`; `422` с `details[].field ∈ {ssh_private_key, ssh_key_passphrase}` подсвечивает соответствующий контрол; в режиме `edit` переключателя **нет**.
- **ServerDetailModal ([ADR-067](adr/ADR-067-server-ssh-key-auth.md) §6):** строка **«Способ входа»** отражает `auth_method`; при `password` — строка «Пароль» с кнопкой-глазом (под `servers:edit`); при `key` — строка «SSH-ключ» с маской и **без кнопки-глаза даже у супер-админа** (обязательный кейс: глаз отсутствует **при любых правах**, а не «скрыт из-за отсутствия `edit`»), и **ни одного запроса** к reveal-эндпоинту не уходит.
- **DocumentEditor — изображения ([ADR-068](adr/ADR-068-documents-image-attachments.md)):** кнопка тулбара и `Ctrl+V` ведут в **один** upload-путь и вставляют ноду с `src` из поля `url` ответа (клиент URL не собирает); **`data:`-URI в сериализованном `content_md` отсутствует** (гейт `allowBase64:false`) — обязательный кейс на вставку картинки из буфера; **drag-and-drop файла не загружает** (`handleDrop` не перехвачен); отрисовка идёт через `fetch` с заголовком `Authorization` (ассертить наличие заголовка) и `createObjectURL`, а на размонтировании вызывается **`revokeObjectURL`**; ответ `404` → плашка «Изображение недоступно» + alt, редактор не падает.
- **Состояния**: skeleton при загрузке, empty state (нет серверов, только «+ Добавить»), toast при ошибке/успехе.
- **a11y**: focus-ring, aria-атрибуты gauge (`role="meter"`, `aria-valuenow/min/max`), контраст.
- **Форма ящика ([ADR-054](adr/ADR-054-mail-app-password-field.md)):** поле секрета доступно по лейблу **«Код приложения»** (старый `«Пароль (IMAP)»` в DOM **отсутствует**); оно — **второе** поле формы (сразу после «Адрес почты»: сверить порядок в DOM); подсказки `add`/`edit` и ошибка «Укажите код приложения» — по словарю; **«SMTP-пароль (опц.)» на месте** и не переименован; в payload уходит поле **`password`** (контракт не менялся). ⚠️ Существующие тесты по старому лейблу (`MailboxFormModal.test.tsx:225`, `MailboxFormModalTimeouts.test.tsx:114`/`:216`) — **переписать** (штатный хендофф на qa).
- **Фильтр «Команда» — порог 2 ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §6), по кейсу на КАЖДЫЙ из ПЯТИ экранов** (1 — `/mail` «Сообщения», 2 — `/mail` «Почты», 3 — `/sms` «Сообщения», 4 — Mini App `/tg/mail`, 5 — Mini App `/tg/sms`): при `me.<channel>_teams.length === 1` и `includes_unassigned=false` контрол **не рендерится вовсе**; при 2 командах (или 1 команда + «Без команды») — рендерится. Опции берутся **из `/api/auth/me`** у **любого** актора (запроса к `GET /api/teams` быть не должно — ассертить отсутствие вызова **и под admin-уровнем тоже**). Выбор «Без команды» → серверный **`no_team=true`** (у клиентского фильтра вкладки «Почты» — `team_id === null`).
- **Актор admin-уровня в Mini App (регрессия дефекта редакции 1 ADR-055, [ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §5.1) — ОБЯЗАТЕЛЬНЫЙ кейс на ОБЕИХ Mini App (`/tg/mail`, `/tg/sms`):** при `me.sees_all_<channel>_teams=true` фильтр «Команда» **рендерится И содержит команды** (`me.<channel>_teams` непуст — все команды системы) + опцию «Без команды»; **пустой контрол (0 команд) = дефект**. Ассертить, что `GET /api/teams` **не вызывается**. Состояние достижимо в проде: БД-пользователь с полным каталогом прав проходит Telegram-линковку (запрещена только консольному супер-админу).
- **Mini App почты ([ADR-056](adr/ADR-056-mail-mini-app-mailbox-context.md)):** карточка и деталь рендерят пилюли **«Номер: …»** (`accent`), **«Приложение: …»** (`yellow`) и команду (`green` / `neutral` «Команды нет»); пустое значение → **`-`** (пилюля всё равно есть); строка **«Получено на: {email}»** (без `display_name`).
- **Форма пользователя ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §6.1):** блоки «СМС»/«Почты» **свёрнуты** по умолчанию; базовая команда внутри блока — **checked + disabled**; снятие команды в блоке «Команды» делает её чекбокс в блоке канала обычным **неотмеченным**; в запрос уходят **только добавки** (`*_extra_team_ids` без базовых) + `*_extra_includes_unassigned`; под чекбоксом «Без команды» — **обязательная подсказка объёма**, и в блоке «СМС» она **другая**, чем в «Почтах» (нормативные строки — [08-design-system.md](08-design-system.md#блоки-смс-и-почты-в-форме-пользователя-нормативно-adr-055)).
- **Источник команд канала на клиенте ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §6.3; регрессия прод-бага 2026-07-14) — по кейсу на КАЖДЫЙ контрол:** под не-админом (`sees_all_* = false`, права канала полные, `teams:view` **нет**) — (а) **форма ящика** (`MailboxFormModal`, `add` **и** `edit`): селектор «Команда» содержит команды из `me.mail_teams`, опции **«Без команды» в DOM НЕТ**, запроса к `GET /api/teams` **не происходит** (ассертить отсутствие вызова), создание ящика в своей команде проходит; **отдельный ОБЯЗАТЕЛЬНЫЙ кейс на исключение «зеркало текущего состояния» ([ADR-055](adr/ADR-055-per-channel-teams-mail-sms.md) §6.3.1):** не-админ с `mail_includes_unassigned=true` открывает на **`edit`** **бесхозный** ящик (`team_id = null`) ⇒ селектор **`disabled`**, его значение — **«Без команды»** (ассертить именно отображаемое значение: чужая команда в контроле = дефект), опция выбрана быть **не может**; тот же не-админ в режиме **`add`** опции «Без команды» **не видит**; (б) **таблица вкладки «Почты»**: имя команды в колонке резолвится (не «—»/пусто), `team_id === null` → «Без команды»; (в) **`Select` переноса номера** (`/sms`, право `sms:transfer`): опции = `me.sms_teams`, опция «Без команды» есть **только** при `me.sms_includes_unassigned=true`. **Под admin-уровнем — те же контролы наполняются из ТОГО ЖЕ `me.<channel>_teams`** (там все команды системы), `GET /api/teams` **не вызывается** ни в одном из них; опция «Без команды» в **форме ящика** показывается только admin-уровню (гейт `me.sees_all_mail_teams`, не `includes_unassigned`).

- **Страница «Продукты и тарифы» ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md)) — по кейсу на КАЖДОЕ состояние и КАЖДЫЙ гейт:**
  - **себестоимость (регресс-гейт §5):** `provider_cost_usd: null` → в DOM **`—`**, и **строки `$0.00` в этой ячейке НЕТ**; `provider_cost_usd: 0` → **`$0.00`**; `0.0004` → **`$0.0004`** (не `$0.00` — формат до 4 знаков). То же для `tokens_spent` (`null` → `—`). При наличии хотя бы одного `—` — **легенда** под таблицей;
  - `refunded: true` → пометка возврата **текстом** (не только цветом), `tokens_spent` **по-прежнему показан**;
  - **карандаш скрыт без права:** нет `backend-economics:edit` → контролов правки в DOM нет; есть `edit`, но **`capabilities: null`** → контролов правки **тоже нет** (fail-closed); есть `edit` + `features` **без** `pricing.write_tokens` → карандаши в таблице продуктов есть, в таблице тарифов — **нет** (независимость двух таблиц); есть `edit` + фича есть, но **`tokens: null`** у элемента → карандаш этой строки **не рендерится** (править нечего);
  - **бэк уровня v1 ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §1.1) — ТРИ отдельных кейса на таблицу «Продукты»:** (1) `items` непуст, **ни один** элемент без `tokens` → **жёлтый информационный блок** (тот же, что у тарифов), таблицы в DOM нет; (2) **частично** (часть элементов с `tokens`, часть без) → **таблица есть**, жёлтого блока **нет**, у элементов без `tokens` ячейка **`—`** и карандаша нет; (3) **`items: []`** → **empty state «Продуктов нет»**, а **НЕ** жёлтый блок (обязательный кейс: спутать пустой каталог с отсутствием расширения — ровно тот дефект, против которого писано правило);
  - **архив продуктов ([ADR-073](adr/ADR-073-products-archive-and-price-columns.md)) — по кейсу на каждое правило:** (1) по умолчанию в таблице **нет** строк с `archived: true`, а после включения переключателя они появляются с пилюлей «В архиве»; (2) **колонок «Цена» и «Период» в DOM НЕТ** (негативный ассерт — они сняты нормой, а не забыты); (3) контрол архива не рендерится **без `products.write_archived`** в `features` — **даже при наличии `products.write_tokens`** (обязательный кейс: фичи не взаимозаменяемы); (4) контрол архива **рендерится у строки с `tokens: null`** (правило read-only на него не распространяется); (5) ни один элемент не несёт `archived` ⇒ **переключателя и контролов в DOM нет**, все продукты показаны; (6) **все элементы архивные** при выключенном переключателе ⇒ строка-подсказка «Все продукты в архиве…», а **НЕ** «Продуктов нет» и **НЕ** жёлтый блок (негативный ассерт на обе строки); переключатель при этом виден;
  - **тост архивной правки несёт окно применения ([ADR-073](adr/ADR-073-products-archive-and-price-columns.md) §7):** при `effective_after_seconds` в ответе обе строки («скрыт с витрины» / «возвращён на витрину») содержат хвост **«Применится в течение {N} с»**; при отсутствии поля хвост **опускается**, остальная часть строки не меняется (⛔ негативный ассерт: безусловного утверждения о состоявшемся переходе без окна быть не должно);
  - **`grantable: null` → `—`, а НЕ «Нет»** (ассертить отсутствие строки «Нет» в этой ячейке): «не отдано» ≠ «не выдаётся»;
  - **валидация в вызывающем коде:** ввод пустого/нечислового/отрицательного значения → **`toast.error`**, **запроса `PATCH` НЕ уходит** (ассертить отсутствие вызова), **значение ячейки не изменилось**, поле **вернулось в просмотр**. ⛔ **НЕ ассертить «поле осталось в режиме правки»** — это невыполнимо до закрытия [TD-079](100-known-tech-debt.md) (`InlineEditField.commit()` закрывает режим безусловно, `frontend/src/components/InlineEditField.tsx:45-48`);
  - **`backend_admin_extension_not_supported` на `PATCH`** (список уже загружен) → **тост** с сообщением кода, строка возвращается в просмотр, **таблица НЕ подменяется жёлтым блоком** (обязательный негативный ассерт: строки «Бэк не отдаёт продукты и тарифы» в DOM нет — на непустой таблице она была бы ложью);
  - **пустой список бэков** (`GET …/backends` → `items: []`): рендерится строка «Нет приложений с Admin API Key…», **селектор в DOM отсутствует** (негативный ассерт), запросов к спискам продуктов/тарифов **не уходит**;
  - **`refunded`:** `true` → пометка «Возврат»; `false` → пометки нет; **`null` → пометки тоже нет** (и `tokens_spent`/`provider_cost_usd` в этой строке `—`);
  - **`limits`:** значение выше `product_tokens_max` → `toast.error`, запрос не уходит; **ключа `product_tokens_max` НЕТ** (или `limits`/`capabilities` отсутствуют) → **проверка границы не выполняется, запрос УХОДИТ** (обязательный кейс: отсутствие лимитов не должно блокировать правку) — валидация «число/целое/`≥ 0`» при этом продолжает работать. ⛔ Границу брать **из фикстуры ответа**, конкретное число **в код и в ассерт не зашивать** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §7.2);
  - **подсказка при `capabilities === null` — ДВА кейса:** держатель `backend-economics:edit` видит строку «Правка недоступна: бэк не подтвердил поддержку изменения значений» (карандашей при этом нет); держатель только `view` — **той же строки в DOM НЕТ** (обязательный негативный ассерт: подсказка адресована тому, у кого право есть);
  - **успех — ТРИ кейса по составу тела** ([ADR-072](adr/ADR-072-crm-admin-api-v11-economics.md) §8): (1) правка только `tokens` → тост «Токены продукта: {previous_tokens} → {tokens}»; (2) правка только `avatar_tokens` → тост **«Аватар-токены: …»**, где «было» взято из **отрисованного до правки** значения, и **⛔ обязательный негативный ассерт: `previous_tokens` в тосте НЕ фигурирует** (регресс-гейт на прод-дефект «Токены продукта: 1000 → 60» при правке `50 → 60`); (3) правка обеих величин одним `PATCH` → в тосте **обе** дельты. Во всех трёх — задержка из `effective_after_seconds`;
  - **фолбэк без дельты:** прежнее значение аватар-токенов клиенту недоступно → тост **«Аватар-токены: {current}…»** без «было»; дельта **не выдумывается**;
  - **неполный ответ `PATCH` ([ADR-073](adr/ADR-073-products-archive-and-price-columns.md) §8):** ответ без `changed` → показывается тост **успеха**, а НЕ нейтральный «Значение не изменилось»; ответ без `effective_after_seconds` → тост **без** предложения о задержке; ответ без `previous_tokens` → тост **без дельты**. Ни один из трёх случаев не даёт ошибочного состояния страницы; `changed: false` → **нейтральный** тост «Значение не изменилось», не ошибка;
  - **`backend_admin_extension_not_supported`** → информационный **жёлтый** блок с нормативным текстом, **кнопки «Повторить» в DOM НЕТ**; это **не** красная ошибка и **не** «пользователь не найден»;
  - **`backend_admin_conflict`** → тост + **refetch** списка (ассертить повторный запрос);
  - **селектор приложения:** опции — из `GET /api/backend-economics/backends`, запроса к `GET /api/backends` **не происходит**; **пункта «Все приложения» в DOM НЕТ**; пока бэк не выбран — запросов списков нет; **подпись опции — `«{name} — {code}»`** (одна строка; порядок частей ассертить явно — он **единый** с фильтром страницы «Юзеры бэков», где формат раньше был обратным);
  - **форма «Установить план» ([ADR-073](adr/ADR-073-products-archive-and-price-columns.md) §5):** архивный продукт в ответе `scope=grantable` **остаётся в списке опций** (негативный ассерт: он НЕ отфильтрован — выдача архивного плана законна) и его подпись содержит суффикс **« (в архиве)»**; у неархивного суффикса нет;
  - **инвалидация:** успешный `PATCH` продукта инвалидирует **и** кэш списка продуктов страницы «Юзеры бэков» (форма «Установить план» показывает тот же продукт) — ассертить повторный запрос по обоим ключам.
- **Навигация:** пункт **«Продукты и тарифы»** рендерится ⇔ есть `backend-economics:view`; пункт **«Юзеры бэков»** — ⇔ `backend-users:view`; пункт **«Рассылка»** — ⇔ `broadcast:view`; порядок ряда — по [08-design-system.md](08-design-system.md#навигация-плоская-applayout) (13 пунктов).
- **`/users`:** бейдж **«Бот»** / **«Бот не запущен»** из `bot_started` (тот же `Badge`, green/red). Роль «Админ» с `me.is_admin_level=true` видит пункт «Пользователи»; роль с видимым CRUD без `share`/`broadcast` (`is_admin_level=false`) — нет.
- **Матрица `/roles`:** столбцы = объединение действий `GET /api/permissions/catalog` (есть `share`/`send`/`sync`/`tags`/`transfer`); контракт «каждый ключ `CATALOG` имеет `PAGE_LABEL`/`ACTION_LABEL`» ([TD-071](100-known-tech-debt.md) — гейт завести вместе с закрытием TD-068). Каталог запрашивается **на `/roles`**, не в `AppLayout` ради admin-уровня.
- **`/broadcast`:** «Всем» дизейблит роли; submit шлёт `all=true` без `role_ids`; toast из `sent`/`failed`/`skipped_not_started`; `503` на **POST** → «ИИ-бот не настроен» (GET `/audience` токен не проверяет и 503 не отдаёт); кнопка «Отправить» скрыта без `broadcast:send`; **accessible name** чекбокса роли = `{name} (получат: N, без бота: M)` ([ADR-077](adr/ADR-077-broadcast-page-visual-redesign.md) — видимый `label` = только имя; `Badge` вне `label`). Дополнительно (редизайн, [ADR-077](adr/ADR-077-broadcast-page-visual-redesign.md)): есть видимый legend **«Аудитория»**; счётчик **`{n} / 4096`** связан с textarea через `hint`; live-region сводки — **text content** sr-only **«Получат: N · Без бота: M»** (ассертить text content, **не** `aria-label`; видимые ячейки `aria-hidden`); композер в `ui/Card`; loading/error/empty-карточки без регресса. **Раскладка (амендмент ADR-077, implemented):** у композера и loading/error/empty 503 — общий предок с `items-center` + `justify-center` + `min-h` (не `h-`/`max-h-`/`overflow-y-auto`); `InsufficientPermissions` этим предком не обёрнут; `AppLayout` / `isFullBleed` без регресса.
- **`AdminRoute` / пункт «Пользователи» ([ADR-078](adr/ADR-078-me-is-admin-level.md)):** гейт — `me.is_admin_level` (стор из `/me`, персист `crm.auth.isAdminLevel`). При `true` — ссылка и страница, **без** запроса `GET /api/permissions/catalog` и **без** Spinner `catalogPending`. При `false` — пункт скрыт, прямой URL → заглушка «нет доступа к разделу». Ошибка каталога **не** влияет на гейт (каталог для него не нужен). Негатив: запроса catalog из `AppLayout` нет (ассертить отсутствие вызова, если мок каталога включён только на `/roles`).
- **Ошибка `/me` ≠ `401` ([ADR-078](adr/ADR-078-me-is-admin-level.md)):** персист `crm.auth.isAdminLevel=true`, `GET /api/auth/me` падает с 5xx/сеть (не 401) → пункт «Пользователи» **остаётся**, сессия **не** сбрасывается, `GET /api/permissions/catalog` **не** вызывается. `401` на `/me` → сброс сессии (регресс не ломать).
- **«Сменить видимость» ([ADR-078](adr/ADR-078-me-is-admin-level.md)):** `me.permissions.documents` содержит `share` → пункт kebab **есть** (даже если клиентский `coversFullCatalog` не вызывался / каталог упал). `me.is_admin_level=true` при пустом/устаревшем `permissions` без `share` → пункт **есть** (фолбэк). Нет `share` и `is_admin_level=false` → пункта нет.
- ⚠️ **Прогон `tsc -b` — с удалённым `tsbuildinfo`**: инкрементальный кеш маскирует TS6307 ([ADR-066](adr/ADR-066-tailwind-config-in-app-tsproject.md) §Урок).

### E2E (Playwright, против поднятого стека или замоканного API)
- **E2E-1**: двухшаговый вход (логин → пароль) → редирект на **первую доступную по правам вкладку** плоского меню (permission-aware; для супер-админа — первый лист порядка `mail → …`; порядок `DefaultRoute` не меняется при переходе на плоскую навигацию). «Дашборд» из меню убран, доступен по прямому URL ([ADR-022](adr/ADR-022-teams-nav-categories.md), [ADR-033](adr/ADR-033-flat-nav-theme-toggle-numbers-table.md), [08-design-system.md](08-design-system.md#навигация-плоская-applayout)).
- **E2E-2**: список серверов с метриками отрисован.
- **E2E-3**: добавление сервера → модалка → отправка → карточка в статусе `pending/installing` → (с замоканным провижинингом) → `online`.
- **E2E-4**: неверный пароль → ошибка, без перехода.

## Runtime-smoke стека (перед релизом)

- Помимо статической проверки (`docker compose config`) обязателен **рантайм-smoke**: поднять стек `docker compose up -d`, дождаться `healthy` всех контейнеров, проверить `GET /api/health` → `200`, отдачу SPA (`GET /` → `200`) и запуск `proxy` (TLS-серт сгенерирован). Ловит рантайм-баги, невидимые статической проверкой (CRLF в entrypoint → `exit 127`, отсутствие `openssl`). Детали и чек-лист — [07-deployment.md §CI/CD](07-deployment.md#cicd); автоматизация в CI — [TD-014](100-known-tech-debt.md).

- **⚠️ Права каталогов под non-root — обязательный пункт smoke (класс дефекта «ломает всё в проде, невидимо в тестах»).** Образ работает под `USER app`, а Docker монтирует пустой named-volume и `tmpfs` **под `root:root`** ⇒ ошибка в правах не ловится ни юнит-, ни интеграционными тестами (там нет контейнера) и проявляется только на реальном стеке. В поднятом стеке проверить **изнутри контейнера backend**:
  - **шаг 0 (до `up`) — `docker compose config`.** Поле `tmpfs.mode` числовое (`uint32`), поэтому значение **в кавычках** отвергается ещё на разборе: `decoding failed … 'services[backend].volumes[N].tmpfs.mode' expected type 'uint32', got unconvertible type 'string'`. Стек при этом не поднимается вовсе — отказ ловится этим шагом, до `up`, и правится заменой на `mode: 0o1777`;
  - `docker compose exec -u app backend python -c "import tempfile,os; d=tempfile.mkdtemp(dir=os.environ['ANSIBLE_PRIVATE_DATA_ROOT']); print(d)"` → путь, **без `PermissionError`** (иначе `tempfile.mkdtemp` упадёт и **любое** добавление сервера вернёт ошибку — включая парольную ветку; типовая причина — режим `tmpfs`: либо явный `mode=1700`, либо **голое `mode: 1777` в compose, которое разбирается ДЕСЯТИЧНО** и даёт `0o3361` вместо `1777`. Диагностика на месте: `docker compose exec -u app backend python -c "import os,stat;print(oct(os.stat(os.environ['ANSIBLE_PRIVATE_DATA_ROOT']).st_mode))"` → обязано быть `0o41777`; `0o43361` = тот самый десятичный разбор ⇒ править на `mode: 0o1777` — **без кавычек** (кавычки дадут отказ разбора из шага 0) ([ADR-067](adr/ADR-067-server-ssh-key-auth.md) §5, [07-deployment.md §tmpfs](07-deployment.md#tmpfs-для-приватных-данных-ansible-нормативно-adr-067-5));
  - `docker compose exec -u app backend python -c "import os; p=os.environ['DOCUMENTS_ATTACHMENTS_DIR']; os.makedirs(p+'/aa/bb', exist_ok=True); open(p+'/aa/bb/.probe','w').close()"` → успех (иначе первая же загрузка изображения упадёт на создании шард-каталога — [ADR-068](adr/ADR-068-documents-image-attachments.md), [07-deployment.md](07-deployment.md#каталоги-создаваемые-в-образе-нормативно));
  - `docker compose exec backend id app` → **`uid=999(app) gid=999(app)`**. Значение запинено в `backend/Dockerfile` и является **частью формата бэкапа** (в tar и на volume лежит число): расхождение здесь означает, что уже снятые архивы вложений после восстановления окажутся нечитаемыми для backend — [07-deployment.md §Пин uid/gid](07-deployment.md#пин-uidgid-пользователя-app-нормативно).

## Тестирование провижининга (Ansible)

- Плейбук проверяется на **идемпотентность**: повторный прогон `--check`/реальный прогон не даёт `changed` на втором запуске (NFR-6).
- Запуск против эфемерного контейнера/ВМ Linux с systemd (molecule или ручной smoke в CI-окружении с docker-in-docker) — отнесено к devops/qa, объём smoke зафиксирован в [09-provisioning.md](09-provisioning.md).
- Unit-уровень backend мокает ansible-runner (не запускает реальный SSH).
- **Happy-path провижининга нельзя проверять ТОЛЬКО моками** (усвоенный урок: моки скрыли отсутствие `sshpass` в backend-образе). Минимально обязательно:
  1. **Контейнерная проверка состава backend-образа:** в собранном образе присутствуют `ansible` (`ansible --version`), `sshpass` (`command -v sshpass`), `ssh`. Это дешёвый тест, ловящий отсутствие системной зависимости до рантайма.
  2. **При возможности — реальный прогон плейбука** против эфемерного тест-хоста (sshd + systemd) с **password-SSH** (чтобы задействовать `sshpass`-путь), проверка `:9100/metrics` и идемпотентности.
- Полноценный автоматизированный e2e-провижининг (реальный SSH-хост в CI) на Этапе 1 не автоматизирован — [TD-016](100-known-tech-debt.md). Контейнерная проверка состава образа (п.1) — обязательна и дёшева, должна быть включена как минимум.
- **Definition of done мониторинга — `up=1` (нормативно):** провижининг считается успешным ТОЛЬКО когда метрики реально текут (`up=1` в Prometheus), а не когда node_exporter «установлен». Тесты/приёмка happy-path обязаны проверять реальный скрейп: после добавления сервера в течение разумного окна `up{instance=...}==1` и `GET /api/servers/{id}/metrics` возвращает значения (`online=true`). Это покрывает классы отказов цепочки скрейпа (права file_sd `0644`/`0755`, firewall `:9100`), невидимые при проверке только установки. См. [09-provisioning.md](09-provisioning.md#definition-of-done-провижининга-нормативно).

## Что НЕ тестируем на Этапе 1

- Нагрузочное/стресс-тестирование (только ориентир NFR-5).
- Реальные внешние серверы в CI (используем контейнеры/моки).
- Совместимость с Windows-таргетами (вне scope).

## Тестовые данные и окружение

- Фикстуры серверов с разными `provision_status` и зонами нагрузки.
- Замоканные ответы Prometheus (JSON фикстуры реальных PromQL-результатов).
- Тестовая БД изолирована (отдельная схема/контейнер), миграции применяются перед прогоном.

## Definition of Done (для каждого модуля)

- [ ] Все указанные unit/интеграционные тесты зелёные.
- [ ] Coverage не ниже порога.
- [ ] Контракт API соответствует [04-api.md](04-api.md).
- [ ] Lint/format/type-check проходят (команды из [02-tech-stack.md](02-tech-stack.md)).
- [ ] Нет секретов в логах/коде/тестовых фикстурах.
